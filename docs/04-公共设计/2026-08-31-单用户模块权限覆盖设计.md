# 单用户模块权限覆盖设计

| 属性 | 内容 |
|---|---|
| 日期 | 2026-08-31 |
| 状态 | 第二阶段正式实施基线 |
| 范围 | GAIOP-Admin 页面、REST、SSE、Gateway RPC 与 SQLite |

## 1. 目标与不变量

本阶段在 `basic/auditor/standard/admin` 四角色默认矩阵之上增加单用户模块覆盖。没有覆盖时完全沿用角色默认；`allow` 强制增加，`deny` 强制削减；删除覆盖恢复角色默认。覆盖只属于目标用户，不改变角色或同角色其他用户。

以下边界不因模块覆盖改变：

- 对话工作台不进入可配置目录，保持现有角色、本人会话归属和 SSE 会话隔离。
- 报告列表、预览和下载继续先执行 `source_user_id` 归属；无权访问统一 404。
- 会话列表、历史、操作和媒体继续执行 `workspace_sessions.owner_user_id` 归属。
- 告警记录、通知和导出仍是系统级脱敏数据；导出只接受当前页六字段且最多 100 条。
- 审计、认证、首次改密、初始管理员、最后管理员、敏感字段投影和退役接口 410 不可绕过。
- 模块允许只开放该模块已登记的入口、REST/SSE 和 Gateway 能力。初始管理员专属或现有高风险写动作继续执行原有身份/动作保护，不把某个模块的允许扩张为管理员全局写权限。

有效权限只由服务端固定目录和 resolver 计算。前端不得从角色重新推算默认权限。

## 2. 固定模块目录

风险枚举固定为 `low/medium/high/critical`；分组固定为“业务管理、系统运维、高级管理”。`dataScope` 是覆盖生效后仍必须执行的数据范围。

| key | 分组 | 模块/导航 | 路由 | REST | SSE | Gateway RPC | 风险 | 数据范围与动作边界 |
|---|---|---|---|---|---|---|---|---|
| `dashboard` | 业务管理 | 仪表盘 | `/` | `GET /api/dashboard/summary`、`GET /api/dashboard/usage` | 无 | 通过 BFF 聚合会话 Usage；不直接扩大 RPC | low | basic/standard 仍只重算本人会话；auditor/admin 为全量只读 |
| `alerts.records` | 业务管理 | 告警记录 | `/alerts` | `GET /api/alerts`、`GET /api/alerts/time` | 无 | 无 | medium | 系统级脱敏告警；不提供 Receiver、规则或系统配置写入 |
| `alerts.notifications` | 业务管理 | 告警通知/弹窗 | 全局铃铛与抽屉 | `GET /api/alerts/changes`、`GET/PUT /api/alerts/preferences` | `/api/events` 中的 `alert`、`alertStreamState` | 无 | medium | 可独立于 records；records 最终拒绝时隐藏详情跳转，通知偏好只属于当前账户 |
| `alerts.export` | 业务管理 | 告警导出 | `/alerts` 内操作 | `POST /api/alerts/export` | 无 | 无 | medium | 依赖 `alerts.records`；仅当前页六字段、最多 100 条，不重新查询 Receiver |
| `sessions` | 业务管理 | 会话管理 | `/sessions`、`/sessions/:key` | `GET/POST/PUT /api/session-retention*` 按原动作边界 | `/api/events` 中可归属的 session 事件 | `sessions.*`、`session.*` 的管理页能力 | high | 本人/全量/只读范围保持；与对话工作台共用的 RPC 继续受不可配置的工作台能力保护，不能用 deny 破坏对话 |
| `reports` | 业务管理 | 报告文件管理 | `/files` | `GET /api/reports`、预览、下载；DELETE 仍执行原高风险边界 | 无 | 无 | high | basic/standard 仅本人；auditor 全量只读；admin 全量；无权统一 404 |
| `cron` | 业务管理 | 任务计划 | `/cron` | 无 | 相关 session 事件仍按会话归属 | `cron/crons/schedule/schedules` 已登记方法 | high | 读取可由模块允许开放；创建、修改、执行、删除继续执行既有高风险动作边界 |
| `memory` | 系统运维 | 记忆管理 | `/memory` | 无 | 无 | `agents.files.list/get/set`、必要的 `agents.list/config.get` | high | 只限正式 agent 文件 RPC；写入继续执行现有管理动作边界，不开放通用文件接口 |
| `models` | 系统运维 | 模型管理 | `/models` | 无 | 无 | `models.list/model.list`、`config.get`、`agent.model.set` | high | 非管理员使用安全投影；模型切换继续执行现有高风险动作边界 |
| `channels` | 系统运维 | 频道管理 | `/channels` | `/api/channels*` | Gateway 状态仍为固定基础事件 | `channels/channel/plugins` 已登记方法 | high | 非管理员只返回安全运行状态；凭据、配对、接入和配置写入保持原边界 |
| `skills` | 系统运维 | Skills 管理 | `/skills` | 无 | 无 | `skills.status/list/install/update` | high | 非管理员只读安全投影；安装、更新保持原管理动作边界 |
| `system` | 系统运维 | 系统监视器 | `/system` | `GET /api/system/metrics`、`GET /api/system/storage-watermarks`、`GET /api/status` | `gatewayState` 为固定连接底座 | `system-presence`、`node.list`、`health/status` | medium | 只返回安全系统摘要；不提供文件、路径、清理或主机写入 |
| `agents` | 系统运维 | 多智能体 | `/agents` | 无 | session 事件按原归属 | `agents.*`、`agent.*`、agent files 的已登记方法 | high | 读取可按模块开放；创建、编辑、删除和文件写入保持原管理动作边界 |
| `office` | 系统运维 | 智能体工坊 | `/office` | `/api/wizard/scenarios*`、`/api/wizard/tasks*` | 相关 session 事件按原归属 | 仅页面实际使用的正式 session/agent 能力 | high | 任务和场景写入保持原管理动作边界；不开放遗留桌面/终端 |
| `users` | 高级管理 | 账户列表安全投影 | `/users` | `GET /api/users` | 无 | 无 | high | 只读账户安全字段；无密码哈希、Token 或首次改密内部字段；初始管理员本人锁定允许 |
| `userAdministration` | 高级管理 | 账户新增、编辑、重置、启停、删除与模块权限 | `/users/create`、`/users/:id/edit` | 用户写接口与 `/api/users/:id/module-permissions` | 目标用户 `permissionsChanged` | 无 | critical | 依赖 `users`；普通管理员/初始管理员层级、最后管理员、自身账户和密码规则继续生效；初始管理员本人锁定允许 |
| `audit` | 高级管理 | 审计信息 | `/audit-logs` | `GET /api/audit-logs` 及受控导出 | 无 | 无 | high | 全量只读安全投影；覆盖审计不记录密码、Token 或请求体 |
| `settings` | 系统运维 | 系统设置 | `/settings` | `/api/system-settings/sessions`、报告目录安全状态、当前账户告警偏好 | 无 | 必要的安全配置读取 | medium | 页面级允许不绕过各子项现有查看/管理动作边界 |
| `systemConfiguration` | 高级管理 | 高级配置（独立可达父入口） | `/system-configuration` 及子路由 | `/api/system-config/*`、`/api/data-sources*`、告警接入配置 | 无 | 必要的 `config.get/patch/apply/set` | critical | `/system-configuration` 保持正式侧栏父入口；敏感值只写不回显，连接测试/激活/配置写入保持高风险边界 |
| `systemUpgrade` | 高级管理 | 系统升级 | `/system-upgrade` | `/api/system-upgrade/*` | 升级进度沿用既有安全事件 | `update.run` 仅保留既有诊断用途 | critical | 概览可按模块开放；上传、执行、回滚、删除备份继续要求原高风险动作授权 |
| `platformBranding` | 高级管理 | 平台品牌配置 | `/platform-branding` | `/api/system-settings/branding` | 无 | 无 | high | 永久保留初始管理员身份锁；非初始管理员不能通过 allow 获得该能力 |

### 2.1 角色默认矩阵

下表是无覆盖时的唯一默认值，必须与当前第一阶段一致。

| key | basic | standard | auditor | admin |
|---|---:|---:|---:|---:|
| `dashboard` | 否 | 是 | 是 | 是 |
| `alerts.records` | 是 | 是 | 是 | 是 |
| `alerts.notifications` | 是 | 是 | 是 | 是 |
| `alerts.export` | 是 | 是 | 是 | 是 |
| `sessions` | 否 | 是 | 是 | 是 |
| `reports` | 是 | 是 | 是 | 是 |
| `cron` | 否 | 否 | 是 | 是 |
| `memory` | 否 | 否 | 否 | 是 |
| `models` | 否 | 否 | 否 | 是 |
| `channels` | 否 | 是 | 是 | 是 |
| `skills` | 否 | 是 | 是 | 是 |
| `system` | 否 | 是 | 是 | 是 |
| `agents` | 否 | 否 | 否 | 是 |
| `office` | 否 | 否 | 否 | 是 |
| `users` | 否 | 否 | 是 | 是 |
| `userAdministration` | 否 | 否 | 否 | 是 |
| `audit` | 否 | 否 | 是 | 是 |
| `settings` | 否 | 是 | 是 | 是 |
| `systemConfiguration` | 否 | 否 | 否 | 是 |
| `systemUpgrade` | 否 | 否 | 否 | 是 |
| `platformBranding` | 否 | 否 | 否 | 是（另需初始管理员身份） |

### 2.2 依赖与锁定

- `alerts.export` 依赖 `alerts.records`。最终允许 export 且 records 最终拒绝时，PUT/DELETE 均拒绝，不做隐式修正。
- `alerts.notifications` 可独立允许；无 records 时服务端仍发送脱敏通知，前端隐藏“查看详情”。
- `userAdministration` 依赖 `users`。
- `systemConfiguration` 自身就是侧栏正式父入口，不依赖 `settings`。
- 目标是初始管理员时，`users` 和 `userAdministration` 锁定为允许，避免关闭账户管理和权限管理核心能力。
- `platformBranding` 始终受初始管理员身份锁；覆盖不能提升其他用户为初始管理员。
- 锁定和依赖是服务端校验，前端禁用只用于解释原因。

## 3. 数据模型与迁移

新增表：

```sql
CREATE TABLE user_module_permission_overrides (
  user_id TEXT NOT NULL,
  module_key TEXT NOT NULL,
  effect TEXT NOT NULL CHECK (effect IN ('allow', 'deny')),
  updated_by TEXT NOT NULL,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  PRIMARY KEY (user_id, module_key)
);
```

`users` 新增 `permission_version INTEGER NOT NULL DEFAULT 0`。启动迁移幂等创建表、列和 `updated_by/updated_at` 索引；不修改既有用户角色、状态、密码或数据归属。`inherit` 不落库。

覆盖保存采用一个 SQLite 事务：读取目标用户与当前版本、校验 `expectedVersion`、删除旧覆盖、插入新覆盖、将 `permission_version + 1`、写结构化审计。事务失败不留下部分覆盖或版本变化。用户删除时同事务清理其覆盖。

## 4. 唯一 resolver

服务端 resolver 输入用户数据库行和该用户覆盖，按固定顺序计算：

1. 从固定目录读取角色默认 `defaultAllowed`。
2. 应用 `allow/deny` 得到候选 `effectiveAllowed`。
3. 应用初始管理员核心锁和品牌身份锁。
4. 校验依赖；持久化写入前依赖不满足直接拒绝，读取时若遇到历史异常数据则安全降级依赖项并报告诊断，不自动改库。
5. 投影 `moduleKey/defaultAllowed/override/effectiveAllowed/locked/lockReason/risk/group/dependencies/dataScope`。

认证 session 不作为权限权威缓存。每个受认证请求从 SQLite 刷新角色、状态、初始管理员标记、`permission_version` 和 `effectiveModules`，REST、SSE、RPC 共用同一投影。前端只保存服务端返回的 `effectiveModules` 与 `permissionVersion`。

## 5. 在线生效

保存或恢复默认成功后，BFF 立即：

1. 更新与目标用户关联的 SSE client 权限快照；
2. 只向目标用户发送 `{type:"permissionsChanged", permissionVersion}`；
3. 浏览器重新调用 `/api/auth/check` 获取完整投影；
4. 若当前路由失权，替换到无权限页；导航立即重算；
5. 断开并重建唯一 `/api/events`，使告警通知过滤与新权限一致。

不广播覆盖内容给其他用户。同角色其他用户的版本和连接不变化。目标用户被停用或删除时，先发送目标事件再关闭其既有 SSE 连接，避免已撤销账户继续接收数据。

## 6. 前端管理体验

入口位于单用户编辑页的“模块权限”卡片，仅初始管理员可操作。表格按三个固定分组显示模块、角色默认、最终允许复选框、跟随角色/个人增加/个人削减、风险/数据范围；支持搜索和只看个人调整。

切换只修改本地草稿。底部显示允许、拒绝和个人调整数量；保存前展示增加/削减差异。涉及 `high/critical` 的增加或削减必须二次确认。恢复默认提交空覆盖集合并同样使用版本检查。

角色编辑与权限覆盖分开保存。角色变化后重新读取权限投影，覆盖仍保留并在新角色默认上重新计算。

## 7. 测试不变量

- 四角色无覆盖逐项等于第 2.1 节。
- allow、deny、空集合恢复、角色变化和同角色用户隔离均覆盖。
- 未知 key、非法 effect、重复项、锁定项、依赖冲突、版本冲突、非初始管理员写入均拒绝。
- 导航、直接路由、REST、告警 SSE 和已登记 RPC 使用同一有效权限；共享对话 RPC 明确保留工作台固定边界。
- 报告本人 404、会话归属、告警系统级范围、审计与第一阶段 basic 告警/导出/偏好不回退。
