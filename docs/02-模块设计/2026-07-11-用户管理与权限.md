# 用户管理与权限模块设计

| 属性 | 内容 |
|---|---|
| 创建日期 | 2026-07-11 |
| 最后更新 | 2026-07-15 |
| 状态 | 基础版本已实现 |
| 代码依据 | `src/views/users/*`、`server/routes/users.js`、`server/lib/permissions.js`、`server/database.js` |

## 1. 功能

- 用户列表：更新时间、用户名、角色、描述、状态、操作，支持用户名/描述搜索、角色/状态筛选、前端分页和刷新。
- 新增：用户名、可选描述、四类角色、密码/确认、激活状态。
- 编辑：管理员可修改用户描述、角色和激活状态；用户名当前不可修改。
- 修改密码：用户修改本人密码；管理员可修改其他用户密码。
- 重置：管理员将密码重置为系统默认值，并注销目标用户已有会话。
- 删除：管理员删除用户；禁止删除自己和最后一个激活管理员。

## 2. 角色权限

| 范围 | 基础 | 审计 | 标准 | 管理员 |
|---|---:|---:|---:|---:|
| 查看普通页面 | 是 | 是 | 是 | 是 |
| 查看审计 | 否 | 是 | 否 | 是 |
| 对话、会话日常操作 | 否 | 否 | 是 | 是 |
| 任务和 Skills 常规写操作 | 否 | 否 | 是 | 是 |
| 用户、数据源、主机网络、安全配置 | 否 | 否 | 否 | 是 |
| 通道认证、智能体结构、底层配置 | 否 | 否 | 否 | 是 |

## 3. 数据与安全

用户存于 SQLite `users` 表；密码使用带随机盐的哈希，接口不返回哈希。用户名忽略大小写唯一，最长 64；密码当前至少 6 位；描述最长 500；状态为 `active/inactive`。

前端权限只用于体验，REST 中间件和 `/api/rpc` 决策是最终边界。非管理员读取 Gateway 配置时，密码、Token、API Key、Secret、Credential 等字段被掩码。

## 4. 异常

- 非法输入、用户名重复、用户不存在、密码过短、当前密码错误。
- 禁止删除当前用户、禁止删除最后一个激活管理员。
- 禁止修改当前登录管理员自身角色或将自身停用；禁止通过编辑移除最后一个激活管理员。
- 修改/重置密码后撤销其他活动 Token，避免旧会话继续使用。
- 角色或状态变化后撤销目标用户已有 Token，使新权限在重新登录后生效。

## 5. 验收标准

- 四类角色分别验证查看、对话、任务、Skills、用户和安全配置。
- 直接调用接口或 RPC 不能绕过前端禁用。
- 密码不在列表、审计、日志和响应中出现。
- 删除保护和会话失效机制有效。

## 6. 已知限制

尚无用户名修改、服务端分页搜索、登录锁定、首次改密和组织/租户；会话和报告数据范围未完全按用户隔离。默认重置密码属于过渡方案，生产前应要求首次登录修改。

## 7. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-07-15 | 增加用户搜索筛选、管理员编辑、当前账户保护、最后管理员保护和权限变化会话撤销 |
