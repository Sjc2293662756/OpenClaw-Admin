# 237 WebChat 报告下载卡片发布记录

## 1. 发布目标与边界

本次仅发布 GAIOP-Admin 的 WebChat 报告附件展示能力。当前用户回合完成且正式报告完成登记后，工作台和 Office 对话区会在助手回复后附加报告卡片；用户点击卡片可复用现有 `/api/reports/:id/download` 下载接口。报告查询与下载继续经过 Admin 登录态、会话归属和报告访问权限校验，不向浏览器暴露服务器绝对路径。

本次未修改 OpenClaw 核心、频道插件、GAIOP 后端、Gateway、Upgrade、Caddy、报告 Skill、报告文件、报告登记数据或留存策略；也未主动生成生产报告。

## 2. 代码版本与实现

- 发布分支：`dev-yangshuo`；
- 发布提交：`dbe158aa916089bb11caf988e605321e4fff6c28`；
- 实现提交：`ad098b1 feat(webchat): add report download cards`；
- 会话链路适配提交：`dbe158a fix(webchat): align report cards with completed turns`。

卡片关联以正式报告记录中的 `sourceMessageId` 为权威依据，并使用来源消息预览兼容 Gateway 消息 ID 投影变化。查询被限制在当前 WebChat 会话和当前用户回合；文件名与时间只作为会话内展示位置兜底，不跨会话猜测绑定。当前回合完成后按 `250ms / 1.5s / 3.5s / 7s` 重试，以覆盖生产报告归因工作器约 3 秒的处理窗口。历史消息没有可靠报告 ID 时保持原文本，不伪造下载关系。

## 3. 发布前验证

- 报告卡片专项测试：7 项全部通过；
- Vitest 全量：22 个文件、113 项全部通过；
- Admin Node 回归：194 项中 192 项通过，2 项为修改前同样存在的部署契约基线失败，失败集合未变化；
- `vue-tsc -b`：通过；
- 前端生产构建：通过，4651 个模块完成转换；
- `git diff --check`：通过；
- 发布代码工作树：干净；远端 `origin/dev-yangshuo` 已精确指向 `dbe158a`。

## 4. 237 受控发布

| 项目 | 结果 |
| --- | --- |
| 发布批次 | `20260824T111335Z` |
| 发布包 | `gaiop-admin-webchat-report-card-20260824T111335Z.tgz` |
| 发布包 SHA-256 | `c013d9fb7aafe486cffd6b8fbdcb90d78c67d0f7cd5000ff12d303ee5329149b` |
| 发布白名单 | `dist`、`server`、`package.json`、`package-lock.json`，414 个条目 |
| 本地与生产入口资源 | `assets/index-Cp76RS_K.js` |
| 本地 `dist/index.html` SHA-256 | `f245bf2119da4f3726ec1a84a7dd6db41a87038dff9fdd224140533a5f656085` |
| 工作台分包 | `ChatWorkspace-C7UsRZCz.js` |
| Office 分包 | `OfficePage-CzdCefT5.js` |
| 报告卡片分包 | `ReportAttachmentList-DNQMS3rK.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260824T111335Z` |

stage 在切换代码前完成当前 Admin 快照，并使用生产 Node 运行时和 `better-sqlite3` 在线备份 `wizard.db`、执行完整 `integrity_check`。发布包 SHA-256、解包白名单、Node 语法和 237 原生 `systemd-analyze verify` 均通过。start-verify 返回 `admin-started-loopback-verified`，监听范围为 `loopback-ipv4`，公网路由未改变。

## 5. 发布后验收

- Admin、Upgrade、Gateway、Caddy：均为 `active`；
- Admin 内部健康、Admin 根页面、Upgrade、Gateway、HTTPS 回环与公网检查：均为 HTTP `200`；Upgrade 未认证检查保持 `401`；
- Admin 监听：仅 `127.0.0.1:3000`；Upgrade 监听保持 `127.0.0.1:18900`；
- Admin `wizard.db` 与 Upgrade 数据库：`integrity_check=ok`；
- 生产入口资源：`assets/index-Cp76RS_K.js`，与本地本次构建一致；
- Gateway 会话使用量与缓存探针成功，1/7/30/90 天查询均正常；
- 发布前后业务计数一致：用户 6、工作区会话 91、正式报告记录 254、报告交付 9、审计 1907；未新增、删除或修改报告；
- 正式报告留存、Admin/Upgrade 第一批清理、Admin/Upgrade SQLite 备份和磁盘水位 timer 继续 `active/enabled`；
- 会话留存继续 `inactive/disabled`；
- 磁盘水位约 72%，受管业务文件系统状态正常。

## 6. 回滚说明

如需回退本次 UI/BFF 版本，使用回滚点 `/var/backups/gaiop/admin-prestage-20260824T111335Z` 恢复上一版 `/opt/gaiop/admin` 后重新执行 Admin 回环监听与健康验证。此次发布没有数据库结构或业务数据变更，回滚代码时不应使用备份覆盖实时生产数据库。浏览器中已打开的旧 SPA 标签页不会自动换载新分包，验收前需按 `Ctrl+F5` 强制刷新。

## 7. 2026-08-25 历史回拉适配补充修复

生产会话 `agent:main:main:dm:webchat-0d4429bac86648ba9593bf7255157744` 暴露出一项历史回拉边界：Gateway 已落盘用户消息带有入站时间前缀，而前端发送阶段保留的 `web-*` 乐观消息位于消息数组末尾。旧关联逻辑优先命中报告记录中的乐观消息 ID，但该消息之后没有助手回复，因此虽然正式报告记录、来源会话、来源用户和文件均正确，卡片仍无法挂到已完成的助手回复后。

提交 `d716825 fix(webchat): recover report cards after history merge` 将关联规则调整为：精确来源 ID 只有在同一用户回合内存在后续助手回复时才采用；否则移除 Gateway 入站时间前缀并使用已签名的来源预览，在下一条用户消息之前选择与报告时间最近的已完成回合。相同预览重复出现时按报告登记时间消歧，仍不跨用户回合或会话猜测绑定。

补充修复验证如下：

- 报告卡片专项测试增至 9 项，包含该生产消息形态和重复预览时间消歧；
- Vitest 全量 22 个文件、115 项全部通过；
- `vue-tsc -b`、生产构建、修改代码语法检查和 `git diff --check` 均通过；
- Admin Node 回归仍为 194 项中 192 项通过，失败集合与修改前一致，仍是两个既有部署契约基线失败；
- 受控发布批次为 `20260825T014138Z`，发布包 SHA-256 为 `25987e62d7f4d876621535af3c91a578148352b404e24117980ccf40fc0956c2`；
- 本次回滚点为 `/var/backups/gaiop/admin-prestage-20260825T014138Z`，生产入口为 `assets/index-BcXsEFi7.js`，本地 `dist/index.html` SHA-256 为 `03a43c2abeccf921612b7f61804518abee18814785c0efe7cf7493e0e1f0940f`；
- 发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`，内外健康检查均成功，Admin 仍只监听 `127.0.0.1:3000`，两库 `integrity_check=ok`；
- 业务计数为用户 6、工作区会话 92、正式报告记录 255、报告交付 9、审计 1912；本次未生成、修改或删除报告与会话数据；
- 正式报告留存、两套低风险清理、两套 SQLite 备份和磁盘水位 timer 继续 `active/enabled`，会话留存继续 `inactive/disabled`。

应用内浏览器当时没有保留 GAIOP 登录标签页，因此没有把“浏览器肉眼点击”冒充生产验收结论；生产入口哈希、目标会话真实元数据、修复前确定性复现、修复后专项用例和237健康探针共同构成此次验收依据。用户重新打开或强制刷新工作台后会加载新分包。

## 8. 2026-08-25 卡片刷新与对话用户下载权限收口

会话 `agent:main:main:dm:webchat-5c65e90e38584702bdf9274aa5f13daf` 进一步暴露出三项相互叠加的问题：

1. 生产助手回复只包含“报告已生成”“格式：docx”“将以附件形式发送”，没有字面量 `.docx` 路径；旧前端因此没有启动报告登记后的重查。
2. 完成状态按 Agent ID `main` 保存，旧页面没有校验状态所属的精确会话，多个 WebChat/Office 会话间可能漏掉或误用完成信号。
3. basic 用户虽然允许对话，但 Admin 的全局 REST 边界此前直接拒绝 `/api/reports` 与报告下载接口，导致没有报告管理器权限的对话用户即使生成了报告，也无法取得自己的卡片数据。

提交 `b6cf5c6 fix(webchat): make report cards reliable across roles` 完成以下收口：

- 将正式报告导出工具调用、报告完成文本和 `.docx` 引用统一识别为当前会话的登记刷新信号；按 `250ms / 1.5s / 3.5s / 7s / 15s` 重查，并在切换会话时取消旧会话任务。
- WebChat 和 Office 只接受与当前精确 `sessionKey` 一致的完成状态；发送和新会话接管阶段同步记录该会话键。
- basic 用户可读取按当前登录用户强制过滤后的报告列表，并下载属于自己的 ready 报告；报告预览、删除、留存、恢复区和管理接口继续禁止。standard/admin 原有对话与下载能力不变，auditor 继续保持只读且不具备报告生成能力，非属主下载继续隐藏为不可见。
- 公开卡片名和下载名在审计标题缺少扩展名时，从受信任的 `stored_name` 补齐真实扩展名；带 IP 小数点的数据源名称不会再被误判为文件扩展名。

测试与受控发布结果如下：

| 项目 | 结果 |
| --- | --- |
| 实现提交与远端 | `b6cf5c61ba1d2cd7e0a82f58db1643b45ea668ef`，`origin/dev-yangshuo` 已一致 |
| WebChat/权限/报告接口专项 | 47 项全部通过 |
| Vitest 全量 | 23 个文件、119 项全部通过 |
| Admin Node 回归 | 195 项中 193 项通过；仍只有修改前相同的 2 个留存发布器契约基线失败，失败集合未变化 |
| 类型与构建 | `vue-tsc -b` 通过；4651 个模块生产构建成功 |
| 语法与差异 | 修改 JS `node --check`、`git diff --check` 均通过 |
| 发布批次 | `20260825T023432Z` |
| 发布包 | `gaiop-admin-20260825T023432Z.tgz`，414 个白名单条目 |
| 发布包 SHA-256 | `7edc050aa9d00b034b7d7dcf9f72143e79a41703569a5e8ab73997d0bfd4e9a0` |
| 本地与生产入口资源 | `assets/index-CF-ui8DP.js` |
| 本地 `dist/index.html` SHA-256 | `d0eb532c24ec25a89f1f8b899f2139a20f5c2e9045d805035e59d8d99a61edae` |
| 工作台 / Office / 卡片分包 | `ChatWorkspace-LAqC1ILR.js` / `OfficePage-DFt5Jg3x.js` / `ReportAttachmentList-3_vKjt7-.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260825T023432Z` |

stage 在服务切换前创建 Admin 配置与代码快照，并使用生产 Node 与 `better-sqlite3` 执行 `wizard.db` 完整 `integrity_check` 和在线备份；237 原生 `systemd-analyze verify` 通过。start-verify 返回 `admin-started-loopback-verified`，没有修改 Caddy、网络或公网路由。

发布后目标会话为 active，归属用户 active 且具备对话能力；该会话有 1 条 ready 报告记录，报告属主与会话属主一致。生产运行时代码生成的卡片名为 `238web_业务综述报告.docx`，本人读取判定为允许，其他 basic 用户读取判定为拒绝；文件存在、为普通非符号链接文件且登记大小一致。basic 边界实测为本人报告列表和下载允许，预览、删除、恢复区及报告管理接口拒绝。

发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`；Admin 内部、根页面、Gateway、Upgrade、HTTPS 回环和公网健康检查均正常，Admin 仍只监听 `127.0.0.1:3000`，两库 `integrity_check=ok`。用户 6、工作区会话 93、正式报告 256、交付 9 均保持不变；审计记录由 1917 增至 1918，未生成、修改或删除报告和会话。报告留存、两套低风险清理、两套 SQLite 备份和磁盘水位 timer 继续 `active/enabled`，会话留存继续 `inactive/disabled`。

本次没有修改 OpenClaw、报告 Skill、GAIOP 后端、频道插件、Gateway、Upgrade、Caddy 或系统日志配置。浏览器已打开的旧页面需要强制刷新或重新登录，才能加载本次入口和分包。

## 9. 2026-08-25 卡片闪现后消失竞态修复

生产使用反馈显示卡片能够短暂出现，随后在登记重查或 `chat.history` 回拉后消失。此次将问题收敛为两个前端状态竞态，而不是报告生成、登记、权限或下载失败：

1. 同一会话的多次登记重查原先把每个成功响应都当作完整替换；一个暂时为空的后续响应可以覆盖已经取得的 `reportId`。
2. 卡片原先只能挂在某个助手消息对象上；Gateway 用较旧的持久历史替换实时助手投影时，如果当下没有可挂载助手消息，报告虽仍在 BFF 中，页面却没有独立展示位置。

提交 `3851bab fix(webchat): keep report cards across history refresh` 完成以下修复：

- 当前精确会话的短时登记重查按 `reportId` 合并，不再用后续空结果回退已经取得的报告；切换会话时仍清空旧会话状态并重新执行权威查询。
- 已映射报告继续显示在对应助手回复下；暂时无法映射的报告立即作为独立会话附件显示。持久助手回复恢复后，卡片重新归位且不重复。
- 工作台、Office 普通视图和 Office 展开视图均支持“仅有报告附件、暂时没有可见助手消息”的状态，不再错误显示空会话。

专项用例完整复现“卡片出现 -> 后续空重查 -> 旧历史覆盖 -> 持久回复恢复”的顺序，并验证同一个 `reportId` 在全过程持续可见。测试与发布结果如下：

| 项目 | 结果 |
| --- | --- |
| 实现提交 | `3851bab1bfa6e3cde742657bfc6f20e96b05e9bb` |
| Vitest 全量 | 23 个文件、120 项全部通过 |
| Admin Node 回归 | 195 项中 193 项通过；仍为修改前相同的 2 个留存发布器契约基线失败 |
| 类型与生产构建 | `vue-tsc -b` 通过；4651 个模块构建成功 |
| 差异检查 | `git diff --check` 通过 |
| 发布批次 | `20260825T031955Z` |
| 发布包 | `gaiop-admin-20260825T031955Z.tgz`，414 个白名单条目 |
| 发布包 SHA-256 | `4846b51bc281f3d52bc786785b7bc0888548c696d326fcb9e72f4eb518eb34b7` |
| 本地与生产入口 | `assets/index-BV_IOtvf.js` |
| 本地 `dist/index.html` SHA-256 | `785cd4445c11b5e6bc01098777f4d785abc4a7d4c775b59b9546742f14b749b1` |
| 工作台 / Office / 卡片分包 | `ChatWorkspace-BoqJfVrP.js` / `OfficePage-CkdMC-bt.js` / `ReportAttachmentList-q14hqeJz.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260825T031955Z` |

stage 已执行生产 Node/`better-sqlite3` 完整性检查、SQLite 在线备份和 237 原生 `systemd-analyze verify`；start-verify 返回 `admin-started-loopback-verified`。发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`，Admin 内部、根页面、Gateway、Upgrade、HTTPS 回环和公网健康检查正常，Admin 仍只监听 `127.0.0.1:3000`。

发布前后业务计数保持为用户 6、工作区会话 94、正式报告 257、报告交付 9、审计 1925，两库 `integrity_check=ok`；没有生成、修改或删除报告与会话。各留存、低风险清理、SQLite 备份和磁盘水位 timer 保持原状态，会话留存继续关闭。发布快照创建后文件系统使用率约 `75.4%`，水位探针按既有策略标记 warning；本次未执行磁盘自动治理或删除操作。

此次仍只修改 Admin/BFF 前端状态管理，没有修改报告 Skill、OpenClaw、GAIOP 后端、频道插件、Gateway、Upgrade、Caddy 或系统日志配置。旧浏览器标签页需要 `Ctrl+F5` 或重新登录后加载新入口。

## 10. 2026-08-25 新对话误带历史报告修复

“开始新对话”页面出现历史报告卡片的原因已确定为前端会话边界错误：工作台把空白、尚未建立的会话键回退成会话列表第一条，上一轮为防止卡片闪退而新增的独立附件展示因此取得了第一条历史会话的报告。该问题只改变页面投影，没有改写报告归属、会话、权限或数据库记录。

提交 `3552d46 fix(webchat): isolate report cards from new drafts` 完成以下修复：

- 工作台空白草稿保持真正的空会话键，不再回退到任何已持久化会话；非工作台原有默认选择逻辑不变。
- 切换会话或进入空白新对话时，在 Vue 下一次渲染前同步清空旧会话报告状态，避免历史卡片闪现一帧。
- 保留同一已选会话内的报告登记重查、历史回拉容错、卡片下载和本人权限规则；没有复制报告管理器接口，也没有修改 BFF 报告查询或下载 API。

测试与受控发布结果如下：

| 项目 | 结果 |
| --- | --- |
| 实现提交 | `3552d46a0795ac511ce3e67444aa1ffe3cff30a9` |
| 新增回归 | 旧会话报告在进入空白新对话时同步清空，并且不使用空会话查询报告 |
| Vitest 全量 | 23 个文件、121 项全部通过 |
| Admin Node 回归 | 195 项中 193 项通过；仍为相同的 2 个留存发布器契约基线失败，失败集合未变化 |
| 类型与生产构建 | `vue-tsc -b` 通过；4651 个模块构建成功 |
| 差异检查 | `git diff --check` 通过 |
| 发布批次 | `20260825T034136Z` |
| 发布包 | `gaiop-admin-20260825T034136Z.tgz`，414 个白名单条目 |
| 发布包 SHA-256 | `96b016605d1ce869cd89d73facf2d833011be9df46cbdcebd62290117591b96a` |
| 本地与生产入口 | `assets/index-YgcpqSFi.js` |
| 本地 `dist/index.html` SHA-256 | `63895a998af370d5ee2d1552c9a1344806c479346b60f322626e826ab9ec667f` |
| 工作台 / Office / 卡片分包 | `ChatWorkspace-CLgWQ47X.js` / `OfficePage-Bl6kQHRe.js` / `ReportAttachmentList-BOq7HP8C.js` |
| 生产 Admin 代码树哈希 | `329e0bb6e802e2b4a218784beecc2c5736409f3e77702fc44a95172716a08a82` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260825T034136Z` |

stage 在服务切换前完成生产快照、`wizard.db` 完整 `integrity_check`、SQLite 在线备份、发布包校验、Node 语法检查和 237 原生 `systemd-analyze verify`；start-verify 返回 `admin-started-loopback-verified`。发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`，Admin 内部、根页面、Gateway、Upgrade、HTTPS 回环和公网探针均正常，Admin 仍只监听 `127.0.0.1:3000`，两库 `integrity_check=ok`。

发布后业务计数为用户 6、工作区会话 95、正式报告 258、报告交付 9、审计 1933；本次没有生成、修改或删除报告与会话。报告留存、两套低风险清理、两套 SQLite 备份和磁盘水位 timer 继续 `active/enabled`，会话留存继续 `inactive/disabled`。文件系统使用率约 `76.52%`，水位探针保持 warning，本次未执行自动治理或删除。

应用内浏览器的生产页面验收被当前 HTTPS 证书信任错误阻断，未绕过证书安全警告；没有把该项冒充为肉眼验收。确定性前端回归、生产入口与分包、受控 start-verify、内外健康检查和数据库核查构成本次发布验收依据。此次没有修改报告 Skill、OpenClaw、GAIOP 后端、频道插件、Gateway、Upgrade、Caddy、报告权限或系统日志配置；旧标签页需 `Ctrl+F5` 后加载新版本。

## 11. 2026-08-25 报告卡片跨入后续普通回合修复

会话 `agent:main:main:dm:webchat-37f6eec5e5694da9a86a0a7219a291d4` 出现旧的“业务综述报告.docx”卡片长期停留在会话最下方；用户随后发送“你是？”并收到普通助手回复后，卡片仍被渲染在新回复之后。

根因是前端两条规则不一致：报告登记刷新已经能识别“报告已生成”等不含字面 `.docx` 的完成回复，但卡片定位候选仍只识别带 `.docx` 的助手文本；定位失败的会话报告又被 `unplacedReports` 永久作为会话尾部附件展示。该现象没有改变报告归属、文件、下载权限或数据库记录。

提交 `de79733 fix(webchat): keep report cards with their source turn` 完成以下修复：

- 卡片定位同时识别可见的“报告已生成”完成回复和 `.docx` 引用，按报告登记时间将历史缺少来源消息 ID 的报告放回原报告完成回合；
- 导出工具调用仍只作为登记刷新信号，不作为卡片展示位置，避免把卡片挂到被隐藏的过程消息；
- 独立尾部卡片只用于报告完成历史尚未回拉的短暂窗口；一旦出现时间晚于报告的下一条普通用户消息，未定位卡片立即退出会话尾部；
- 仍不跨会话、不跨用户、不按任意文件名猜测下载权限，下载继续复用现有鉴权报告接口。

验证与发布结果：

| 项目 | 结果 |
|---|---|
| 卡片专项 | 20 项全部通过，覆盖截图所示“报告回合—后续普通回合—尾部卡片”顺序 |
| Vitest 全量 | 24 个文件、135 项全部通过 |
| 类型与构建 | `vue-tsc -b` 通过，4652 个模块生产构建成功 |
| `git diff --check` | 通过 |
| 发布批次 | `20260825T094021Z` |
| 发布包 SHA-256 | `f24f0e2ab28587461f880b8d40b9c2061d81f14125e5e15a958ddea8374be229` |
| 入口 SHA-256 | `f9339ba5cbc4d562db89bcd6f1340b5dc34bae95a6df998f2e5ce4efd0db021a` |
| WebChat / 卡片分包 | `ChatWorkspace-ZVtX0fAM.js` / `ReportAttachmentList-DqdPbgC0.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260825T094021Z` |

受控 stage 已创建 Admin 快照和 SQLite 在线备份，start-verify 返回 `admin-started-loopback-verified`。发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`，内外健康检查正常，Admin 仍只监听 `127.0.0.1:3000`，Admin 与 Upgrade 数据库 `integrity_check=ok`；报告 262、交付 9。正式报告留存、低风险清理、水位监测和两套 SQLite 备份 timer 继续运行，会话留存继续关闭。

本次实时预检同时显示业务文件系统约 `83.23%`，现有水位探针已按既定规则标记 `cleanup_required`。本次卡片修复未执行、也未新增任何 80%/90% 自动磁盘治理或删除操作。旧标签页必须 `Ctrl+F5` 后才会加载新分包。

## 12. 2026-08-25 报告卡片按普通会话记录最终收口

用户再次明确了最终交互模型：助手先发送“报告已生成、格式为 docx、完整报告将以附件形式发送”的文本回复，随后紧接一条独立附件卡片；后续普通对话继续排在卡片之后。卡片应与普通会话记录一样保持在原报告回合，而不是嵌在文本气泡内，也不能作为无法定位的附件长期追随在会话末尾。

提交 `72c376b fix(webchat): render reports as transcript messages` 完成最终收口：

- WebChat、Office 普通视图和 Office 展开视图均把报告完成文本与附件卡片渲染为同一回合中的两个连续独立气泡；
- 卡片继续直接使用报告管理器现有的会话归属、用户归属和下载鉴权接口，没有复制报告文件或另建下载通道；
- 取消 `unplacedReports` 会话末尾兜底。没有可验证完成回复时不展示卡片，待同一会话的持久完成回复回拉后再稳定挂载；
- 删除按任意助手消息时间猜测报告位置的未发布方案，保留来源消息 ID、来源预览、明确报告完成文本和当前会话报告登记关系；
- 后续用户与助手消息不会改变既有报告卡片的挂载位置。

验证与受控发布结果如下：

| 项目 | 结果 |
|---|---|
| 实现提交与远端 | `72c376b56472b3f0dcb69692dca8be97f0631273`，已推送 `origin/dev-yangshuo` |
| Vitest 全量 | 25 个文件、137 项全部通过 |
| 类型与生产构建 | `vue-tsc -b` 通过，4653 个模块构建成功 |
| 差异检查 | `git diff --check` 通过 |
| 发布批次 | `20260825T101355Z` |
| 发布包 SHA-256 | `389421aac016b3d71acbe6c5a67728f733894ebff00c7a858b632cacdb2d5fa6` |
| 本地入口与分包 | `assets/index-D186bg1k.js`；`ChatWorkspace-BfiwyplJ.js`；`OfficePage-BKlILAKx.js`；`ReportAttachmentList-WXhSLmI0.js` |
| 本地 `dist/index.html` SHA-256 | `7ca345def2bc3bd45f632388c77fe527843d96ee818c3d71f2c22b22e2084795` |
| 生产 Admin 代码树哈希 | `e8b24fd71cec4eee2f5231401940415587d957d30d5eb1f4ab8daa674d4bb926` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260825T101355Z` |

stage 创建了 Admin 配置与代码快照，并使用生产 Node/`better-sqlite3` 完成 `wizard.db` 完整 `integrity_check` 和 SQLite 在线备份；start-verify 返回 `admin-started-loopback-verified`，监听范围为 `loopback-ipv4`，没有修改公网路由、Caddy 或网络配置。

发布前后用户 6、工作区会话 106、正式报告 264、报告交付 9、审计 1974，计数完全一致；没有生成、修改、移动或删除报告、会话及业务数据。发布后 Admin、Upgrade、Gateway、Caddy 均为 `active`；Admin 内部、根页面、Gateway、Upgrade、HTTPS 回环和公网检查正常，Admin 仍只监听 `127.0.0.1:3000`，两库 `integrity_check=ok`。报告留存、两套低风险清理、两套 SQLite 备份和水位监测 timer 继续 `active/enabled`，会话留存继续 `inactive/disabled`。

本次发布前后业务文件系统由约 `85.48%` 升至 `86.60%`，主要新增内容是本次受控发布回滚快照；水位状态保持 `cleanup_required`。本次没有执行自动磁盘治理或删除操作，也没有修改报告 Skill、OpenClaw、GAIOP 后端、频道插件、Gateway、Upgrade、Caddy 或系统日志配置。旧浏览器标签页需要 `Ctrl+F5` 或重新进入工作台后加载新分包。
