# 237 四角色权限交互与本人 Usage 聚合补充发布记录

> 发布日期：2026-07-31（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260731T060523Z`
> 发布提交：`dev-yangshuo@cebd534`

## 1. 补充范围

- 基础和审计用户继续使用原对话工作台布局、欢迎态和输入区，不再显示独立只读面板或顶部只读标记。
- 原输入框按角色显示无会话权限说明；“开始新对话”和“发送”保持原位置并锁定。服务端继续拒绝基础/审计用户直接发送、续聊和重置。
- 审计用户按“全局业务审计只读”口径查看全部会话、报告、任务、频道、Skills、系统监控和审计信息；不扩展到用户、系统配置、系统升级、模型、记忆、多智能体或工坊管理页。
- 无权限路由记录实际来源页；从会话管理进入无权限模块后，“返回上一页”显式返回会话管理。无可信来源时回到仪表盘，拒绝协议相对外部地址。
- 标准和审计用户的频道卡片标题保持“安全运行状态”，下方保留组件和配置状态标签，移除重复的安全投影说明；服务端安全字段投影不变。
- 基础和标准用户仪表盘不再接收全局 Usage 后清空聚合。BFF 先按本人有效 WebChat 会话键筛选，再重新计算消息、Token/费用、日期趋势、模型、供应商、工具、智能体和渠道汇总；仪表盘只返回展示需要的聚合字段。
- 本人 Usage 短缓存同时按登录主体、本人会话键集合和时间范围隔离；新增或删除本人会话不会复用旧归属集合的缓存。
- 仪表盘消息数、工具调用和错误率 KPI 直接使用本人聚合消息计数；Token 趋势继续使用本人会话日明细。使用量增加后，刷新会按真实 Gateway 明细增长，不使用固定值或简单清零。

本次仅修改 GAIOP-Admin 前端、Admin BFF、自动化测试和文档；未修改 GAIOP 后端、OpenClaw 核心、频道插件、生产用户或生产业务数据。

## 2. 自动化与构建

- Node：67 项通过。
- Vitest：14 个文件、49 项通过。
- `vue-tsc -b && vite build`：通过。
- `git diff --check`：通过。
- 新增本人 Usage 测试覆盖：非本人会话、模型和工具完全排除；本人多会话的 Token、费用、消息、错误、工具、模型、供应商和日期趋势重新求和；仪表盘投影不返回标题和单会话日明细。
- 既有权限测试继续覆盖四角色页面矩阵、基础/审计会话写入拒绝、审计全量读取和频道安全投影。
- 测试使用内存或临时数据，不读取或修改237真实用户。

## 3. 发布包与237验证

- 条目数：295。
- 大小：3567276 B。
- SHA-256：`8fc3499af2003e84bca67d8192d945a4aa1f892e22d1272b17f7664172735149`。
- 入口资产：`assets/index-C-TzAvkQ.js`。
- 发布包只包含 `dist/`、`server/`、`package.json` 和 `package-lock.json`；不含 `.env`、`data/`、`.bak`、本地数据库、图片删除记录或临时目录。
- 受控暂存脚本建立发布前 Admin 备份和数据库备份；未修改 Caddy、公网、网络或防火墙。
- 启动验证：完成，未触发回滚。
- 服务状态：active；进程退出码0；重启次数0。
- 健康检查：HTTP 200；Gateway：connected。
- 监听：`127.0.0.1:3000`，仅回环 IPv4。
- 发布后只读预检：暂存目录0；运行目录、固定回环绑定与持久化存储补丁均存在；Caddy继续占用443且状态未变化。
- 生产只读性能探针：会话列表79条，增强后78条，默认运行会话未暴露，外部来源错配0；`sessions.usage` 成功；抽样历史读取成功。

## 4. 数据与安全边界

- 基础/标准用户本人聚合只认 Admin BFF 登记且归属当前稳定用户 ID 的有效 WebChat 会话。
- 审计和管理员保留各自已授权的全局查看口径；审计仍无写权限。
- 外部频道身份未映射为 GAIOP 登录用户，不进入基础/标准用户的本人 Usage。
- 未读取、输出、打包或提交密码、Token、私钥、频道密钥、NAPM 凭据、`.env` 或本机安全存储内容。
- 本地 `data/wizard.db`、所有 `.bak`、`docs/images` 删除记录、`.codex-temp` 和既有用户改动均未暂存或提交。

## 5. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T060523Z'
```

回滚仅恢复本批次发布前的 Admin 代码、静态资源、配置、服务单元和受控数据库备份，不操作 GAIOP 后端、OpenClaw 核心、频道插件或其他服务器。
