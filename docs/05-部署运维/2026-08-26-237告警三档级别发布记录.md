# 237 告警三档级别发布记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-08-26 |
| Admin 发布批次 | `20260826T035607Z` |
| Admin 代码 | `0d00f47aee99ae6c9ed03ba7de6557ede415ea62` |
| 分支 | `origin/dev-yangshuo` |
| 结果 | 告警通知界面与导出仅保留轻微、重大、紧急三个 NAPM 业务级别 |

## 1. 修改范围

- 告警通知的严重级别筛选移除“一般”。
- 页面标签仅映射轻微、重大、紧急。
- Excel 导出不再将 `warning` 或“一般”映射为额外业务级别。
- Syslog 接收器、NAPM 告警规则、OpenClaw 核心和频道插件均未修改。

业务级别继续取 Syslog 正文中 `...Alerts severity=<级别>` 的值；Syslog 协议头的 `severity` 不参与 NAPM 业务级别判断。

## 2. 本地验证

- `node --test server/lib/alert-export.test.js`：3 项通过。
- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- `git diff --check`：通过。

## 3. 237 受控发布与验证

- 发布包仅包含 `dist`、`server`、`package.json`、`package-lock.json`，共 414 个条目。
- 包 SHA-256：`4c3614ba57c15cb7ed4c3703262a0b0b016c1e1c5961dec10eaac2a102aa685e`。
- 使用 `Invoke-237AdminStage.ps1` 创建切换前快照、校验发布包并受控切换；发布包未包含 `.env`、SQLite、告警历史、报告、备份或凭据。
- 启动后只读核验结果：`gaiop-admin.service` 为 `active`，仅监听 `127.0.0.1:3000`，Admin 健康探针通过，Gateway 状态为 `connected`。
- 未修改 Caddy、防火墙、DNS、证书、公网路由、GAIOP 接收器或 NAPM 配置。

## 4. 回滚

如需回滚 Admin 发布，使用同一批次执行：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260826T035607Z
```

回滚不覆盖生产 SQLite、告警历史、报告或运行时凭据。
