# 237 系统配置更名“高级配置”发布记录

> 发布日期：2026-08-07（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260807T014803Z`
> 运行代码提交：`d087a45 feat(settings): rename system configuration to advanced configuration`

## 范围与边界

本批次将“系统配置”显示名称统一改为“高级配置”，覆盖：

- 导航栏菜单标签（`routes.systemConfiguration`，中英文）；
- 系统配置页面标题（`pages.gaiop.systemConfig.title`，中英文）；
- 系统配置各子页面“返回系统配置”按钮文案（告警接入、数据源表单、环境/敏感配置、占位页，中英文）；
- 权限模块显示名（`access-control.ts` 中 `systemConfiguration.moduleName`）。

仅修改用户可见文案，未改变路由名、组件名、权限规则、REST/RPC 契约、数据库结构或任何业务逻辑。

## 本地验证

| 验证 | 结果 |
|---|---|
| `vue-tsc -b && vite build` | 通过（318 条发布条目） |
| `git diff --check` | 通过 |
| 源码残留检查 | `src/` 下无“系统配置”用户可见文案残留 |

## 237 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-advanced-config-20260807T014803Z.tgz` |
| 条目数 | 318 |
| 大小 | 3,638,121 B |
| 归档内容 | `dist/`、`server/`、`package.json`、`package-lock.json`（不含 `.env`、数据库、备份或秘密） |

受控暂存完成归档哈希校验、既有 Admin 程序快照与受控数据库备份、依赖复用/安装检查和 Node 语法检查。启动验证结果：

- `gaiop-admin.service` 为 `active`；
- 仅监听 `127.0.0.1:3000`（回环 IPv4）；
- 本地健康检查通过，Gateway 为 `connected`；
- 237 实际部署的 `dist/index.html` SHA-256 与本地构建一致：`d4c1c2b43612891e8cbd44c1142f8b95bc6a4a65ce78f132a6119c928d769bf5`；
- 部署后的 `dist/assets` 中各页面分包已包含“高级配置”文案；
- 未改 Caddy、公网路由、DNS、防火墙、证书、升级服务或 OpenClaw/频道插件；
- 未触发回滚。

如需恢复本批次前的 Admin 程序和受控数据库备份：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260807T014803Z'
```

该回滚点仅覆盖本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份。
