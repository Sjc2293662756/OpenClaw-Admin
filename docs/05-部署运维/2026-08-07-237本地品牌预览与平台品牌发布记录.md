# 2026-08-07 237 本地品牌预览与平台品牌发布记录

## 1. 发布范围

- 发布批次：`20260807T053417Z`
- 仓库：`GAIOP-Admin/dev-yangshuo`
- 内容：平台品牌八项默认名称源、初始管理员专用隐藏品牌配置页、用户可见 OpenClaw 文案向 GAIOP 的统一展示，以及欢迎页、登录页、对话工作台的品牌字标规则。
- 未变更：Caddy、HTTPS 443、HTTP 80 跳转、DNS、防火墙、公网监听、OpenClaw 核心、频道插件、升级服务和 238。

## 2. 本地预览边界

- 本地 Vite 预览使用 `http://127.0.0.1:5178`，Admin 数据和登录请求经用户本机 SSH 回环隧道访问 237 的 `127.0.0.1:3000`。
- 为这项本地预览，237 的 Admin 来源白名单临时包含 `http://127.0.0.1:5178`；其建立时间、备份位置、核验结果和关闭方法见 [本地品牌预览来源临时放行记录](2026-08-07-237本地品牌预览来源临时放行记录.md)。
- 该临时来源不是公网开放，不改变端口监听范围；本地预览结束或项目结束时必须按该记录关闭，恢复对该来源的拒绝。

## 3. 发布与回滚边界

- 发布包显式包含 `dist/`、`server/`、`package.json` 和 `package-lock.json`，不包含 `.env`、SQLite 运行库、报告实体、备份或秘密材料。
- 受控发布器在切换前备份 `/opt/gaiop/admin`、服务配置和生产 Admin SQLite；新增品牌设置表的迁移前数据库也在同一批次备份。
- 发布器仅重启 `gaiop-admin.service`，并验证回环 `127.0.0.1:3000`、`/api/health` 和 Gateway 状态。失败自动回滚；必要时可按同一批次执行受控回滚。

## 4. 发布后记录

本节在 237 发布完成后补充实际包哈希、服务状态、监听范围、健康与 Gateway 结果，以及本地预览回归结果。
