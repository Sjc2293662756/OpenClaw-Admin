# 237 Admin 主题整改发布记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-07-21 |
| 发布批次 | `20260721T033750Z` |
| 功能说明 | [界面主题策略与可读性验收](../04-公共设计/2026-07-21-界面主题策略与可读性验收.md) |

## 1. 发布范围

用户明确授权发布已验证的 GAIOP-Admin 当前版本。发布包仅包含生产 `dist`、`server`、`package.json` 和锁文件；不包含 `.env`、SQLite 数据、报告、用户图片、私钥或其他敏感配置。

## 2. 备份与发布保护

受控发布器在切换前创建本批次的版本化备份，并在独立暂存目录完成校验、解包、依赖准备、Node 语法检查和 systemd 单元校验。准备阶段成功后才短暂停止服务和切换目录。

## 3. 验证结果

启动验证结果为 `admin-started-loopback-verified`：

- 服务只监听 `127.0.0.1:3000`；
- 内部健康接口成功，Gateway 为 `connected`；
- 经既有本地 `127.0.0.1:3005` SSH 隧道只读请求 `/api/health`，得到 HTTP 200 且 Gateway 已连接；
- 未触发回滚，未修改 Caddy/443、防火墙、Gateway、Syslog 接收器、频道插件、数据源内容、环境变量值或公网路由。

## 4. 回滚

若启动或验证失败，使用：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260721T033750Z
```

回滚器会停止失败版本、保留失败副本、恢复本批次发布前目录并启动旧服务；随后必须运行受控启动验证，确认回环监听、健康接口和 Gateway 连接。执行服务器回滚前仍需另行授权。
