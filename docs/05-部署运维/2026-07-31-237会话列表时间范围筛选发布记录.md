# 237 会话列表时间范围筛选发布记录

> 发布日期：2026-07-31（UTC+8）
> 代码提交：`dev-yangshuo@53bfc90`
> 发布批次：`20260731T024400Z`

## 1. 变更

- 在会话列表“当前数量 / 总数量”右侧增加统一时间范围组件。
- 组件复用告警和仪表盘现有的最近1小时、今日、昨日、最近7日、最近30日、本月和自定义范围交互。
- 会话列表默认显示最近7日有活动的会话。
- 时间范围以会话“最近活动时间”为准，起止边界均包含。
- 时间条件与搜索、渠道和模型条件叠加；当前数量、全选范围和分页数据同步更新。
- 选择新范围或修改其他筛选条件后，分页自动回到第一页。
- “清空筛选”恢复搜索、渠道、模型、排序和默认最近7日范围。
- 时间基准优先使用 Admin BFF 健康响应的服务器时间；同步完成前用户已手动选择的范围不会被覆盖。

本次只修改 Admin 前端过滤和展示，没有修改 OpenClaw、GAIOP 后端或会话数据。

## 2. 测试

- Node：55项通过。
- Vitest：13个文件、46项通过。
- 时间边界测试覆盖起点、终点、范围外时间和无效时间戳。
- TypeScript和Vite生产构建通过。

## 3. 发布与验证

发布包：

- 文件数：287
- 大小：3557380 B
- SHA-256：`00d37686774c1b8df38587600dce94bb4440da974ac61f33a77d17cae84ec7d0`
- 未包含 `.env`、`data/`、`.bak`、本地数据库、图片删除记录或临时目录。

237验证：

- 服务状态：active。
- 健康检查：HTTP 200。
- Gateway：connected。
- 监听：`127.0.0.1:3000`。
- 服务重启次数：0。
- 入口资产：`assets/index-CUqslu19.js`。
- 会话分包：`assets/SessionsPage-BZOgOfy8.js`。
- 生产会话分包已确认包含最近7日默认值、服务器时间同步和右侧弹层定位。

## 4. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T024400Z'
```
