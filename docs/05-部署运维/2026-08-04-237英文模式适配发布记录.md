# 237 GAIOP 英文模式适配发布记录

> 发布日期：2026-08-04（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260804T040229Z`
> 运行代码提交：`2cbc1be feat(i18n): complete formal English mode`

## 范围与边界

本批次完成正式 GAIOP-Admin 英文模式适配，覆盖入口/登录、无权限页、账户、系统配置、告警、报告管理、审计、系统设置与升级页，以及对话工作台、会话、任务、频道和 Skills 的正式可见界面。

- 仅翻译页面固定文案和稳定枚举；不翻译告警名称/描述、报告文件与正文、用户名、会话、数据源、频道用户、历史审计动作和说明等真实业务数据。
- 告警与审计 Excel 导出按 `zh-CN` / `en-US` 白名单生成本地化文件名、工作表、表头和稳定枚举；导出仍由服务端安全投影、筛选和 TOP 上限控制。
- 未改变四角色权限、会话和报告归属、审计写入、直接 REST/RPC 权限、Gateway 默认语言，未启用隐藏遗留模块。
- 系统升级页仅调整 Admin BFF 页面显示；未改 18900 升级服务、内部令牌、升级包路径、升级服务或 OpenClaw/频道插件。

## 本地验证

| 验证 | 结果 |
|---|---|
| Node 服务端测试 | 81/81 通过 |
| Vitest | 16 个文件、63/63 通过 |
| `vue-tsc -b && vite build` | 通过 |
| `git diff --check` | 通过 |
| 导出英文工作簿 | 覆盖稳定字段本地化，原始告警和历史审计文本保留 |

所有测试使用隔离数据和模拟 API，未读取或修改 237 真实业务数据。

## 237 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-english-mode-20260804T040229Z.tgz` |
| 条目数 | 305 |
| 大小 | 3,594,810 B |
| SHA-256 | `17feda0a427596c11f150840e02d994b246a4b6dc41df1b6671935f215b273ee` |

受控暂存流程完成归档哈希校验、既有 Admin 程序快照和受控数据库备份、依赖检查与 Node 语法检查。启动验证结果：

- `gaiop-admin.service` 为 `active`；
- 仅监听 `127.0.0.1:3000`；
- 本地健康检查通过，Gateway 为 `connected`；
- 未改 Caddy、公网路由、DNS、防火墙、证书或 OpenClaw/频道插件；
- 未触发回滚。

如需恢复到本批次前的 Admin 程序和受控数据库备份，使用：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T040229Z'
```

该回滚点只覆盖本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份。

## 工作台补丁发布

> 发布批次：`20260804T052416Z`
> 运行代码提交：`04b24e5 fix(i18n): localize workspace shell and branding`

根据工作台实际页面核验，本补丁将独立 `ChatWorkspace` 壳层接入现有语言资源：左下角用户菜单、管理控制台入口、会话操作、连接反馈和顶部操作在英文模式下均显示英文。

- 中文平台全称统一为“观枢GAIOP智能运维分析平台”。
- 英文全称统一为“Guanshu AI-driven Operations Platform”，并同步首页全称和浏览器标题。
- 本地 `vue-tsc -b && vite build` 与 Vitest 63/63 通过。
- 受控归档 `gaiop-admin-workspace-i18n-20260804T052416Z.tgz`，305 条目、3,595,876 B，SHA-256 为 `18af300c291b8063c3d29a9506065c3d35c2fadeac5342362e2811fc4ffcbbe6`。
- 237 验证结果：服务 `active`，仅 `127.0.0.1:3000` 监听，Gateway `connected`；未改变公网路由，未触发回滚。

如需恢复该补丁前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T052416Z'
```

## 首页英文品牌名称补丁发布

> 发布批次：`20260804T054055Z`
> 运行代码提交：`17f8c53 fix(branding): align English company and product names`

依据产品与公司命名规范，本补丁仅修正首页英文品牌固定文案：组合品牌为 `NetInside GAIOP`，公司英文全名为 `Beijing Wangshen Technology Co., Ltd.`，公司英文简称为 `NetInside`；平台英文全称继续使用 `Guanshu AI-driven Operations Platform`。未改权限、数据归属、真实业务数据或隐藏遗留模块。

- 本地 `vue-tsc -b`、Vitest 16 个文件 63/63、Node 服务端测试 81/81 与 `vite build` 均通过。
- 受控归档 `gaiop-admin-branding-20260804T054055Z.tgz`，305 条目、3,594,179 B，SHA-256 为 `3319df31789d82ed08b465d7be337f51dca933978bc2686b2e2e8492fd807f7b`。
- 237 暂存已创建应用和受控数据库备份；服务启动后为 `active`，仅监听 `127.0.0.1:3000`，Gateway 探测为 `connected`。
- 未改 Caddy、公网路由、DNS、防火墙、证书、OpenClaw 或频道插件，未触发回滚。

如需恢复本补丁前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T054055Z'
```

## 控制台品牌与仪表盘操作补丁发布

> 发布批次：`20260804T055359Z`
> 运行代码提交：`dd02b20 fix(i18n): localize console branding and dashboard action`

本补丁将设置页和管理控制台左上角品牌接入统一语言资源：中文保持“观枢 GAIOP”，英文显示 `NetInside GAIOP`；仪表盘快捷入口“对话工作台”在英文模式显示 `Chat Workspace`。未改变权限、数据归属、真实业务数据或隐藏遗留模块。

- 本地 `vue-tsc -b`、Vitest 16 个文件 63/63、Node 服务端测试 81/81 与 `vite build` 均通过。
- 受控归档 `gaiop-admin-console-i18n-20260804T055359Z.tgz`，305 条目、3,594,893 B，SHA-256 为 `dc607ca78bf3c4db5ea0bfcf29bc409f120430210214ae067b80ffaeb43e3229`。
- 237 暂存已创建应用和受控数据库备份；服务启动后为 `active`，仅监听 `127.0.0.1:3000`，Gateway 探测为 `connected`。
- 经 `http://127.0.0.1:3005/welcome?redirect=/` 资源核验，HTTP 200 的当前前端资源包含 `NetInside GAIOP`、`Chat Workspace` 和仪表盘操作语言键。
- 未改 Caddy、公网路由、DNS、防火墙、证书、OpenClaw 或频道插件，未触发回滚。

如需恢复本补丁前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T055359Z'
```

## 欢迎页与工作台产品简称补丁发布

> 发布批次：`20260804T060414Z`
> 运行代码提交：`6a94bd2 fix(i18n): shorten workspace and welcome product label`

为避免与左侧公司简称 `NetInside` 重复，英文欢迎页和工作台左上角的产品标识统一显示 `GAIOP`。工作台使用独立语言键，中文仍显示“观枢 GAIOP”。未改变平台英文全称、公司英文名称、权限、数据归属或真实业务数据。

- 本地 `vue-tsc -b`、Vitest 16 个文件 63/63、Node 服务端测试 81/81 与 `vite build` 均通过。
- 受控归档 `gaiop-admin-product-label-20260804T060414Z.tgz`，305 条目、3,594,827 B，SHA-256 为 `02f4fa0e4eee8e05cbc2342dfedd98290fbaba98c73a2affd9d086e9b6b1704e`。
- 237 暂存已创建应用和受控数据库备份；服务启动后为 `active`，仅监听 `127.0.0.1:3000`，Gateway 探测为 `connected`。
- 经 `http://127.0.0.1:3005/welcome?redirect=/workspace` 核验，HTTP 200 的当前资源包含英文 `product:"GAIOP"`，工作台资源使用独立产品标识语言键。
- 未改 Caddy、公网路由、DNS、防火墙、证书、OpenClaw 或频道插件，未触发回滚。

如需恢复本补丁前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T060414Z'
```
