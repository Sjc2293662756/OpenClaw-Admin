# 2026-09-04 237 Admin 告警提示音发布记录

## 发布内容

- 发布提交：`7101ff2`（可按级别选择并试听企业风格告警提示音）。
- 237 发布批次：`20260904T084122Z`。
- 发布包只包含 Admin 源码、生产构建产物和依赖清单；未包含本机 `.env`、数据库或 `node_modules`。

## 发布前验证

- `npm test`：通过（Node 282 项、前端 198 项）。
- `npm run build`：通过。
- 发布前 237 受控预检确认 Admin BFF 为 active、仅监听 `127.0.0.1:3000`、健康接口返回 200。

## 受控发布与结果

1. `Invoke-237AdminStage.ps1` 已完成，创建应用、服务配置和 SQLite 发布前快照。
2. `Invoke-237AdminStartVerify.ps1` 已完成，延迟稳定性检查通过。
3. 发布后只读预检确认服务 active、回环监听和健康接口 200，未遗留发布暂存目录。

现有告警持久化专用只读核验未返回完成状态，因此没有把 Receiver 上游链路写为已验证；页面中的告警设置、保存和真实到达告警播放由用户继续浏览器验收。

## 回滚

如本批次需要回滚，使用：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260904T084122Z
```

回滚会恢复本批次切换前的 Admin 程序、服务配置和发布前数据库备份，再重新启动原服务；不会清理业务告警、报告和会话数据。
