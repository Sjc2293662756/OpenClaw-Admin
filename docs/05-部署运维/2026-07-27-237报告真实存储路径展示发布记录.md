# 237 报告真实存储路径展示发布记录

| 属性 | 内容 |
|---|---|
| 发布日期 | 2026-07-27 |
| 发布批次 | `20260727T074240Z` |
| 目标 | 系统设置向管理员只读展示正式报告的真实存储路径 |
| 真实存储路径 | `/var/lib/gaiop/reports` |
| 备份目录 | `/var/backups/gaiop/report-root-display-20260727T074240Z` |
| 结果 | 发布成功 |

## 1. 路径边界

- 正式报告实体及配对审计 JSON 均归档在 `/var/lib/gaiop/reports`。
- 系统设置只显示该真实根目录，不提供编辑、目录选择或写入接口。
- 企业微信等外部渠道发送附件时，会按需把正式报告复制到 OpenClaw 媒体交付目录；该副本用于附件发送并按保留策略清理，不是第二个正式报告库。
- `/reports/...` 属于访问或交付表达，不作为系统设置中的存储路径展示。

## 2. 发布范围

- 前端 `dist`：报告存储区域由“已配置”改为“真实存储路径”。
- `server/routes/report-storage.js`：管理员只读接口增加 `reportStorageRoot`。
- 未修改报告生成、来源登记、报告列表、下载、企业微信附件交付或 OpenClaw 核心代码。

## 3. 验证

- Node 测试 43 项通过。
- Vitest 17 项通过。
- TypeScript 检查和 Vite 生产构建通过。
- 237 的 `GAIOP_REPORTS_DIR` 核验为 `/var/lib/gaiop/reports`。
- 237 实机路由探针返回 `reportStorageRoot=/var/lib/gaiop/reports`。
- Admin 服务重启后健康检查通过。
- 发布后前端目录哈希为 `7b275dc584b524f5a909d27174537b5c86a191b70e1bccc30a3c174ea7b95533`。
- 发布后接口文件哈希为 `efbe161bf0729551f71fef86a2b7dd0761e6484aef030e1aacc3c0142303ca89`。

## 4. 回滚

若需要回滚，停止 `gaiop-admin.service`，从备份目录恢复 `dist` 和
`server/routes/report-storage.js`，再启动服务并执行健康检查。受控发布脚本在发布未完成时会自动执行同等回滚。
