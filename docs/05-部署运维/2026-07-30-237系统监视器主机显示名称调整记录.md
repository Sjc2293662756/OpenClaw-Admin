# 237 系统监视器主机显示名称调整记录

| 项目 | 结果 |
|---|---|
| 日期 | 2026-07-30 |
| 发布批次 | `20260730T020029Z` |
| 修改范围 | Admin 系统监视器前端展示 |
| 237 备份 | `/var/backups/gaiop/system-hostname-label-20260730T020029Z` |
| OpenClaw、BFF、数据库 | 未修改 |

## 1. 调整原因

237 的 `/api/system/metrics` 返回真实操作系统主机名 `netinsideopenclaw`，系统监视器此前直接展示该值。产品界面需要将该主机显示名称统一为 `NetInside GAIOP`。

## 2. 实现边界

- 仅当主机名忽略大小写后精确等于 `netinsideopenclaw` 时，页面显示 `NetInside GAIOP`；
- 其他环境或其他主机名继续显示接口返回的原值；
- 不修改 Linux 主机名、DNS、证书、服务名、日志标识或监控采集数据；
- `/api/system/metrics` 仍返回真实主机名，运维识别能力保持不变。

## 3. 验证

- TypeScript 检查与 Vite 生产构建通过；
- 构建产物 `SystemPage` 分包包含 `NetInside GAIOP`；
- 237 新静态资源已替换，发布包 SHA-256 为 `174b67b4476e2da74d6a0f382e4f8cab712b31f3f1b6d178c277ecf81b276c86`；
- 237 首页静态入口、Admin 健康接口及 Gateway 连接验证通过；
- 发布未触发回滚。

## 4. 回滚

从 `/var/backups/gaiop/system-hostname-label-20260730T020029Z/dist` 恢复 `/opt/gaiop/admin/dist` 即可。回滚不涉及数据库、报告目录、Gateway、频道插件或 OpenClaw。
