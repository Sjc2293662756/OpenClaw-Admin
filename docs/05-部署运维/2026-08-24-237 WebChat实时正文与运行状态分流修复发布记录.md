# 237 WebChat 实时正文与运行状态分流修复发布记录

日期：2026-08-24

## 1. 问题与生产证据

用户在会话 `agent:main:main:dm:webchat-288e17f63044441c9bbb10e168f84f41` 生成 238web 最近 7 天综述报告时，页面实时阶段反复追加“报告已生成”等旧内容；刷新后重复内容消失，权威历史恢复正常。

237 只读核查确认：Gateway transcript 只有一个用户回合、一次 `napm-summary`、一次 `napm-report-export`、三个助手回合和两个工具结果，持久化内容不存在重复哈希。GAIOP 后端去重正常，重复只存在于 Admin 浏览器的实时投影。

## 2. 根因

OpenClaw 在同一次运行中会发出两类实时数据：

- `chat`：面向会话正文的累计快照，同一 run 的新快照应替换旧快照；
- `agent`、`tool`、`model`：面向运行状态、模型阶段和工具执行的遥测，不属于会话正文。

此前 Admin 将两类数据都递归提取为助手正文，并在内容不是前缀时直接拼接。报告包含多个工具阶段，低层 `agent` 文本会在阶段切换时重新开始，因而形成“旧内容 + 新内容 + 再次旧内容”的长串重复。刷新后页面重新读取未损坏的 Gateway transcript，所以又恢复正常。

这不是报告文件、报告留存、来源归因或后端去重故障，也不是正常的流式输出现象。

## 3. 第一阶段修复

本次只完成数据职责分流：

1. 会话正文只接收 `chat` / `chat.*` 事件；store 层和两个页面入口均设硬边界。
2. `agent`、`tool`、`model` 继续更新运行状态和工具进度，但不再进入 transcript 正文。
3. 同一 run 的 `chat.delta` 使用固定临时消息 ID；累计快照只替换同一个气泡，不再拼接到工具调用前的持久化助手消息。
4. 运行结束后停止保护临时投影；下一次 `chat.history` 以 Gateway 持久化 transcript 为唯一权威，防止临时内容跨切换或刷新残留。
5. 历史回拉期间仅保护本地待持久化的用户占位消息和当前正在进行的一个 `chat` 流，不再保护任意合成助手消息。

默认交互保持为：流式正文开启、运行状态显示、工具详情默认收起。后续第二阶段再在高级配置中增加显式开关；“运行过程”仅指可审计的状态和工具步骤，不展示模型隐藏推理文本。

## 4. 影响边界

- 已修复对话工作台和 Office 对话面板，两者此前共享同一错误的数据合并方式。
- MyWorld 只消费运行状态，不写入会话正文，不受本问题影响。
- 未修改 GAIOP 后端、NAPM Skills、OpenClaw 核心、Gateway 或频道插件。
- 未修改 BFF 路由、Admin SQLite、正式报告、报告来源归因、留存策略、清理任务、Caddy 或公网配置。
- 未生成新的 238 报告，未修改已有损坏会话或猜测补写历史记录。

## 5. 代码与验证

- 代码提交：`8048b43`（`fix(webchat): separate chat text from agent telemetry`）。
- `origin/dev-yangshuo` 已快进到该提交。
- 相关 Vitest：5 个文件、34 项通过。
- 新增覆盖：双流事件隔离、多工具报告累计快照、历史助手/工具边界、终态后权威历史替换、历史短暂滞后。
- `npm run build`：通过，4651 个模块完成生产构建。
- `git diff --check`：通过。
- 全仓直接执行 `npx vitest run` 会把使用 Node test runner 的服务端文件误作为 Vitest suite 收集，并因本机缺少 `better-sqlite3` 原生绑定产生既有环境失败；本次相关前端套件无失败，未为绕过环境问题修改数据库或服务端测试。

## 6. 237 受控发布

| 项目 | 结果 |
|---|---|
| 发布批次 | `20260824T093509Z` |
| 发布包 | `gaiop-admin-webchat-event-split-20260824T093509Z.tgz` |
| 发布包 SHA-256 | `1e1a34e1632e5d8f26cd76646e6d8a2d3a15c3e82bee4bf8a4850fc71c8281c4` |
| 发布白名单 | `dist`、`server`、`package.json`、`package-lock.json`，414 个条目 |
| 本地 `dist/index.html` SHA-256 | `9e6c58df8a83940bc973895b0b709c1bf09fce3b406be77a9532106d7e6346ff` |
| 工作台分包 | `ChatWorkspace-B6c1FHq6.js` |
| Office 分包 | `OfficePage-BF3vLjq2.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260824T093509Z` |

发布使用现有 `Invoke-237AdminStage.ps1`、`Invoke-237AdminStartVerify.ps1` 和性能/监听只读探针。stage 已创建 Admin 与 SQLite 安全备份，未修改 Caddy 或网络；start-verify 返回 `admin-started-loopback-verified`。

发布后结果：Admin `active`、健康 HTTP `200`、Gateway `connected`、进程退出码 `0`、重启次数 `0`；会话列表和 `chat.history` 探针成功，监听仍为 `127.0.0.1:3000`，环境文件和源码监听保护存在。

## 7. 用户验收

使用一个新 WebChat 会话发送一次多工具问题或综述报告请求，验收以下结果：

1. 运行状态和工具步骤可显示，但助手正文不会反复复制旧段落；
2. 同一时刻只有一个正在增长的流式助手气泡；
3. 完成后切换会话再返回，内容与 Gateway 历史一致；
4. 刷新页面后不出现临时重复内容；
5. 报告文件管理的来源渠道和用户归属保持既有受控链路，不因本次前端分流变化。

## 8. 17:43 现场记录与规范投影补充修正

用户在 17:43 使用新会话发送“给我回溯238web 最近3天的综述报告！”，现场复制内容仍显示大量逐字累计和阶段重置文本。对应会话为 `agent:main:main:dm:webchat-e8352f4cf89142de9344b06296a4f7bb`。237 只读核查确认该键同时存在于 Admin 和 Gateway、Gateway transcript 实体存在，因此本次现象不属于建会话、历史持久化、报告文件或来源登记故障。

现场记录暴露了第一阶段剩余的协议归一化漏项：

- `chat` 的 `runId` 和 `state` 可能位于 `payload/data` 等内层封装，第一版只从顶层读取；
- 同一个 `chat` 事件可能同时携带累计 `message` 和短 `delta`，通用递归正文提取会把两者都当作候选消息；
- 当运行字段未识别时，实时消息没有稳定投影 ID，仍可能进入历史的相邻正文拼接分支。

补充修正不再依赖顶层字段或内容前缀：

1. 在受限深度内递归解析 `runId/state/phase/status`；
2. 每个 `chat` 事件只选取一个规范助手投影，优先保留完整累计正文，不把短 `delta` 建成独立气泡；
3. 即使传输层暂时缺少 `runId/state`，也使用当前会话的稳定临时投影 ID；
4. 所有非前缀阶段快照都执行替换，彻底禁止在 `chat` 投影中使用字符串相加。

补充回归以现场结构覆盖多层封装、累计正文与短 `delta` 并存、英文准备阶段切换到中文最终阶段三种情况。相关测试提升为 5 个文件、36 项通过，生产构建和 `git diff --check` 通过。

## 9. 补充发布结果

| 项目 | 结果 |
|---|---|
| 修复提交 | `860668a`（`fix(webchat): normalize wrapped chat projections`） |
| 发布批次 | `20260824T095228Z` |
| 发布包 | `gaiop-admin-webchat-chat-projection-20260824T095228Z.tgz` |
| 发布包 SHA-256 | `9ddf8fd17cac50648f7b021737c5291612a754df3977ba5c4a5cd6585a9feadc` |
| 发布白名单 | `dist`、`server`、`package.json`、`package-lock.json`，414 个条目 |
| 本地 `dist/index.html` SHA-256 | `0952d3e6ac52acd4951ba8c6af17dc0391148bf7e6e0e8289f7085feedf5c9e6` |
| 工作台分包 | `ChatWorkspace-BYPtYtRC.js` |
| Office 分包 | `OfficePage-CnTG4Leh.js` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260824T095228Z` |

受控 stage 再次创建 Admin 和 SQLite 安全备份，未修改 Caddy、网络、报告或留存配置。start-verify 返回 `admin-started-loopback-verified`；发布后 Admin `active`、HTTP `200`、Gateway `connected`、重启次数 `0`，监听仍为 `127.0.0.1:3000`，会话列表与 `chat.history` 探针成功。

SPA 标签页不会在服务器发布后自动替换已加载的旧 JavaScript。补充发布验收前必须先按 `Ctrl+F5` 强制刷新，确认加载 `ChatWorkspace-BYPtYtRC.js`，再新建会话测试；否则旧标签页仍会继续执行发布前的重复拼接代码。
