# 237 审计底座与权限拒绝记录第一阶段发布记录

> 发布日期：2026-08-03（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260803T055808Z`
> 运行代码提交：`b8a4f7f feat(audit): add structured denial records`

## 1. 范围与边界

本批次仅修改 GAIOP-Admin Node BFF、`audit_logs` 幂等迁移、认证/RPC 权限拒绝记录、审计查询、隔离测试和文档。

- 不修改审计页面布局；第二阶段“审计信息页面改造”尚未实施。
- 不修改 GAIOP 后端、OpenClaw、Gateway、Skills、频道插件、升级模块、Caddy、证书、防火墙或公网配置。
- 发布包不包含 `.env`、SQLite、报告、备份、密钥、Token、客户配置或本地临时目录。

## 2. 实施结果

`audit_logs` 已按可重复迁移增加分类、结果、来源、REST 方法/规范化路径、RPC 方法、稳定错误码、请求编号和服务端套接字来源地址。历史记录保留，新增字段保持空值，不推断回填。

统一审计写入器继续兼容现有 `recordAudit` 四参数调用；请求上下文为新写入补充结构化字段。没有保存密码、Token、Cookie、密钥、完整请求体、查询参数原文、RPC 参数、聊天内容、报告正文或敏感配置。

- 已登录 REST 403、Gateway RPC 拒绝（包括管理员未登记方法）记录为 `denied`。
- 会话、报告和媒体的归属隐藏 404 在内部记录为 `denied`，对外响应保持原有 404。
- 登录失败、临时锁定和 `PASSWORD_CHANGE_REQUIRED` 保持认证审计。
- 匿名 401 与退役入口 410 使用有界内存限频；本批次的无凭据生产探针因此仅产生少量匿名拒绝记录。
- `GET /api/alerts` 成功刷新不再写入“查看 GAIOP 告警”；仪表盘、会话列表、Usage、频道状态、SSE 与心跳不写成功审计。
- `GET /api/audit-logs` 保持 `limit=200` 兼容，新增服务端时间、用户名、角色、分类、结果、来源、错误码、关键词、分页、总数和成功/失败/拒绝汇总；排序固定为 `created_at DESC, id DESC`。

## 3. 测试与构建

所有权限验证使用临时/内存 SQLite、隔离 Express 应用和隔离角色，不使用 237 真实用户。

| 验证 | 结果 |
|---|---|
| 审计迁移可重复、旧记录保留、新旧写入兼容和敏感文本脱敏 | 通过 |
| 四角色 REST/RPC 拒绝、管理员未登记 RPC、隐藏 404、401/410 限频 | 通过 |
| 审计查询筛选、分页、总数、汇总、稳定排序与 `limit` 兼容 | 通过 |
| 账号安全、会话、报告、媒体、频道、告警与权限回归 | 通过 |
| Node 测试 | 75/75 通过 |
| Vitest | 15 个文件、54/54 通过 |
| `vue-tsc -b && vite build` | 通过，4631 个模块 |
| `git diff --check` | 通过 |

## 4. 发布包与受控切换

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-audit-foundation-20260803T055808Z.tgz` |
| 条目数 | 301 |
| 大小 | 3,577,748 B |
| SHA-256 | `7db767c1726316cf8a979c1378a6cc12d9384979c21a04ac3feaa1b17715b7d5` |
| 内容 | `dist/`、`server/`、`package.json`、`package-lock.json` |

受控暂存器先创建 Admin 快照与生产 `/var/lib/gaiop/admin/wizard.db` 备份，再验证归档、依赖和 Node 入口，最后切换。暂存结果：`completed=true`、`backupCreated=true`、`databaseBackupCreated=true`、`caddyChanged=false`、`networkChanged=false`。

启动验证结果：`admin-started-loopback-verified`；未触发回滚，未修改公网路由。

## 5. 237 发布后验证

- `gaiop-admin.service` 为 active，主进程退出正常。
- 监听仍为 `127.0.0.1:3000`；健康接口 HTTP 200；Gateway TCP 从服务账户可达。
- 暂存目录为 0，发布目录、持久化路径和回环绑定保护均存在。
- 无凭据回环探针：health 为 200；events、media、RPC、升级入口均为 401；九组遗留前缀均为 410。
- Caddy 仍运行并占用既有 443；本批次未读取或改变 Caddy、证书、防火墙、DNS 或公网配置。

若后续发现本批次问题，使用同一批次回滚：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260803T055808Z'
```

回滚恢复本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份；不操作 GAIOP 后端、OpenClaw、频道插件或其他服务器。

## 6. 后续边界

第二阶段“审计信息页面改造”尚未实施，届时才评估页面筛选、时间范围、分页与汇总交互。动态审计规则、策略配置页、删除/留存、导出、WORM、防篡改签名、SIEM、代理信任和公网网络改造均不在本阶段范围内。
