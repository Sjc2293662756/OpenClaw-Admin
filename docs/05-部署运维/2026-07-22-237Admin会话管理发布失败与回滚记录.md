# 237 Admin 会话管理发布失败与回滚记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-07-22 |
| 发布批次 | `20260722T100501Z` |
| 变更范围 | Admin 前端/BFF 的会话归属、历史 WebChat 标题、会话列表展示与页面体验改动 |
| 结果 | 发布未完成，已回滚到切换前的 Admin 版本 |

## 1. 发布包与边界

发布包仅包含生产 `dist`、`server`、`package.json` 与锁文件。构建包核验为 272 个条目，未包含 `.env`、SQLite、报告、私钥、Token 或客户配置。

未修改 Gateway、频道插件、数据源、告警接收器、Caddy/443、防火墙或公网路由。

## 2. 备份与受控操作

使用既有受控发布器执行阶段发布。发布器在切换前创建以下批次备份：

`/var/backups/gaiop/admin-prestage-20260722T100501Z`

该备份覆盖原 Admin 应用目录、`/etc/gaiop` 环境目录和 `gaiop-admin.service` 单元。发布器在本机等待 124 秒后未获得完成回执；随后只读检查发现 `gaiop-admin.service` 处于 failed 状态，因此没有重试发布或继续变更。

受控回滚命令：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260722T100501Z
```

## 3. 非敏感诊断与恢复

只读诊断结果：服务状态为 `failed`，systemd 结果为 `timeout`，主进程退出状态为 `9`；现有诊断分类器将最近日志特征归类为 `permission-or-filesystem`。该分类不是根因结论，未记录或输出原始日志、环境值或凭据。

已执行上述批次回滚，结果为 `admin-rollback-restored`。随后执行受控启动核验：

- `gaiop-admin.service`：`active`；
- 监听：仅 `127.0.0.1:3000`；
- 健康接口：通过；
- Gateway：已连接；
- 公网路由变更：无。

## 4. 后续

当前代码已推送至 `dev-yangshuo`，但 237 仍运行回滚后的旧 Admin 版本。再次发布前应先针对本次超时与权限/文件系统诊断分类执行只读根因核对，再确定是否修复发布器或服务器运行目录问题；不得直接重复切换。
