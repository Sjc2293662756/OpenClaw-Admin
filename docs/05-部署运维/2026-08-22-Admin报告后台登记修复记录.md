# 2026-08-22 Admin 报告后台登记修复记录

## 1. 结论与当前状态

本批修复 Admin 报告“文件已经生成并被报告归因 Sidecar 捕获，但没有及时进入 Admin `report_files` 台账”的问题。

根因不是留存期限改变，也不是报告被删除，而是原来的登记同步主要挂在 `GET /api/reports` 请求上：只有管理员打开报告列表时，Admin 才会触发一次登记。报告在后台生成后，如果没有人打开列表，就会暂时处于“文件存在、Sidecar 有索引、数据库未登记”的状态。留存服务对未登记文件默认保护，因此没有误删，但这批报告没有进入完整的 365 天在线留存和 7 天恢复区流程。

本批代码已经完成、本地验证通过，并已按授权发布到 237。代码没有推送到远端 `dev-yangshuo`；生产发布批次为 `20260821T235019Z`。

- Admin 提交：`98bf3c2`；
- 发布包：`gaiop-admin-report-registration-20260821T235019Z.tgz`；
- 发布包 SHA-256：`59568ab3e05b4d5b7949d0050c1293376b4b1373ee072ae3fd5e3190bf77d3e1`；
- 发布包大小：`3,724,895` 字节；
- 暂存结果：备份和 SQLite 在线备份均成功；
- 启动结果：`admin-started-loopback-verified`。

## 2. 修复范围

### 2.1 Admin/BFF

- 新增 `server/report-registry-sync.js`。
- Admin 启动完成路由注册后立即执行一次报告和交付登记同步。
- 默认每 30 秒执行一次后台同步，可通过 `GAIOP_REPORT_REGISTRY_SYNC_INTERVAL_MS` 调整，但最小间隔为 5 秒。
- 同步复用现有 `syncGeneratedReports()` 和 `syncReportDeliveries()`，只写 Admin 自己的 `wizard.db`，不新增第二套报告登记规则。
- 单次同步失败只记录安全错误并继续运行，不使 Admin 进程退出。
- Admin 收到 `SIGINT` 或 `SIGTERM` 时停止定时器。

### 2.2 发布清单和测试

- 将 `server/report-registry-sync.js` 加入 Admin ISO 发布清单。
- 发布契约测试检查该运行文件必须随发布包交付。
- 将后台同步专项测试加入 `npm run test:node`。

### 2.3 明确不在本批范围

- 不修改 GAIOP 后端、报告 Skill、OpenClaw 核心或频道插件。
- 不扫描或删除后端目录、Gateway 目录、附件、memory 或频道发送副本。
- 不移动报告到恢复区，不执行永久删除，也不改变现有 365 天在线和 7 天恢复区期限。
- 不修改 Caddy、Syslog、journald、网络暴露或 237 系统配置。

## 3. 代码安全边界

后台同步只负责把能够由现有报告登记逻辑可靠配对的报告和审计文件写入 Admin 台账。未知文件、格式错误、路径异常、配对不完整或归属不明确的内容继续保护；同步失败也不会触发删除。

报告留存任务仍然是另一条独立链路：它只处理已登记、满足期限、通过实体和审计配对检查的报告组。新增后台同步不会把 Admin 变成全盘清理器，也不会越过 GAIOP 后端和 OpenClaw 的所有权边界。

## 4. 本地验证

验证在隔离 worktree `codex/report-registration-20260822` 完成，基于远端 `origin/dev-yangshuo=c002dcd1769d89a2ec90e000031b71ad7d81c99d`。没有修改原始脏工作副本，也没有读取或提交密码、Token、私钥、`.env` 或报告内容；数据库和备份只通过受控脚本做完整性及计数核查。

通过项目专项测试：

- 后台登记同步：2/2；
- 报告路由：4/4；
- 报告留存：8/8；
- Vitest：21 个测试文件，90/90；
- 前端生产构建：通过；仅有既有大 chunk 体积提示。

Node 全量测试共 187 项，185 项通过。以下 2 项失败与本批无关，和修改前基线一致：

- `independent policy contains only the six approved report settings`；
- `Upgrade retention production enablement repairs the live unit before starting its timer`。

另外已完成本批新增文件、入口文件和报告路由的语法检查，以及 `git diff --check`。

## 5. 237 生产发布与检查

### 5.1 发布前

本批已使用项目已有的受控脚本完成发布：

1. 从隔离 worktree 构建只包含代码、静态资源和必要运行文件的 Admin 发布包；排除 `.env`、凭据、数据库、报告、备份和 `node_modules`。
2. 运行 `ops/237/Invoke-237AdminStage.ps1`，完成发布前 Admin 目录备份和 SQLite 在线备份，再把包暂存到 237。
3. 运行 `ops/237/Invoke-237AdminStartVerify.ps1`，启动并验证 Admin。

### 5.2 发布后

启动后核查结果：

- Admin 为 `active`，健康接口 HTTP 200；
- 仍只监听 `127.0.0.1:3000`；
- Gateway 连接正常；
- Sidecar 仍为 `active/enabled`；
- 正式报告留存 timer 仍为 `active/enabled`；
- `wizard.db` `integrity_check=ok`；
- 后台同步已运行，`report_files` 登记数量增加；
- 发布前列出的 Sidecar 未登记报告逐条进入台账；
- 报告文件、审计文件和目录没有被移动或删除，未知保护项仍然保留。

### 5.3 本批现场结果

| 项目 | 结果 |
|---|---|
| Admin | `active`；健康 HTTP 200 |
| Admin 监听 | `127.0.0.1:3000`，回环监听 |
| Upgrade | `active`；健康 HTTP 200；未认证接口 HTTP 401 |
| OpenClaw Gateway | `active`；Admin 健康显示 connected |
| Caddy/HTTPS | `active`；回环 HTTPS 和公网 HTTPS 均 HTTP 200 |
| Sidecar | `active/enabled` |
| Sidecar 索引 | 43 条，43 条已登记，索引新鲜 |
| Admin `report_files` | 236 条；发布前 228 条，本批增加 8 条 |
| `wizard.db` | `PRAGMA integrity_check=ok` |
| 正式报告目录 | Admin/Gateway 均 `read-write`；配对报告 236 组 |
| 正式报告留存 timer | `active/enabled` |
| 会话留存 timer | `inactive/disabled`，保持默认关闭 |
| SQLite 备份 timer | Admin/Upgrade 均 `active/enabled` |
| 水位监测 timer | `active/enabled`；文件系统约 60.39%，状态 normal |

本批没有执行报告删除、恢复区移动、数据库删除或 OpenClaw 重启。Sidecar 43 条索引全部进入台账，原先 8 条未登记报告已经纳入 `report_files`；未登记未知文件仍按保护规则处理。

现场旧版报告存储探针的模拟数据库检查仍返回 `adminReportSyncProbe=failed`。原因是探针的假数据库只实现了 `INSERT` 查询，而现有报告同步逻辑还会准备 `SELECT` 和归因更新语句；这不是生产服务错误，也不影响本批真实数据库登记。真实生产核查已通过 `report_files=236`、`wizard.db integrity_check=ok` 和 Sidecar 全量登记结果确认。

发布前的最近一次只读核查显示：Sidecar 索引 43 条，其中 35 条已登记、8 条未登记；正式报告台账 228 条；未登记保护文件 17 个，其中 16 个属于这 8 组 Sidecar 报告和审计，另 1 个为没有可靠归属的旧报告实体。发布时应重新读取现场计数，不能直接套用本快照。

## 6. 回滚与后续

本批没有数据库结构变更。若发布后同步异常，先停止新版本并按发布批次回滚 Admin 代码；保留发布前备份和 SQLite 在线备份，不删除报告，不用备份覆盖正在使用的数据库。

后续每次同事修改报告 ID、输出目录、审计字段、文件配对命名或实际加载的 Skill，仍需补做一次真实端到端验收：

`Gateway 生成报告 -> Sidecar 捕获 -> 正式目录出现配对文件 -> Admin 后台登记 -> 报告留存候选检查`

以后如果同事修改报告 ID、输出目录、审计字段、文件配对命名或实际加载的 Skill，必须重新执行这条验收。适配失败时应继续保护未登记文件，并报警或阻止发布，不应通过猜测删除文件。
