# 2026-08-20 237 Admin 正式报告留存生产启用记录

## 1. 结论与范围

本批已在 237 启用 Admin/BFF 自有的正式报告留存：正式报告在线保留 365 天，严格超过 365 天后才允许将报告文件、审计 JSON 和已登记交付 JSON 整组移入独立恢复区；恢复区保留 7 天，严格超过 7 天后才进入永久删除候选。长期保留、路径异常、符号链接、大小或配对不一致、时间异常、归属异常和未知文件继续保护。

- 成功批次：`20260820T101109Z`；
- 受控发布器代码：`c41905ff2c63a483d2f39a6324ca7095df43a8fb`；
- 启动时远端 `origin/dev-yangshuo`：`64ec95ca54c918662582b047576b86ca5ff86a2e`；
- 回滚点：`/var/backups/gaiop/report-retention-enable-20260820T101109Z`；
- SQLite 在线安全备份完整性：`ok`。

本批没有修改报告正文，没有处理会话或附件，没有修改 OpenClaw、频道插件、GAIOP 后端、Caddy、Syslog 或 journald 配置，也没有重启 Admin、Upgrade、Gateway 或 Caddy。

## 2. 路径、权限与策略隔离

启用前后的受控核查确认：

- Admin 数据库真实路径为 `/var/lib/gaiop/admin/wizard.db`，普通文件、`0640`；
- 正式报告真实目录为 `/var/lib/gaiop/reports`，非符号链接、`gaiop:gaiop`、`0770`；
- 恢复目录为 `/var/lib/gaiop/report-recovery`，非符号链接、`gaiop:gaiop`、`0750`；
- 两个目录不相同、不互相包含，且与数据库位于同一设备 `64768`；
- `gaiop` 对两个目录均有受控读写和遍历权限，目录不允许其他用户写入。

生产 service 使用独立 `/etc/gaiop/report-retention.policy`，该文件只有 Admin 数据目录、报告目录、恢复目录、锁、批次上限和报告自动处理开关六项。生产 drop-in 先清空原 `EnvironmentFile`，再只加载该策略，并将 `/etc/gaiop/admin.env` 设为不可访问；主环境文件没有被改写。

现场发现 6 个报告留存相关 JS 文件内容哈希正确，但权限为 `0666`。发布器在候选门禁和 SQLite 在线备份完成后，将这 6 个文件收紧为 `0644`，未改变内容和属主；原 mode 清单保存在回滚点，失败回滚可恢复原状态。

## 3. 候选与实际数据变化

启用门禁时间为 2026-08-20 10:11:12 UTC。只读取数据库和文件元数据，未读取报告正文。

| 项目 | 结果 |
| --- | ---: |
| 数据库报告登记 | 228 |
| 365 天可移入恢复区候选 | 0 |
| 候选异常 | 0 |
| 长期保留报告 | 0 |
| 恢复区登记/文件 | 0 / 0 |
| 超过 7 天永久删除候选 | 0 |
| 恢复区未知文件/目录 | 0 / 0 |
| 门禁异常 | 0 |

报告树中有 3 个未纳入 `report_files` 成组登记的保护项，发布器未处理：

- `_sidecar/665776af102728c5ab3e/238web_业务综述报告_20260820_102736.docx`；
- `_sidecar/665776af102728c5ab3e/238web_业务综述报告_20260820_102736.json`；
- `_unattributed/report/napm_全局综述报告_20260731_134036.docx`。

关闭态 one-shot 返回 `auto_process_disabled`，并通过完整元数据不变比较。随后批次上限 1 开启态验证、正式上限 50 的 timer 启用和与 timer 相同 service 的手工 one-shot 均成功。因为没有到期候选，三次验证均处理 0 组：

- 报告树：`468` 个文件、`20,676,488 B`，前后不变；
- 恢复区：`0` 个文件、`0 B`，前后不变；
- 用户 6、会话 63、报告 228、交付 9、审计 1605，前后不变；
- `report_retention_artifacts` 和 `report_retention_audits` 均为 0。

## 4. Timer 与生产验收

- `gaiop-report-retention-cleanup.timer`：`active/enabled`；`Persistent=true`；下一次为 `Fri 2026-08-21 10:52:08 CST`。
- `gaiop-report-retention-cleanup.service`：正式手工 one-shot 成功，`systemd-analyze verify=ok`。
- Admin、Upgrade、Gateway、Caddy：均为 `active`，进程 PID 在发布期间未变化。
- Admin 内部健康、根入口、Gateway、HTTPS 回环和公网 HTTPS：HTTP 200；Upgrade 健康 HTTP 200，未认证接口 HTTP 401。
- Admin 仍只监听 `127.0.0.1:3000`。
- `wizard.db` 与 Upgrade SQLite 的完整 `integrity_check` 均为 `ok`。
- 文件系统约 57.49%，水位状态正常。
- Admin/Upgrade 第一批低风险清理、水位监测、Admin/Upgrade SQLite 备份 timer 均继续 `active/enabled`。
- 会话留存继续 `inactive/disabled`；Admin/Upgrade SQLite 过期备份清理继续为 `false`。

## 5. 测试与基线比较

测试使用官方校验后的 Node 22.23.2，没有修改依赖版本：

- 报告留存实现与受控发布器专项：15/15 通过；
- Admin Node 全量：185 项，184 通过；
- 修改前基线：179 项，178 通过；
- 前后唯一失败用例集合完全相同：`Upgrade retention production enablement repairs the live unit before starting its timer`，属于既有 Upgrade 留存契约断言，不是本批新增回归；
- Vitest：21 个文件、87/87 通过；
- 前端生产构建：通过，仅有既有大 chunk 提示；
- CJS、PowerShell、Bash 语法检查：通过；
- `git diff --check`：通过；
- 237 原生 `systemd-analyze verify`：通过。

## 6. 回滚与排除项

自动失败回滚只恢复报告 timer、service、drop-in、独立策略和本批收紧前的源文件 mode；不使用在线备份覆盖生产数据库，不删除报告或恢复区，不撤销已经产生的业务事实。成功回滚点和 SQLite 安全备份必须保留，人工回滚前应先重新列出恢复区、永久删除候选和报告留存状态。

本批仍明确排除：会话及附件留存、外部频道报告发送副本、OpenClaw 核心和频道插件、GAIOP 后端、Caddy/Syslog/journald 日志治理、80%/90% 自动磁盘治理，以及 SQLite 旧备份自动清理。后续任何永久删除都必须先单独列出候选并取得明确授权。
