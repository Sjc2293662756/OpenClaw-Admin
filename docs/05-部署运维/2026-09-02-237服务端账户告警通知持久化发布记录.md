# 2026-09-02 237 服务端账户告警通知持久化发布记录

## 结论

GAIOP-Admin 服务端账户告警通知稳定版已发布到 237。告警通知展示副本、账户已读/清空状态和离线汇总水位现在保存在 237 Admin SQLite；浏览器不再是通知历史和读取状态的权威存储。Receiver 仍是原始告警权威来源。

发布后的只读数据库验收发现服务器已保存 1 份共享告警事件，并为 6 个符合当时有效权限和账户设置的账户生成 6 条账户通知状态。这是发布时点的聚合证据，不包含告警正文、账户名或其他业务内容，数量后续会随正常告警变化。

## 发布范围

- 新增 `alert_notification_events`、`account_alert_notifications`、`account_alert_notification_state` 三张表，继续复用 `alert_notification_preferences`。
- 每个 Receiver 事件只保存一份安全通知投影；账户表只保存事件引用、已读、清空和是否离线创建等账户状态，不按账户复制完整正文。
- `(receiver_generation, stream_sequence)` 保证 Receiver SSE 重复投递幂等；共享事件、账户关系和 BFF 游标在同一 SQLite 事务内提交。
- 收件人按服务端最新有效模块权限、账户总开关和三级“告警通知”开关决定；页面弹窗和声音仅影响在线提示。
- 告警通知列表改为服务端游标分页和真实计数，支持全部/未读、轻微/重大/紧急、单条及筛选后已读/清空。
- 账户在线状态复用现有已认证 SSE 连接，多标签页和多设备中任一有效连接存在即为在线；离线汇总使用独立领取租约和确认水位，不把通知自动标为已读。
- 离线告警不逐条补弹、不集中播放声音；包含紧急告警的汇总保持到手动关闭，其他汇总 12 秒自动关闭。
- 未新增消息中间件、轮询、WebSocket、第二条 SSE 或独立服务；未修改 Receiver、NAPM 规则、Gateway、OpenClaw、Skill、频道插件、Caddy 或公网配置。
- 31 天仅为建议保留期，本批没有实现或启用告警通知自动清理。

## 代码与验证

- 发布基线：`origin/dev-yangshuo@daa70ff432c7066350678df659e164985f1d3edd`。
- 实现提交：`608a25d7bb3f49982e97c39b06b91571991e3b6c`，已推送 `origin/dev-yangshuo`。
- 模块权限专项 12/12、告警持久化专项 10/10、Admin Node 234/234、Vitest 189/189 通过。
- `vue-tsc`、Vite 生产构建、相关 JavaScript `node --check` 和 `git diff --check` 通过；构建仅有项目既有大分包提示。
- 临时旧数据库迁移已验证幂等，旧游标保留，三表和 `receiver_generation` 列存在，`integrity_check=ok`。

## 237 受控发布

- 首个批次 `20260902T062417Z` 错误使用 ZIP 包，服务器在 `EXTRACT` 门禁拒绝并自动回滚；新代码未启动，旧服务随后仍为 connected。该批次不属于成功发布。
- 成功发布 ID：`20260902T062544Z`。
- 白名单 tar 包仅包含 `dist`、`server`、`package.json`、`package-lock.json`，共 437 项、4,616,830 字节，SHA-256 为 `25C448E2F75E9DC41FF32EB8C167E0A2D273433CCA30905F1B172BE57DAE9DA2`；禁入项检查为 0。
- Stage 已创建 Admin 代码/配置回滚快照和 SQLite 在线备份，未启动服务、未修改 Caddy 或网络。
- StartVerify 完成且 12 秒延迟稳定性复核通过：Admin active、健康 200、Gateway connected、仅监听 `127.0.0.1:3000`，公网路由未改变。

## 生产只读验收

- Receiver 上游状态 `connected`，`gap_state=none`，`receiver_generation=1`。
- 三张新表、六个查询索引和 `alert_stream_runtime.receiver_generation` 均存在。
- SQLite `integrity_check=ok`，外键违规 0。
- 未认证通知列表和离线汇总领取均返回 401；浏览器告警 SSE 未认证访问也返回 401。
- 服务器端 `alert-notification-store.js` 和 `server/index.js` 哈希与本地实现提交一致。
- 验收时聚合计数为共享事件 1、账户通知 6、账户汇总状态 6，证明 Receiver 事件已经进入 237 SQLite 持久化链路。

生产验收使用 `scripts/gateway237-admin-alert-notification-persistence-verify.cjs`，只读取服务状态、数据库结构、完整性、聚合计数和部署哈希；未读取凭据、告警正文、账户名或其他业务内容，也未创建测试账户或注入告警。

## 待人工验收与已知边界

- 尚未由人工完成两个真实浏览器或两台设备之间的通知列表、已读、清空同步体验验收。
- 尚未人为制造生产告警验证退出登录期间汇总只出现一次、无逐条补弹和无声音；这些行为已有自动化测试覆盖。
- 极端情况下，账户恰好在 BFF 重启并追补 Receiver 积压事件期间建立 SSE，首次汇总领取可能早于积压事件写入；通知仍会持久化且刷新/重新连接后可领取汇总，但当前没有额外增加“追补完成”协议，避免为低概率时序引入第二套复杂状态机。

## 回滚

如需回滚，使用现有受控脚本并指定成功发布 ID：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260902T062544Z
```

回滚前必须评估发布后新增通知：代码回滚和 SQLite 备份回退会使发布后生成的账户通知不可保留。不得用本批回滚覆盖 Gateway 历史、Receiver 原始告警、报告文件、报告登记或其他业务数据。
