# 2026-08-18 237 磁盘水位按文件系统探针收口与启用记录

## 1. 范围与结论

第十一批只收口磁盘水位探针并启用 `gaiop-storage-watermark-monitor.timer`。生产登记的九个数据标签经固定路径 `stat`、`findmnt -T` 和 `df -P` 核查，全部位于同一个实际文件系统，因此水位配置只保留 `/var/lib/gaiop/admin` 作为唯一探针。其他八个目录不再直接暴露给监测 unit，而是在本记录中保留与该探针的文件系统覆盖关系。

本批没有修改水位算法、数据库结构、API、前端、OpenClaw、Upgrade、Caddy、Syslog、journald 或 logrotate。没有启用清理、SQLite 备份创建、数据库切换、80%治理、90%应急治理或写入阻断；没有读取、移动或删除生产业务文件。

## 2. 九标签与文件系统映射

2026-08-18 只读核查结果为：设备号 `64768`、挂载点 `/`、设备 `253:0`、文件系统 `ext4`。九个固定路径的 `df -P` 均指向同一设备。`upgrade_rollback` 和 `gateway_state` 由 root 对路径本身取得设备和挂载证据；`gaiop` 不直接访问这两个受保护目录，也没有修改其权限。

| 数据标签 | 生产路径 | 所属文件系统 | 使用的监测探针 | 覆盖方式 |
|---|---|---|---|---|
| `admin_state` | `/var/lib/gaiop/admin` | `64768` / `/` / `253:0` | `/var/lib/gaiop/admin` | 直接探针 |
| `runtime_state` | `/var/lib/gaiop/runtime` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |
| `formal_reports` | `/var/lib/gaiop/reports` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |
| `upgrade_state` | `/var/lib/gaiop-upgrade` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |
| `upgrade_rollback` | `/var/backups/gaiop/upgrade` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖，不直接检查 |
| `admin_upgrade_staging` | `/opt/gaiop/admin/data/upgrade-upload-staging` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |
| `gateway_state` | `/home/netinside/.openclaw` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖，不直接检查 |
| `raw_syslog` | `/var/log/netinside` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |
| `caddy_access_logs` | `/var/log/caddy` | 同上 | `/var/lib/gaiop/admin` | 同一文件系统覆盖 |

生产水位配置最终只含 `admin_state -> /var/lib/gaiop/admin`。水位状态中的当前标签表示实际探针，不表示服务直接检查了其余八个受保护目录；九标签的完整覆盖关系以本表为准。

## 3. 配置、发布和回滚

实现提交为 `e64d183447a22b5d96327942f8cea5f26229fb58`。候选包严格只含 `managed-roots.json`、水位 service 和 timer，SHA-256 为 `9b2723241719f1a9875ba84fdcceff165c7a66ffe22eaffb9467046cfa2a4ade`。

发布批次为 `20260818T021214Z`，生产回滚点为 `/var/backups/gaiop/storage-watermark-probes-20260818T021214Z`。生产文件哈希为：

| 文件 | SHA-256 |
|---|---|
| 水位探针配置 | `7236675a01b42de7207e920372333fb518db242d22f1552f411c0532ce4ead29` |
| 水位 service | `2739bb3083e284af1828925efe41909690b376214815cdedd14194374e8530c2` |
| 水位 timer | `99d4d03e9a4b1d0ae000830d9d53ab26e214a64531e611058c2f68d50ee4ede6` |

候选安装前和安装后均通过原生 `systemd-analyze verify`。unit 仍以 `gaiop` 运行，只写 `/var/lib/gaiop/admin` 中现有水位状态；不再包含 Gateway、Upgrade 回滚、Admin staging、Syslog 或 Caddy 日志目录的绑定或只读暴露。配置和 unit 变更没有重启 Admin、Upgrade、Gateway 或 Caddy。

## 4. one-shot 与 timer 验证

以 `gaiop` 对唯一探针执行 `stat/statfs` 成功，设备号与 root 核查一致。启用前连续三次 one-shot 均成功：当前文件系统数和识别探针数均为 `1`，unknown 为 `0`，事件计数保持 `1 -> 1 -> 1 -> 1`，没有重复写入同状态事件。

timer 启用后观察了两个新的完整周期：

| 项目 | UTC 时间 |
|---|---|
| 启用后观察基准 | `2026-08-18T02:12:52.679Z` |
| 第一个 timer 周期完成 | `2026-08-18T02:15:25.523Z` |
| 第二个 timer 周期完成 | `2026-08-18T02:20:16.497Z` |

最终 timer 为 `active/enabled`，当时上次触发为 `2026-08-18 10:20:16 CST`，下次触发为 `2026-08-18 10:25:16 CST`。最终原始使用率为 `55.4976%`，状态 `normal`；同一时刻 `df -P` 展示为 `56%`，两者符合 `df -P` 原始块数计算和整数显示规则。

## 5. 数据库、API 与服务

- `wizard.db` 的 `PRAGMA integrity_check` 为 `ok`。
- 发布前后聚合计数不变：用户 `6`、会话元数据 `63`、报告文件 `222`、报告交付 `9`。
- 水位当前状态只有一个已识别文件系统，历史事件数保持 `1`；已有历史状态和事件未删除。
- 部署代码上的水位角色与脱敏测试通过，状态投影不含完整路径；未认证接口仍拒绝访问。
- Admin、Upgrade、Gateway、Caddy 均为 `active`；Admin、Upgrade、Gateway 内部健康、Caddy 回环 HTTPS 和公网 HTTPS 均为 `200`。
- Admin 继续只监听 `127.0.0.1:3000`，Upgrade 继续只监听 `127.0.0.1:18900`。
- 观察窗口内没有新增水位或 Admin 启动、迁移、SQLite 错误。

## 6. 关闭态

除水位 timer 外，以下 timer 最终均为 `inactive/disabled`：Admin 与 Upgrade 第一批清理、报告留存、会话留存、Admin SQLite 备份、Upgrade SQLite 备份。自动删除、报告/会话自动处理和 SQLite 备份创建/清理开关继续使用关闭默认值。

本批没有部署后端，没有应用 Caddy、Syslog、journald 或 logrotate 配置，没有调用任何清理器，没有创建 SQLite 定时备份，没有修改生产业务数据，也没有开启80%或90%治理。后续清理机制仍须单独实施，本记录不授权继续开启。

## 7. 测试结果

- 水位、ISO 和统一留存定向 Node 测试通过；水位相关组合 `16/16` 通过。
- Admin Node 全量 `173/174`；唯一失败仍为既有的报告来源清理固定时间与当前临时文件 mtime 冲突，本批未修改该实现或测试。
- Vitest `21` 个文件、`87/87` 通过。
- 前端类型检查与生产构建通过，Vite 转换 `4646` modules。
- JavaScript、MJS 和 PowerShell parser 检查通过。
- systemd 静态契约、生产原生 `systemd-analyze verify` 和 `git diff --check` 通过。
