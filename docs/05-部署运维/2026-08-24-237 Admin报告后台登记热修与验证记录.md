# 2026-08-24 237 Admin报告后台登记热修与验证记录

## 结论

237 上报告文件持续写入 `/var/lib/gaiop/reports`，但 Admin 的 `report_files` 登记表没有同步更新，因此“报告文件管理”列表不显示新报告。问题不是报告正文生成失败，而是文件写入后的 Admin 登记链路缺失。

本次只修复 Admin 登记链路，未修改 OpenClaw、频道插件、Upgrade、Gateway 或 Caddy。

## 根因

当前生产 Admin 的 `server/routes/reports.js` 已有报告和交付扫描函数，但函数未导出；`server/index.js` 没有启动后台登记同步，也没有退出时清理同步定时器。Skill 修复后，报告文件和审计 JSON 已经成对写入正式目录，但没有进 `report_files`，所以前端管理器查询不到。

## 发布与回滚证据

- 发布标识：`20260824T052900Z`
- 发布器：`scripts/Invoke-237AdminReportRegistryHotfix.ps1`
- 变更文件：`server/index.js`、`server/routes/reports.js`、`server/report-registry-sync.js`
- SQLite 在线备份和完整性检查：通过
- 备份点：`/var/backups/gaiop/admin-report-registry-20260824T052900Z`
- 发布失败时已验证自动回滚；未恢复数据库、未删除报告文件或恢复区文件

发布器在切换前保存三份代码文件和 `wizard.db` 在线备份，只停止并重启 `gaiop-admin.service`。失败时恢复代码文件并重新启动原本 active 的 Admin，数据库备份仅作为回滚证据保留，不用于覆盖生产数据库。

## 生产验证

- 后台同步日志：`Background registry sync registered 6 report(s)`
- `report_files`：236 -> 242
- 6 组最新报告与审计 JSON 配对存在，登记状态为 `ready`
- Admin：active；监听 `127.0.0.1:3000`
- Admin、Upgrade、Gateway、Caddy：active
- Admin、Upgrade、Gateway、内部 HTTPS、公网 HTTPS 健康检查：200（Upgrade 未认证接口 401）
- Admin `PRAGMA integrity_check`：`ok`
- 生产后验时间：2026-08-24 04:41Z
- 会话留存：inactive/disabled

本次只读取报告元数据、路径、数量、时间、大小和登记关系，没有读取报告正文。
