# 237 仪表盘默认今日范围发布记录

> 发布日期：2026-07-31（UTC+8）
> 代码仓库：`GAIOP-Admin`
> 代码提交：`dev-yangshuo@3b00975`
> 发布批次：`20260731T020539Z`

## 1. 变更

- 每次登录后新建仪表盘时，统计范围默认由“最近7日”改为“今日”。
- 用户手动选择其他时间范围后，当前仪表盘实例继续按用户选择查询。
- 时间范围不写入本地持久化；退出登录后组件销毁，再次登录重新默认为“今日”。
- 保留上一批次的 BFF 并发合并、30秒短缓存、字段投影和最新请求保护。
- 未修改 OpenClaw、GAIOP 后端、数据库或登录接口。

## 2. 测试

- `npm run test`
  - Node：55项通过。
  - Vitest：13个文件、45项通过。
- 仪表盘组件测试确认：
  - 服务端时间为 2026-07-31 时，首个统计请求的开始和结束日期均为 `2026-07-31`。
  - 用户手动选择 2026-07-02 至 2026-07-31 后，下一个请求使用该范围。
- `npm run build` 通过，仅有既有的大分包体积提示。

## 3. 发布

发布包只包含 `dist/`、`server/`、`package.json` 和 `package-lock.json`：

- 文件数：287
- 大小：3556412 B
- SHA-256：`e137b5ace81af9c919e30b568cb71a7a89d21d111a71ccb2daf65d47d89694de`
- 未包含 `.env`、`data/`、`.bak`、本地数据库、图片删除记录或临时目录。

受控发布已创建服务目录和数据库备份，未修改 Caddy、公网路由或网络配置。

## 4. 237 验证

| 验证项 | 结果 |
|---|---:|
| 今日统计冷查询 | 654 ms |
| 今日原始响应 | 525 B |
| 最近7日统计冷查询 | 1650 ms |
| 最近7日原始响应 | 154413 B |
| 最近30日统计冷查询 | 5996 ms |
| 服务状态 | active |
| 健康检查 | HTTP 200 |
| Gateway | connected |
| 服务重启次数 | 0 |
| 监听 | `127.0.0.1:3000` |
| 发布入口资产 | `assets/index-B89IVFSv.js` |

生产探针执行时“今日”尚无会话统计，因此响应体仅 525 B；即使当天数据增加，默认一天范围仍可避免每次登录都扫描最近7至30日的历史数据。

## 5. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T020539Z'
```

回滚只恢复本批次之前的 Admin 服务目录和数据库备份。
