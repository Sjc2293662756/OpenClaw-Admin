# 2026-09-02 237 WebChat 显示思考过程设置发布记录

## 1. 变更范围

- 仅发布 `GAIOP-Admin` 的账户级 WebChat 显示设置。
- 用户可在头像菜单“我的设置”中切换“显示思考过程”。默认开启；关闭后只隐藏思考、工具执行和回复状态，普通对话结果、报告完成消息、独立报告附件卡片、停止操作和流式输出保持不变。
- `GET/PUT /api/chat/preferences` 只允许当前已认证账户读取/更新自己的设置；`basic`、`standard`、`admin`、`auditor` 均可用，不授予报告管理器、预览、删除、恢复区或他人报告权限。
- 未修改 OpenClaw、Gateway 核心、Skill、频道插件、GAIOP 后端、Caddy、公网路由、系统日志配置或报告下载接口。

## 2. 本地验证

- 使用兼容 `better-sqlite3` 的 Node `v22.14.0`；未使用 Node 24 结果作为证据。
- Admin Node 回归：`231/231` 通过；修改前 Node 22 基线：`227/227` 通过；前后失败用例名称集合均为空。
- Vitest 全量：35 个文件、`188/188` 通过。
- 模块权限回归：`12/12` 通过。
- 生产构建：4682 个模块通过。
- 修改/新增 JavaScript `node --check`：10 个文件通过；`git diff --check` 通过。
- 浏览器验收：本地临时数据库和测试账号验证“我的设置”、用户可见名称、默认开启、关闭后刷新保持关闭；390×844 移动视口无横向溢出或控件遮挡。

## 3. 受控发布

- 基线：`origin/dev-yangshuo` 发布前为 `41d3145b29555ef7282b134e32d4eb2a1b53f9e6`。
- 功能提交：`17d8171`（`feat(webchat): add thinking process display preference`）。
- 发布批次：`20260902T020030Z`。
- 发布包白名单仅含 `dist`、`server`、`package.json`、`package-lock.json`；435 个条目，禁止路径 0；包 SHA-256 为 `e7ce32a102359e95eaeed3c9e20fe8f29d01a2477860f7f07d0d7b5788bd1147`。
- stage 回执：完成，Admin 回滚快照已创建，SQLite 在线备份已创建，未修改 Caddy/网络。
- start/verify 回执：`admin-started-loopback-verified`，稳定性复核通过，监听 `loopback-ipv4`，公网路由未改变。

## 4. 生产复核

- 237 Admin：`active`、`enabled`，仅 `127.0.0.1:3000`；健康 HTTP 200；Gateway、Upgrade 内部健康 HTTP 200；Caddy active；公网 HTTPS `/` 返回 200。
- 发布前后报告存储聚合计数不变：报告文件 540、审计 JSON 274、配对报告 265；未读取报告正文。
- 发布后 237 同名前端分包与本地构建产物 SHA-256 一致：`chat-display-preferences`、`ChatDisplayPreferencesPanel`、`ChatWorkspace` 均一致并返回 HTTP 200。
- 会话留存保持关闭；报告留存、低风险清理、水位监测和 SQLite 备份任务未被本次变更修改。
- 只读预检仍报告既有 Gateway CLI/部分报告登记探针不可用；这是发布前后均存在的独立运行态问题，本批次未借机修改上游或报告数据。

## 5. 后端变更注意事项

后续修改 GAIOP 后端或 Gateway 时，必须保留 Admin 依赖的认证用户 ID、会话归属、持久消息 ID/时间/角色、报告来源会话和来源消息 ID/预览等字段及其稳定语义；不得把流式临时对象替换成缺少持久消息 ID 的对象，不得用文件名、时间或“最近一条助手消息”推断报告归属。报告正式登记、`report_files` 权限过滤和现有下载接口必须继续工作，且后端发布后要用真实消息转持久历史、跨会话隔离、报告卡片连续顺序和本人/非本人下载测试复核。若上游契约字段需要变化，应先同步 Admin/BFF 契约和回归测试，再发布上游。

## 6. 回滚

使用受控发布状态中记录的 Admin 代码快照和 `wizard.db` 在线备份回滚；回滚后重新执行 Admin/Gateway/Upgrade/Caddy/公网 HTTPS 健康、回环监听、报告计数和前端静态资源校验。不得通过回滚脚本删除报告文件或修改 OpenClaw、Skill、频道插件和 Caddy。
