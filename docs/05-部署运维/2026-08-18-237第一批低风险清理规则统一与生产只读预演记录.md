# 237第一批低风险清理规则统一与生产只读预演记录

## 1. 范围与版本

- 批次：第十三批；目标是统一第一批低风险清理的候选发现逻辑，并在237执行关闭态只读预演。
- Admin远端 `dev-yangshuo`：`889f8fedc6db8aa70b167405baa50aa6a9b9ff7d`。
- Upgrade远端 `dev-yangshuo`：`cdd9fdb5fed46a9bb82889e9db117a202717a66c`。
- 两个版本均基于实时远端开发分支建立隔离worktree后精确推送，未修改后端仓库、原工作副本或正式开发分支。
- 发布批次：`20260818T063501Z`。

发布包已通过原生 `systemd-analyze verify`。Admin包包含完整 `dist`、`server`、`package.json`、`package-lock.json`和`deploy`；Upgrade包包含完整 `src`、`package.json`、`package-lock.json`和`deploy`。包内不含 `scripts`、`data` 或 `.env`。

| 组件 | 包SHA-256 | 回滚点 | SQLite在线备份 | 备份完整性 |
| --- | --- | --- | --- | --- |
| Upgrade | `b582acfe290ce3a94be10fe17c72bde69690cb3c7c5cb59be5c0314e451eacd3` | `/var/backups/gaiop/deployments/upgrade-retention-20260818T063501Z` | `.../napm-upgrade.db.before-release` | `ok` |
| Admin | `dab262fdd348c34338c9576054e8d3ff5f03d94739b27a0aa8921c5e38b144a0` | `/var/backups/gaiop/admin-prestage-20260818T063501Z` | `.../wizard.db.before-retention-release` | `ok` |

Upgrade先于Admin发布。两个发布过程均使用受控发布器，未直接覆盖生产目录；未发生组件回滚。

## 2. 生产执行边界

发布前实时核对：Admin、Upgrade、Gateway、Caddy均为 `active`；Admin健康200且仅监听 `127.0.0.1:3000`；Upgrade健康200，未认证接口401；公网HTTPS返回200。发布前六个清理/备份timer均为 `inactive/disabled`，水位timer为 `active/enabled`。

清理unit实时属性如下，依据237的 `systemctl show` 结果记录，不沿用旧文档假设：

| unit | User/Group | WorkingDirectory | 沙箱与读写边界 |
| --- | --- | --- | --- |
| `gaiop-admin-retention-cleanup.service` | `gaiop/gaiop` | `/opt/gaiop/admin` | `NoNewPrivileges=yes`、`ProtectSystem=strict`、`ProtectHome=yes`、`PrivateTmp=yes`、`PrivateDevices=yes`；读写仅为Admin状态、报告来源信封、Admin staging和运行锁目录 |
| `gaiop-upgrade-retention-cleanup.service` | `root/root` | `/opt/gaiop-upgrade-e2e-20260805` | `NoNewPrivileges=yes`、`ProtectSystem=strict`、`ProtectHome=read-only`、`PrivateTmp=yes`、`PrivateDevices=yes`；读写仅为Upgrade状态、回滚目录和运行锁目录 |

本批没有读取环境文件、秘密、聊天正文、附件或生产会话；没有修改权限、账户、目录权限或网络边界。

## 3. 统一候选与237 dry-run结果

Admin和Upgrade的dry-run与真实删除共用同一候选发现函数。关闭态运行只获取候选摘要，不执行 `unlink`、`rm`、数据库 `DELETE` 或业务文件修改；输出不包含路径、文件名、任务ID、正文或秘密。

237本批systemd journal中的脱敏结果：

| 组件/类别 | safe_candidate数量 | 字节数 | 最早UTC | 最晚UTC | protected/unknown摘要 |
| --- | ---: | ---: | --- | --- | --- |
| Admin来源信封 `report_provenance_envelope` | 63 | 34,042 | 2026-07-23T06:47:13.676Z | 2026-08-14T03:25:36.628Z | 本次没有失败项 |
| Admin上传中转 `admin_upgrade_upload_staging` | 0 | 0 | - | - | 无候选 |
| Upgrade成功/失败/回滚包 `upgrade_task_package` | 0 | 0 | - | - | 无候选 |
| Upgrade staging `upgrade_staging_package` | 0 | 0 | - | - | 无候选 |
| Upgrade回滚备份 `upgrade_rollback_backup` | 0 | 0 | - | - | 3项 `unregistered_directory`，全部保护 |

Upgrade dry-run的三个类别均按真实数据库归属、任务状态、文件身份和期限规则执行；未登记物理目录没有深入扫描或删除。严格期限边界（24小时、48小时、7天、90天）及共享回滚组/最近5组保护已由专项测试覆盖。

## 4. 前后不变性与状态

发布前（2026-08-18T06:36:26Z）与最终核对（2026-08-18T06:50:25Z）的受管目录结果：

| 受管目录类别 | 数量（前/后） | 文件字节（前/后） |
| --- | ---: | ---: |
| 报告来源信封 | 63 / 63 | 34,042 / 34,042 |
| Admin上传中转 | 0 / 0 | 0 / 0 |
| Upgrade包 | 2 / 2 | 25,169,245 / 25,169,245 |
| Upgrade staging | 0 / 0 | 0 / 0 |
| Upgrade回滚目录 | 3 / 3 | 未登记目录不展开统计，未发生删除 |

Upgrade数据库前后均为完整性 `ok`、总行19，任务2、组件14、SQLite备份记录0。Admin数据库业务表前后均不变：用户6、会话63、报告228、交付9。Admin总行从1927变为1930，`audit_logs` 从1590变为1593；增加的3条是发布器为验证新增API权限而产生的“拒绝受保护请求”运行审计，不是dry-run、清理或数据库删除写入，未记录聊天正文或附件内容。不得据此宣称完整数据库总行数未变。

Admin和Upgrade的 `wizard.db`/`napm-upgrade.db` integrity check均为 `ok`。发布后Admin、Upgrade、Gateway、Caddy均为 `active`，Admin健康200且保持loopback监听，Upgrade健康200且未认证接口401，公网HTTPS 200。

发布后状态：

- Admin第一批清理、Upgrade第一批清理：`inactive/disabled`。
- 报告留存、会话留存、Admin SQLite、Upgrade SQLite：`inactive/disabled`；所有自动删除和备份创建开关继续为关闭态默认值。
- 水位timer：`active/enabled`，systemd原生校验 `ok`；当前水位为 `df -P` 57%，水位记录原始使用率约56.50%，状态 `normal`，低于75%没有错误告警。
- 本批未部署后端留存分支，未应用Caddy、rsyslog、journald或logrotate配置，未启用80%/90%治理或高容量写入阻断。

整体文件系统使用率从56%变为57%是本批发布回滚包和部署快照的新增占用，不是业务目录清理释放结果；本批没有删除、移动、压缩任何生产业务文件。

## 5. 测试与一致性证据

- Admin专项测试：21/21；全量Node测试退出码0；Vitest 21个文件、87/87；前端生产构建通过（4646 modules）。
- Upgrade专项测试：20/20；全仓测试：95/95。
- 两仓Node语法检查、Upgrade/相关脚本语法检查、`git diff --check`：通过。
- Admin/Upgrade统一候选逻辑测试覆盖：dry-run与真实模式候选集合一致、dry-run零删除/零数据库写入、四类严格期限边界、未登记/未知/符号链接/越界保护、活动任务保护、共享回滚组与最近5组保护、审计预留失败时删除调用为0、删除前身份变化跳过。
- 237原生 `systemd-analyze verify`：通过；关闭态Admin四个one-shot和Upgrade两个one-shot均成功；重复启动迁移幂等，新增迁移表保留。
- 测试使用Node 22.23.2；本机Node 24的`better-sqlite3` ABI不兼容属于既有环境限制，未修改依赖清单。

## 6. 结论

本批已完成统一候选规则、生产关闭态dry-run和Admin/Upgrade关闭态发布。没有首次真实删除，没有启用任何清理timer，没有调用生产清理器执行删除，没有读取或删除生产会话/附件/业务数据。后续首次真实清理需单独授权，并继续保持按组件、按类别、按批次上限和删除前复核执行。
