# 2026-08-30 237 基础用户告警导出与偏好重试发布记录

## 发布范围

- Admin 从 `045f849626dca807bfae11c99a524812ae3d885c` 更新至 `dc5903a14b4d7c6222ec7f2a6e1e5f80b6472bdd`。
- 基础用户现在可调用 `POST /api/alerts/export` 导出**当前页面**已有的六个展示字段；服务端最多处理 100 行，继续清理输入、生成既有 XLSX、写入既有导出审计，且不会额外查询 Receiver 或返回隐藏字段。
- 告警规则、接收器/系统配置和其它管理写操作仍被拒绝；没有实现逐用户模块授权，也没有扩大 Gateway RPC 或非告警 SSE 权限。
- 当前账户告警偏好读取失败时保持全部开启的安全默认值，并在通知抽屉设置面板提供显式重试；不形成自动请求循环，账户切换或退出会 Abort 并失效旧请求。
- 未修改 GAIOP 后端、Receiver、OpenClaw/Gateway 核心、Skill、频道插件、防火墙或公网路由。

## 版本、发布包与回滚

| 项目 | 记录 |
| --- | --- |
| 远端分支 | `origin/dev-yangshuo` |
| 发布提交 | `dc5903a14b4d7c6222ec7f2a6e1e5f80b6472bdd` |
| Release ID | `20260830T060817Z` |
| 发布包 | 仅 `dist`、`server`、`package.json`、`package-lock.json`，423 个条目 |
| 发布包 SHA-256 | `b62311da4430986b24ce120b5bc017eaae327509d5d555471d75ffbadf38de64` |
| 回滚 | 使用 Release ID `20260830T060817Z` 的既有 Admin 回滚入口；不得覆盖生产 SQLite、告警历史、报告或运行时凭据 |

发布包已按路径白名单核验，不含 `.env`、SQLite、报告/告警历史、`node_modules` 或凭据。Stage 已创建既有 Admin、systemd、运行配置和在线 SQLite 备份，未修改 Caddy 或网络设置。

## 测试证据

- `node --test server/routes/alerts.test.js` 使用注入的离线 `readAlertChanges` stub 连续运行两次，均为 11 项通过；basic `/changes` 不再依赖本地或 237 Receiver/隧道状态。
- 定向 Node：权限与告警路由 23 项通过；覆盖 basic XLSX、四角色不回退、空数组拒绝、101 行限额和控制字符清理。
- 定向 Vitest：23 项通过；覆盖首次偏好读取失败、显式重试、账户切换 Abort/旧响应失效，以及设置面板的“重试”入口。
- 完整 `npm test`：Node 220 项通过、Vitest 32 文件/170 项通过；`npm run build` 与 `git diff --check` 通过。

## 237 受控发布与验证

- `Invoke-237AdminStage.ps1` 完成，`backupCreated=true`、`databaseBackupCreated=true`，没有依赖失败。
- `Invoke-237AdminStartVerify.ps1` 返回 `admin-started-loopback-verified`，监听范围为 `loopback-ipv4`，未改变公网路由。
- 告警流只读核验通过：健康接口正常，未认证浏览器 SSE 继续拒绝，Receiver 上游为 `connected`，`gapState=none`。
- 未注入生产告警、未触发外部频道，未读取、打印或复制任何用户登录凭据。

## 真实交互验收

仍建议使用现有基础用户账号完成一次浏览器验收：从告警列表导出当前页、在通知设置读取失败时点击“重试”，并确认导出不包含详情或隐藏字段。此操作未在生产中以用户账号自动模拟。
