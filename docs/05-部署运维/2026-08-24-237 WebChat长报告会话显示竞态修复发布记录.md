# 2026-08-24 237 WebChat 长报告会话显示竞态修复发布记录

## 1. 结论与范围

本批处理“生成最近七天综述报告”等长时间报告请求在聊天区不显示、刷新后回到“开始智能运维分析”的同类问题。问题属于 Admin WebChat 前端/BFF 会话消息显示链路，不是报告存储清理问题，也没有修改 GAIOP 后端、Gateway、OpenClaw、频道插件或报告数据。

覆盖范围：

- `useChatStore` 的会话历史回拉、实时事件识别和发送后兜底刷新；
- BFF 会话事件归属提取；
- ChatPage、Office AgentChatPanel 等复用 `useChatStore` 的 Admin 对话入口；
- 回归测试和生产构建。

## 2. 根因

报告请求与普通问答的执行形态不同：`chat.send` 可能在较长工具链完成后才返回，同时新会话路由切换会触发历史回拉。旧逻辑存在两个相互叠加的窗口：

1. 本地刚追加用户消息后，较早发出的 `chat.history` 可能返回空数组或不完整历史，旧赋值直接覆盖本地消息，页面因此被清空；
2. 报告/工具事件可能嵌套在超过原四层的 Gateway 信封中，前端和 BFF 提取不到精确 `sessionKey`，实时事件被丢弃；而发送期间没有提前启动历史兜底，实时事件丢失时没有恢复路径。

## 3. 修复内容

- 将前端和 BFF 的事件遍历边界从 4 层提高到 8 层，覆盖 `context`、`meta`、`request`、`response`、`envelope`、`body` 等信封，并在已知字段之外继续检查对象字段；仍要求精确会话键匹配，不放宽跨会话授权。
- 所有历史刷新统一合并本地待确认消息，空/部分历史不能覆盖刚发送的用户回合。
- 发送后立即启动 1.4 秒、4.2 秒和 10 秒的同会话历史刷新；发送 RPC 成功后保留第二轮短窗口，失败时清理定时器和本地占位消息。
- 为深层报告事件、路由历史竞态和长时间发送补充自动化回归测试。

## 4. 验证

- 前端相关 Vitest：5 个测试文件、25 项通过；
- `npm run build`：`vue-tsc -b` 与 Vite 生产构建通过；
- `git diff --check`：通过；
- BFF 会话测试新增的深层事件授权用例通过。其余本机 BFF SQLite 用例受 Windows Node 24 与 `better-sqlite3` 原生 ABI/构建工具链不匹配影响，未改依赖清单；237 生产验证仍以受控启动、健康、Gateway 连接和资源哈希为准。

## 5. 发布信息

- 代码分支：`fix/webchat-report-transcript`；
- 代码提交：`7542e4d7a41669bbb7636b01ea46a8e1647129ea` (`fix(webchat): preserve long report conversations`)；
- 发布批次：`20260824T035247Z`；
- 发布包：`gaiop-admin-webchat-long-report-20260824T035247Z.tgz`；
- 发布包 SHA-256：`a9e99dd14d59a71c2c3700d78f335a4b7b92e68b2451d2ed16656e6078b3e037`；
- 本地构建 `dist/index.html` SHA-256：`9c31c32477f34694275b90511d5d02d346c9c6000099ff58fcd5bef1aec77256`；
- 237 回滚点：`/var/backups/gaiop/admin-prestage-20260824T035247Z`。

## 6. 237 验证记录

发布使用 `Invoke-237AdminStage.ps1`、`Invoke-237AdminStartVerify.ps1` 和 `Invoke-237AdminPerformanceProbe.ps1`。

实际结果（2026-08-24）：

- stage：`completed=true`，`backupCreated=true`，`databaseBackupCreated=true`；服务未在 stage 阶段启动，Caddy 和网络未修改。
- 首次 start-verify 在 `GATEWAY_CONNECTED` 等待窗口结束并自动停止服务；随后受控 start-probe 返回 `connected`，第二次 start-verify 成功，属于 Gateway 短时连接等待，不是应用启动失败。
- 最终 start-verify：`completed=true`，`status=admin-started-loopback-verified`，监听 `loopback-ipv4`，未回滚。
- performance probe：服务 `active`、健康 HTTP `200`、Gateway `connected`、监听 `127.0.0.1:3000`、`NRestarts=0`、进程退出码 `0`、磁盘水位 `62%`；会话列表 100 条，BFF 增强后 99 条，默认运行会话不可见，外部来源错标 `0`。
- 性能探针选取的当前活动会话历史调用返回运行时失败（Gateway 对该会话没有可回放历史）；这与本批前端竞态修复不同，不能作为报告生成成功样本。用户验收应使用新建 WebChat 会话发送报告并观察实时消息、刷新后的历史回放。

## 7. 回滚

发布失败时只恢复本批 Admin 程序、前端资产、unit 和发布前运行状态，不覆盖 `wizard.db`、报告、会话、附件、环境秘密或其他业务数据：

```powershell
.\ops\237\Invoke-237AdminRollback.ps1 -ReleaseId 20260824T035247Z
```
