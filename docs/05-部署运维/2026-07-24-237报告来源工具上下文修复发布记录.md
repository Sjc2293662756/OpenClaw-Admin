# 237 报告来源工具上下文修复发布记录

> 后续核验发现本批次仍未完成真实链路：Admin 基础 `EnvironmentFile` 覆盖了 drop-in 密钥，且报告工具把包装对象传给了报告 Skill。本文保留为问题追踪记录，最终修复见 `2026-07-24-237报告来源链路最终修复发布记录.md`。

| 项目 | 内容 |
|---|---|
| 日期 | 2026-07-24 |
| 成功发布批次 | `20260724T061432Z` |
| 服务器备份 | `/var/backups/gaiop/report-provenance-20260724T061432Z` |
| 验证样本 | `/reports/napm_全局综述报告_20260724_140508.docx` |

## 现象与证据

14:04:59 Admin 已为真实 Web 会话写入带用户、渠道、会话和数据源的签名快照；14:05:08 生成的报告审计 JSON 仍进入 `_unattributed`，且四个来源字段为空。这证明 Admin 写入正常，缺口在 Gateway 报告工具调用阶段。

237 当前 OpenClaw 运行时不会向插件注册工具的 `before_tool_call` 上下文传递 `sessionKey`。原实现依赖该上下文建立 trace 与来源的映射，因此真实报告无法命中；发布探针直接调用读取函数，没有覆盖这个运行时缺口。

## 修复

- `napm-report-export` 改用 OpenClaw 工具工厂注册。
- 工具工厂创建报告工具时直接捕获该次会话的真实 `sessionKey`。
- 报告执行时按捕获的会话键读取快照，并继续执行既有 HMAC、会话 ID 和有效期验证。
- 不采用“最新快照”或模糊匹配，避免并发会话之间串用户。
- Admin 来源预览兼容 `params.message` 和 `params.input` 两种请求字段。

## 验证

- Admin Node 测试 42/42 通过。
- Gateway 报告来源与输入契约测试 9/9 通过。
- Admin 前端生产构建通过。
- 发布脚本完成备份、替换、Gateway/Admin 重启和签名快照跨进程探针。
- `gaiop-admin.service` 与 `openclaw-gateway.service` 均为 `active`。
- Admin 仍仅监听 `127.0.0.1:3000`。
- 正式报告目录 174 份报告与 174 份审计 JSON 成对存在；列表、结构和同步探针通过。

14:05:08 的报告在本批次发布前生成，不自动回填。验收应使用本批次发布后通过 Web 会话新发送的报告生成请求。
