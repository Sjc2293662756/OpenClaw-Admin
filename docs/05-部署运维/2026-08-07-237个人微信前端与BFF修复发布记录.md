# 2026-08-07 237 个人微信前端与 BFF 修复发布记录

## 1. 发布范围

- 发布批次：`20260807T080129Z`
- 仓库：`GAIOP-Admin/dev-yangshuo`
- 前端：个人微信折叠状态、配置状态、渠道总开关、刷新和轮询行为与同页其他频道统一；面板收起后停止个人微信状态轮询，展开后恢复。
- BFF/适配器：内部 Token 启动保护、并发配置原子写入、账号启停/删除/扫码落库补偿、运行状态未知语义、个人微信报告账号归因和关键写操作审计。
- 未变更：公网入口、Caddy、DNS、防火墙、OpenClaw 核心、个人微信插件版本、其他频道插件、升级服务和 238。

内部 Token 是 Admin BFF 调用回环适配器的服务间凭证，不是微信注册信息。管理员仍只需填写管理名称并使用微信扫码；部署脚本负责在服务器内部生成或沿用 Token，页面不输入、不读取也不回显该值。

## 2. 审计边界

以下个人微信写操作记录成功或失败审计：

- 启动、完成、失败、验证码提交和取消扫码接入；
- 账号启用、停用和删除；
- 个人微信渠道总开关启用和停用。

状态刷新和扫码状态轮询属于只读高频请求，不写审计，避免审计日志被轮询噪声淹没。审计详情不记录二维码、验证码、登录会话标识、Token 或微信登录凭据。

## 3. 本地验证

| 验证项 | 结果 |
| --- | --- |
| 基线 | 从实时 `origin/dev-yangshuo@b2b085d` 创建隔离 worktree，保留同日平台品牌改动 |
| Node 测试 | 118 项通过 |
| Vitest | 21 个文件、86 项通过 |
| 合计 | 204 项通过，0 失败 |
| 适配器真实子进程测试 | 缺少 Token 拒绝启动、无鉴权 401、并发账号写入不互相覆盖 |
| 生产构建 | `npm run build` 通过 |
| 差异检查 | `git diff --check` 通过 |

## 4. 备份与发布

- 发布前全量回滚备份：`/var/backups/gaiop/deployments/personal-wechat-before-20260807T080129Z`
  - `filesystem.tar.gz`：591,375,647 bytes；
  - `SHA256SUMS` 校验通过；
  - `INCOMPLETE` 标记已清除；
  - 备份结束后 Admin 和 Gateway 均恢复正常。
- Admin 切换前快照：`/var/backups/gaiop/admin-prestage-20260807T080129Z`，包含旧 Admin、服务配置和生产 SQLite 一致性备份。
- Admin 发布包：`gaiop-admin-20260807T080129Z.tgz`，3,669,091 bytes。
- 发布包 SHA-256：`e3642a027999a4fbe0bee969653f1af52f392ccc2fe2ab0ecabbc54bd245eb3b`。
- 适配器受控发布完成，保留已存在的内部 Token；个人微信插件保持 `2.4.4`，未执行插件安装或升级。
- Admin stage 完成且未修改 Caddy/网络，随后独立启动验证为 `admin-started-loopback-verified`，未触发回滚。

## 5. 237 发布后验证

| 验证项 | 结果 |
| --- | --- |
| 服务 | `gaiop-admin.service`、`gaiop-personal-wechat.service`、`openclaw-gateway.service`、`caddy.service` 均为 active |
| 内部监听 | Admin `127.0.0.1:3000`、适配器 `127.0.0.1:19091`，均为回环监听 |
| Admin 健康 | `/api/health` 成功，Gateway 为 connected |
| 适配器健康 | 内部鉴权状态成功、插件可用、版本 `2.4.4`；未输出账号标识或凭据 |
| 公网入口 | HTTPS 200；未修改公网路由或证书 |
| 未认证保护 | Admin 个人微信接口 401；适配器状态接口 401 |
| 远端代码 | 适配器、个人微信路由、报告路由、`dist/index.html` 和本批次 `ChannelsPage` 资产哈希均与本地构建一致 |
| 日志 | 发布后最近窗口未发现模块缺失、端口占用、SQLite 打开失败或未处理异常 |

本轮不替用户创建、删除或重新扫码真实微信账号。管理员登录后应重点验收：个人微信折叠交互、渠道总开关、账号启停、删除确认、添加账号二维码、扫码完成状态，以及审计信息中的对应写操作。

## 6. 回滚

- 仅回滚 Admin：使用批次 `20260807T080129Z` 的受控 Admin 回滚流程，恢复 Admin 快照和迁移前 SQLite 备份后重新验证回环健康。
- 同时回滚适配器和相关配置：在维护窗口停止 Admin、个人微信适配器和 Gateway，校验全量备份 `SHA256SUMS` 后按备份目录内 `RESTORE.md` 恢复，再启动并验证 Admin、Gateway、个人微信及其他频道。
- 回滚不需要也不得修改 Caddy、公网端口、DNS、证书或防火墙。
