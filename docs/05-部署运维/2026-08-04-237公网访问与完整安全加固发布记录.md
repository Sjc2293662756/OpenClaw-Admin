# 2026-08-04 237 公网访问与完整安全加固发布记录

## 1. 发布结论

| 项目 | 结果 |
| --- | --- |
| 公网地址 | `https://101.254.114.237/` |
| 发布批次 | `20260804T153830Z` |
| Admin 代码提交 | `1d0ad30 feat(security): harden public Admin access` |
| Caddy | `active / enabled`，80 跳转 443，443 反向代理 `127.0.0.1:3000` |
| GAIOP Admin | `active / enabled`，只监听 `127.0.0.1:3000` |
| OpenClaw Gateway | `active / enabled`，由 `0.0.0.0:18789` 收口到回环地址 |
| 升级服务 | `active / enabled`，只监听 `127.0.0.1:18900` |
| Fail2ban | `active / enabled`，`sshd` jail 正常 |
| 公网端口 | 22、80、443 可达；3000、18789、18900、19090 不可达 |
| 内部证书 | 继续使用 Caddy internal CA；浏览器不受信任警告属于当前预期 |
| 防火墙 | 沿用已生效的 UFW 默认入站拒绝和既有规则，没有重构 iptables/nftables |
| 数据与秘密 | 未修改用户密码、Token、频道凭据、数据库、报告、告警或历史数据；未读取或输出秘密内容 |

本次是在最小 Caddy 公网入口已经生效的基础上，继续完成原方案中的必要安全加固。没有修改 GAIOP 后端、GAIOP-upgrade 源码、OpenClaw 核心或频道插件版本，也没有操作 238。

## 2. 修改前状态

2026-08-04 15:31 UTC 左右的发布前只读核查结论：

- Caddy 已监听 80/443，公网根站点已代理 Admin，未继续代理 18789 或 18900；
- Admin `active`，只监听 `127.0.0.1:3000`，但 systemd unit 尚未 enabled；
- OpenClaw `active / enabled`，`gateway.bind=lan`，实际监听 `0.0.0.0:18789`；
- 升级服务和告警 HTTP 接收器分别只监听 `127.0.0.1:18900`、`127.0.0.1:19090`；
- UFW 已经是 active，默认 `deny (incoming)`；22、80、443 保持允许，18789 有公网拒绝和回环允许，UDP 514 只允许既有 238 来源；
- nftables 输入链实际执行丢弃策略，18789 拒绝规则已有命中计数；
- Fail2ban 已安装但 inactive/disabled，没有 GAIOP sshd jail；
- SSH 的 root 密码登录已禁用，普通密码登录和 `AllowTcpForwarding` 仍保留；
- `/opt/gaiop/admin/dist` 权限异常为 777；
- 根文件系统使用率已经从早前记录的 88%～91% 降到约 57%～58%，本次未执行容量清理。

云控制台没有提供给本次受控连接，因此无法读取云安全组对象本身。发布条件由“主机侧 UFW/nftables 确认 + 修改前外部 TCP 探测”共同判定；3000、18789、18900、19090 修改前均不可从公网建立连接，未触发停止发布条件。

## 3. 备份和发布物

### 3.1 本次总备份

本次变更前创建：

```text
/var/backups/gaiop/security-hardening-20260804T153830Z
```

只读确认权限为 `root:root:700`，其下目录为 700、文件为 600。备份包含以下类型，但未读取或输出敏感文件内容：

- Caddy 活动配置；
- Admin 受限环境文件和 systemd unit；
- OpenClaw 配置；
- Fail2ban 原始状态和可能存在的 jail；
- Caddy、Admin、OpenClaw、Fail2ban 的 active/enabled 状态；
- iptables、ip6tables、nftables、UFW 快照；
- Admin `dist` 修改前权限清单；
- 基线哈希。

Admin 受控发布器另建：

```text
/var/backups/gaiop/admin-prestage-20260804T153830Z
```

其中保留发布前 Admin 目录、unit、服务器环境副本以及 SQLite 在线备份。数据库备份只供紧急、同批次、明确评估数据时效后的恢复；普通代码回滚不得直接覆盖当前 `wizard.db`。

### 3.2 早前候选备份

早前“过度设计”阶段的服务器备份、9 组 Caddy 尝试备份和本地候选归档仍保留，完整盘点见[237 公网安全加固候选备份与后续完善清单](2026-08-04-237公网安全加固候选备份与后续完善清单.md)。这些旧备份是历史恢复点，不应机械覆盖当前配置。

### 3.3 本地发布归档

```text
D:\杨硕文件\GAIOP\GAIOP\gaiop-security-hardening-20260804T153830Z.tgz
```

- 大小：3,601,555 字节；
- SHA-256：`3fdeca3d2e56e05e91f198cd1c3adfc3f759ad55de226115401dd60e1d5d8c1d`；
- 只包含 `dist`、`server`、`package.json`、`package-lock.json`，不包含 `.env`、数据库或秘密。

237 活动文件与本地发布物校验一致：

- `server/index.js`：`7d478ddc389057dd8cbc73ec5da0897b7d128f7f8f2f8967a787518f44d22f95`；
- `dist/index.html`：`04e1a577b48ec275a7fb8e51cf9b0f3a3c65f90ea13c9aa557b118e6982bd369`。

## 4. 实施内容

### 4.1 GAIOP Admin 应用安全

提交 `1d0ad30` 完成：

1. 生产环境 CORS 改为精确来源白名单，只允许 `https://101.254.114.237`；开发环境行为保持兼容；
2. Express 只信任回环地址上的同机代理；外部客户端直连时不能用伪造的 `X-Forwarded-For` 覆盖源地址；
3. 审计来源仅在请求确实来自可信回环代理时使用转发地址；
4. 登录接口增加按来源的服务端限速：10 分钟内 30 次、最多跟踪 5,000 个来源；触发时返回 429、`Retry-After`，且同一限速窗口只写一次安全审计；
5. 生产服务器环境新增 `NODE_ENV=production` 与 `GAIOP_ALLOWED_ORIGINS=https://101.254.114.237`，其他环境项未读取、未显示、未更换；
6. 没有改变现有四角色、首次改密、密码规则、用户临时锁定、REST/RPC 权限、Bearer SSE 或 410 遗留接口边界。

代码验证：

- `npm run test:node`：91/91 通过；
- `npm run test:vitest`：66/66 通过；
- `npm run build`：类型检查和生产构建通过；
- SSE 客户端测试覆盖 Authorization Bearer、心跳解析、传输失败后的重连；
- 未用237真实用户密码做自动化登录，也未通过生产连续失败请求制造审计污染或自锁。

依赖安装时 `npm audit` 报告 30 项既有依赖风险（2 low、3 moderate、20 high、5 critical）。本次没有执行可能引入破坏性升级的 `npm audit fix`；该项列入独立依赖治理，不影响本批安全边界已经生效的结论。

### 4.2 Caddy

最终路由：

```text
公网 HTTP 80 -> 301 HTTPS
公网 HTTPS 443 -> Caddy internal TLS -> 127.0.0.1:3000
```

配置变化：

- 保留 internal CA 和 IP 站点，不申请公共证书、不配置域名/DNS；
- 未恢复 Caddy Basic Auth，由 GAIOP 自身认证接管；
- 不代理 18789、18900、19090；
- `flush_interval -1`，保留 SSE 实时转发；
- Authorization 由反向代理正常透传；
- Caddy覆盖而不是追加客户端 `X-Forwarded-For`，并转发协议；
- 增加 `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer` 和保守的 `Permissions-Policy`；
- 因内部证书仍可由用户手工绕过，明确不启用 HSTS；
- 访问日志写入 `/var/log/caddy/gaiop-access.log`，权限 `caddy:caddy:640`；单文件 25 MiB、保留 5 份、最长 168 小时；过滤完整 URI、Authorization 和 Cookie，避免敏感查询参数或认证信息进入访问日志；
- 候选配置先通过 `caddy validate`，再 reload；根页与 welcome 页验证通过。

当前 Caddyfile SHA-256：`b17b20467c2186a3b07df74a7b689374a007b14c4d4af73af7c9630e7cc22f83`。

### 4.3 OpenClaw

- 使用现有配置命令把 `gateway.bind` 从 `lan` 改为 `loopback`；
- Gateway user service 重启后为 `active / enabled`；
- 18789 只在回环地址监听，Admin 健康接口显示 Gateway connected；
- 没有升级 OpenClaw 核心或频道插件，没有更换 Token、密码或频道凭据；
- 配置文件仍由 `netinside` 持有并使用 600 权限；当前配置哈希仅记录为 `285a040b8bf681a91c6a8f96ff0b3023e561d2541f4edccd06444f0219517c88`，未读取内容。

频道只读探针：

- 飞书：configured/running/probe ok/no error；
- 企业微信：configured/running/probe ok/no error；
- 钉钉：configured/running/no error，CLI deep probe 仍为 false。该差异与既有237记录一致，不能单凭 deep probe 判为频道故障；本次未用真实频道发送消息，最近一次真实消息验收仍以既有记录为准。

### 4.4 防火墙、SSH 和 Fail2ban

- UFW 实际已 active 且默认拒绝入站，因此没有执行 `ufw enable`，也没有重构 iptables/nftables；
- 继续保留 22、80、443 的既有可达状态；3000、18789、18900、19090 未新增公网规则；
- UDP 514 和 238 来源规则未修改；UDP 5353 未新增公网入口；
- root 密码登录仍为禁止，SSH 端口、普通密码登录、密钥、`AllowTcpForwarding` 均未修改；
- 受控发布连接持续以现有密码 SSH 成功执行，说明密码运维路径未被破坏；本机默认 `id_ed25519` 的 BatchMode 认证未成功，因此没有把“密钥登录已验证”写成通过，也没有读取私钥内容；
- 新增 `/etc/fail2ban/jail.d/gaiop-sshd.local`，只启用 sshd：`maxretry=8`、`findtime=10m`、`bantime=15m`，忽略回环和既有管理来源；
- `fail2ban-client -t` 通过，Fail2ban 与 sshd jail 均为 active，服务为 enabled。

### 4.5 服务和权限

- `gaiop-admin.service` 已设置 enabled；
- `/opt/gaiop/admin/dist` 收紧为目录 755、普通文件 644，属主仍为 `gaiop:gaiop`；
- `/opt/gaiop/admin` 本身仍为 750，受控发布器后续仍可工作；
- 没有调整 OpenClaw 扩展目录、Skills 根目录或操作系统全局权限；
- 根文件系统发布后使用率 58%，未删除任何历史数据、报告、备份或日志。

## 5. 失败与回滚过程

本次按批次实施，没有把多类变更绑定成单一不可分事务：

1. Admin stage、生产环境切换和启动验证一次成功；
2. Caddy validate、安装、reload 和页面检查一次成功；
3. OpenClaw 第一次因登录 shell 找不到 CLI（127）自动恢复配置并重启；第二次已经完成回环监听，但验证脚本误读健康字段，仍按失败自动恢复；改用运行进程的明确 CLI 入口并按实际 `gateway=connected` 判断后完成；
4. 主机批次第一次因 Fail2ban 启动状态竞态退出，随后显式恢复原 jail、服务启停、Admin enabled 和 dist 权限；第二次执行到防火墙核查时因 UFW 输出为中文而被英文判断误报，错误陷阱自动恢复；改为与语言无关的默认拒绝判断后完成；
5. 每次回滚后均先确认服务和原监听恢复，再继续下一次尝试。最终没有遗留候选 Caddyfile、staging 目录或半完成监听状态。

## 6. 发布后验证

### 6.1 服务器内部

| 验证项 | 结果 |
| --- | --- |
| Caddy | active / enabled，配置校验通过 |
| Admin | active / enabled，`127.0.0.1:3000`，health 200 |
| OpenClaw | active / enabled，回环 18789，Gateway connected |
| Upgrade | active，内部健康检查 200；未认证业务请求 401 |
| 告警接收器 | 保持回环 19090，未改配置 |
| SSE | 未认证 `/api/events` 为 401；Bearer、心跳和重连由测试覆盖；Caddy禁用缓冲 |
| CORS | 正式来源 200，`https://attacker.invalid` 为 403 |
| Fail2ban | active / enabled，sshd jail active |
| dist 权限 | 目录755、文件644 |
| 磁盘 | 根分区 58% |

升级只读核查还确认：Gateway root/health 200，Admin 到升级服务的受控地址为 `127.0.0.1:18900`，升级 status/tasks/backups 的既有 BFF 调用最近均为 200；未认证直连保持401。

### 6.2 公网探针

| 项目 | 结果 |
| --- | --- |
| `http://101.254.114.237/` | 301 到 HTTPS |
| `https://101.254.114.237/` | 200 |
| `/welcome?redirect=/` | 200 |
| `/assets/index-D9eR0wQm.js` | 200 |
| `/api/health` | 200 |
| 未认证 `/api/events` | 401 |
| 恶意 Origin | 403 |
| Caddy Basic Auth challenge | 不存在 |
| 安全响应头 | nosniff、frame deny 生效；HSTS不存在 |
| TCP 22、80、443 | 可达 |
| TCP 3000、18789、18900、19090 | 全部不可达 |

浏览器仍会因为 internal CA 显示证书不受信任，用户需要手工继续；这是当前方案的明确已知限制，不代表 HTTPS 链路未加密。

## 7. 未自动完成的人工验收和后续项

本批技术发布已经完成，仍有以下不能在不使用真实凭据或扩大范围的情况下自动闭环的项目：

1. 由用户在浏览器手工完成真实账户登录、首次改密、对话、会话、报告、告警、审计和升级页面逐页验收；本次遵守要求，没有读取或使用真实用户密码；
2. 由用户按需完成三频道真实消息冒烟；自动探针已确认飞书/企微正常，钉钉 configured/running/no error，但其已知 CLI deep probe 差异仍在；
3. 云安全组对象的只读截图或规则导出仍缺少云侧控制面权限；当前只能确认公网最终效果和主机侧控制均满足端口目标；
4. 本机默认 SSH 密钥认证没有成功，密码SSH正常；如交付要求密钥登录，需单独核对服务器 authorized key 与正确客户端公钥，不能读取或传输私钥；
5. 30 项 npm 依赖风险需单独做兼容评估、升级和回归，不能用本次发布直接执行破坏性自动修复；
6. 公共受信任证书、域名和 DNS 仍不在当前范围；
7. 日志容量和磁盘长期治理继续作为日常运维事项，本次只配置了 Caddy日志上限。

原候选清单中的 13 项已不再是“全部未实施”：应用 CORS、可信代理、登录限速、Caddy日志、OpenClaw回环、Fail2ban、Admin enabled、dist权限及联合验证均已完成；剩余内容以上述7项为准。

## 8. 回滚

回滚前先保持现有 SSH 会话，并建立第二条管理连接确认22仍可用。以下命令只在237执行；任何配置文件都不得打印到终端。

### 8.1 Caddy

```bash
sudo install -o root -g caddy -m 0640 \
  /var/backups/gaiop/security-hardening-20260804T153830Z/Caddyfile \
  /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile
sudo systemctl reload caddy.service
curl -kfsS https://127.0.0.1/ -H 'Host: 101.254.114.237' >/dev/null
```

### 8.2 OpenClaw

```bash
sudo install -o netinside -g netinside -m 0600 \
  /var/backups/gaiop/security-hardening-20260804T153830Z/openclaw.json \
  /home/netinside/.openclaw/openclaw.json
sudo -u netinside XDG_RUNTIME_DIR=/run/user/1000 \
  systemctl --user restart openclaw-gateway.service
```

### 8.3 Admin 环境与代码

先恢复环境文件：

```bash
sudo install -o root -g gaiop -m 0640 \
  /var/backups/gaiop/security-hardening-20260804T153830Z/admin.env \
  /etc/gaiop/admin.env
```

普通代码回滚必须保留当前数据库，推荐把当前发布移走而不是删除，再复制发布前目录：

```bash
sudo systemctl stop gaiop-admin.service
sudo mv /opt/gaiop/admin /opt/gaiop/admin.failed-20260804T153830Z
sudo cp -a \
  /var/backups/gaiop/admin-prestage-20260804T153830Z/preexisting-admin \
  /opt/gaiop/admin
sudo systemctl start gaiop-admin.service
curl -fsS http://127.0.0.1:3000/api/health >/dev/null
```

本地包装器 `Invoke-237AdminRollback.ps1 -ReleaseId 20260804T153830Z` 会同时恢复发布前 SQLite 备份。只有确认允许覆盖该批次之后新增的数据库写入时才能使用；常规代码回滚不得直接调用它。

### 8.4 Fail2ban、Admin enabled 和 dist 权限

发布前 Fail2ban 为 inactive/disabled且没有本 jail。如需完整恢复原状态：

```bash
sudo rm -f -- /etc/fail2ban/jail.d/gaiop-sshd.local
sudo systemctl disable --now fail2ban.service
sudo systemctl disable gaiop-admin.service
sudo bash -c 'while IFS= read -r -d "" item; do mode=${item%% *}; path=${item#* }; [ ! -e "$path" ] || chmod "$mode" "$path"; done < /var/backups/gaiop/security-hardening-20260804T153830Z/state/admin-dist-modes.nul'
```

UFW、iptables、nftables和UDP 514本次没有改动，因此不应为了本批回滚盲目导入规则快照。快照只用于人工差异核查；确需恢复时必须再次确认不会中断SSH。

## 9. Git 与保护边界

- 应用安全提交：`1d0ad30`；
- 发布工作基于当时远端 `dev-yangshuo@7a8f458` 的干净独立 worktree；
- 主工作副本中的 `wizard.db`、全部 `.bak`、`docs/images` 删除记录、`.codex-temp` 和用户现有代码改动均未触碰；
- 文档提交在本记录完成后精确暂存本文件和候选清单状态说明，并推送 `origin/dev-yangshuo`；
- GAIOP 后端、GAIOP-upgrade 和238保持只读/未操作。
