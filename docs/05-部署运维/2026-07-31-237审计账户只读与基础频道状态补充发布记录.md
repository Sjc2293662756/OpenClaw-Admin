# 237 审计账户只读与基础频道状态补充发布记录

> 发布日期：2026-07-31（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260731T062505Z`
> 功能提交：`dev-yangshuo@ec7059f`

## 1. 补充范围

- 审计用户可进入账户管理，查看账户 ID、用户名、角色、描述、状态、初始管理员标记和创建/更新时间。
- 审计页面沿用账户管理原有表格样式，显示只读说明，不展示新增、编辑、重置密码、启停、删除及操作列；仍可按正常流程修改本人密码。
- `GET /api/users` 只允许审计和管理员访问。审计响应使用安全字段投影，不返回密码哈希或 `must_change_password`；基础和标准用户直接请求返回403。
- 账户新增、编辑、重置密码、启停和删除继续使用管理员中间件；审计用户直接调用写接口均返回403。
- 基础用户现在可进入频道页，与标准和审计用户一致，仅显示“安全运行状态”及原组件安装/可用、配置状态标签，不获得频道配置内容。
- 频道配置、凭据、启停、保存和应用能力仍只允许管理员使用。

本次仅修改 GAIOP-Admin 前端、Admin BFF、自动化测试和文档；未修改 GAIOP 后端、OpenClaw 核心、频道插件、生产用户或生产业务数据。

## 2. 自动化与构建

- Node：68 项通过。
- Vitest：14 个文件、50 项通过。
- `vue-tsc -b && vite build`：通过。
- `git diff --check`：通过。
- 新增账户权限回归覆盖：审计读取安全投影、基础读取拒绝、审计新增/编辑/重置/删除拒绝、用户总数不变。
- 新增页面回归覆盖：审计用户看到只读账户列表和说明，不出现账户管理按钮或操作列。
- 频道路由回归覆盖基础、标准用户均只获得安全投影。
- 测试使用内存或临时数据，不读取或修改237真实用户。

## 3. 发布包与237验证

- 条目数：295。
- 大小：3567540 B。
- SHA-256：`f7ab3bf172fa068b0caa6991c96fd1756330ab6931af113010998722720327a9`。
- 入口资产：`assets/index-CT0giqUd.js`；账户页资产：`assets/UserManagementPage-BJvvPHnU.js`。
- 发布包只包含 `dist/`、`server/`、`package.json` 和 `package-lock.json`；不含 `.env`、`data/`、`.bak`、本地数据库、图片删除记录或临时目录。
- 首次部署前 SSH 预检连接失败，脚本明确未执行任何变更；同范围重试后预检成功，再进入受控发布。
- 受控暂存脚本建立发布前 Admin 备份和数据库备份；暂存阶段未启动服务，未修改 Caddy、公网、网络或防火墙。
- 启动验证完成，未触发回滚；服务状态 active，进程退出码0，重启次数0。
- 健康检查 HTTP 200，Gateway connected；监听保持 `127.0.0.1:3000`，仅回环 IPv4。
- 发布后只读预检：暂存目录0，运行目录和固定回环绑定均正常；Caddy继续占用443且状态未变化。
- 生产只读探针：会话列表79条，增强后78条，默认运行会话未暴露，外部来源错配0；`sessions.usage` 和抽样历史读取成功。

## 4. 数据与安全边界

- 未使用生产真实用户执行角色写操作；权限行为通过隔离测试数据验证。
- 审计账户投影不包含密码、密码哈希、首次改密状态或其他认证材料。
- 频道安全状态响应不包含频道凭据、原始配置、地址或路径。
- 未读取、输出、打包或提交密码、Token、私钥、频道密钥、NAPM 凭据、`.env` 或本机安全存储内容。
- 本地 `data/wizard.db`、所有 `.bak`、`docs/images` 删除记录、`.codex-temp` 和既有用户改动均未暂存或提交。

## 5. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T062505Z'
```

回滚仅恢复本批次发布前的 Admin 代码、静态资源、配置、服务单元和受控数据库备份，不操作 GAIOP 后端、OpenClaw 核心、频道插件或其他服务器。
