# 237 基础用户工作台权限体验补充修复发布记录

> 发布日期：2026-08-04（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260804T130258Z`
> 运行代码提交：`276dbdb fix(auth): keep basic users in workspace`

## 1. 修复内容

- 工作台“管理控制台”无权限弹窗改为紧凑居中卡片：标题“无访问权限”置于锁图标上方，标题、说明和“我知道了”按钮使用同一中心轴，标题与图标间距收紧，卡片最大宽度调整为 420px；
- 基础用户和标准用户在“修改密码”页点击“返回”时，直接返回对话工作台，不再先跳转账户管理并落入管理控制台布局；
- 审计用户和管理员从修改密码页仍返回账户管理，符合既有页面矩阵；
- 对话、会话归属、密码提交、管理入口提示、直接 URL 的整页拒绝，以及基础用户 REST/RPC 服务端白名单均未改变。

返回目标复用 `resolvePasswordChangeReturn` 的集中式角色矩阵判定，避免界面按钮绕过工作台入口体验；角色矩阵测试覆盖基础、标准、审计和管理员四种返回结果。

## 2. 本地验证

| 验证 | 结果 |
|---|---|
| Node 服务端测试 | 85/85 通过 |
| Vitest | 16 个文件、66/66 通过 |
| `vue-tsc -b && vite build` | 通过（4,632 个模块） |
| `git diff --check` | 通过 |

自动化使用模拟认证与本地测试数据；未读取、创建或修改真实用户、密码、会话或业务数据。

## 3. 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-workspace-ui-20260804T130258Z.tgz` |
| 归档条目 | 305 |
| 归档大小 | 3,597,789 B |
| SHA-256 | `2aca959e89898807c8ba81623dffcc0fadb1a55c042fcda949fd4d48eafd683c` |

归档仅包含 `dist/`、`server/`、`package.json` 和 `package-lock.json`；不包含环境文件、数据库、备份、删除记录或凭据。237 暂存已创建受控程序与数据库备份，未修改 Caddy、网络、公网路由、DNS、防火墙、证书、OpenClaw 核心或频道插件。

发布后验证：`gaiop-admin.service` 为 `active`；健康检查成功，Gateway 受控探针为 `connected`；仅监听 `127.0.0.1:3000`，环境文件模式与源码绑定保护均存在；未触发回滚。

## 4. 回滚边界

如需回退本批次的 Admin 程序、静态资源、服务单元和受控备份，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260804T130258Z'
```

该回滚不修改后端、OpenClaw 核心、频道插件、Caddy 或公网配置。恢复受控数据库备份覆盖生产数据仍属于高风险操作，必须另行确认。
