# 2026-08-04 237 自用公网 Caddy 反向代理发布记录

## 1. 发布范围

- 发布批次：`20260804T090407Z`
- 公网入口：`https://101.254.114.237/`
- 本次仅调整 237 的 Caddy 活动配置，没有修改 GAIOP 前后端、OpenClaw 核心或频道插件、升级服务、数据库、用户密码、SSH、UDP 514 Syslog 规则及 238。
- 继续使用 Caddy 内部 CA 签发的 HTTPS 证书，不申请公网证书，不配置域名或 DNS。

## 2. 路由变化

| 项目 | 修改前 | 修改后 |
| --- | --- | --- |
| HTTP 80 | 跳转到 HTTPS | 保持跳转到 `https://101.254.114.237{uri}` |
| HTTPS 根入口 | Caddy Basic Auth 位于 GAIOP 入口前；活动配置仍包含 OpenClaw 18789 和升级服务 18900 的代理路由 | 直接反向代理 `127.0.0.1:3000`，由 GAIOP 自身登录页负责认证 |
| OpenClaw | Caddy 存在 18789 上游路由 | Caddy 不再代理 18789；GAIOP Admin 仍在服务器内部连接 OpenClaw |
| 系统升级 | Caddy 存在 18900 上游路由 | Caddy 不再代理 18900；GAIOP Admin BFF 仍在服务器内部访问升级服务 |

最终配置保留 `flush_interval -1`，避免 SSE 响应缓冲；HTTP transport 使用 5 秒连接超时、30 秒响应头超时和 30 秒 keepalive。Caddy `reverse_proxy` 未覆盖请求的 `Authorization` Header，按默认规则原样转发。配置同时为 IP 访问设置默认 SNI，兼容不对 IP 地址发送 SNI 的客户端。

## 3. 备份、校验与发布

- 修改前 Caddyfile：`/var/backups/gaiop/caddy-public-20260804T090407Z/Caddyfile`
- 备份目录权限：`root:root:700`
- 备份文件权限：`root:root:600`
- 候选配置校验：`valid`
- 安装后配置校验：`valid`
- `systemctl reload caddy`：成功
- Caddy 最终状态：`active / enabled`
- 自动回滚：未触发

备份中包含修改前认证配置，因此不得读取、打印、复制到仓库或降低其访问权限。

范围收敛前一次未完成的加固事务已自动恢复配置；其曾安装的 Fail2ban 软件包未继续做操作系统级卸载，但服务已恢复为 `inactive / disabled`，任务创建的 jail 配置已移除，不参与当前入站控制。

## 4. 发布后验证

### 4.1 公网与应用链路

| 验证项 | 结果 |
| --- | --- |
| `http://101.254.114.237/` | `301` 跳转到 HTTPS |
| `https://101.254.114.237/` | `200`，返回 GAIOP 页面 |
| `/welcome?redirect=/` | `200` |
| Caddy Basic Auth | 已移除；响应中没有 Basic Auth 挑战头 |
| 页面静态资源 | 入口引用的 3 个 JS/CSS 资源均为 `200` |
| `/api/health` | `200` |
| `/api/auth/config` | `200` |
| `/api/events` 未登录访问 | `401`，认证边界正常 |
| GAIOP Admin | `active`，仅监听 `127.0.0.1:3000` |
| Admin 到 OpenClaw | 健康接口报告 `gateway=connected` |
| OpenClaw Gateway | `active / enabled`；现有 `gateway.bind=lan` 未修改 |
| OpenClaw 频道状态命令 | 通过 |
| 升级服务 | `active / enabled`；`GET /health` 为 `200`，未授权状态接口为 `401` |

未读取生产登录凭据，也未修改用户。因此本次自动验收覆盖欢迎/登录入口、静态资源、公开 API、未授权边界、Admin 到 OpenClaw、频道状态命令及升级服务内部链路；登录后的对话、会话、报告、告警、审计、系统升级页面和已认证 SSE 需由现有用户通过公网入口登录后继续验收。

### 4.2 主机侧与公网入站复测

本次没有启用 UFW，也没有重构 iptables/nftables。只读核查确认：

- UFW 在任务开始前已经处于 active，当前仍为 active，默认入站策略为 deny；
- iptables INPUT policy 为 DROP，并继续进入既有 UFW 链；
- nftables 实际规则允许 22、80、443，18789 命中既有 drop；
- 3000、18900、19090 仅监听 `127.0.0.1`；18789 维持原有 OpenClaw `0.0.0.0` 监听，但既有 UFW 规则阻止公网入站，并保留本机 127.0.0.1 访问；
- 没有改动 UDP 514 的既有 `101.254.114.238` 来源限制。

| TCP 端口 | 公网复测 | 说明 |
| --- | --- | --- |
| 22 | 可达 | 现有 SSH 状态保持 |
| 80 | 可达 | Caddy HTTP 跳转 |
| 443 | 可达 | Caddy HTTPS |
| 3000 | 不可达 | GAIOP Admin 内部端口 |
| 18789 | 不可达 | OpenClaw 内部端口 |
| 18900 | 不可达 | 升级服务内部端口 |
| 19090 | 不可达 | 告警接收内部端口 |

当前执行环境没有云平台安全组的只读控制面入口，因此没有声称直接读取云侧规则明细；以上公网探针从 237 外部发起，确认了云侧与主机侧叠加后的最终入站效果。

## 5. 内部证书说明

当前证书由 Caddy 内部 CA 签发。公网浏览器首次访问时提示证书不受信任属于预期，确认目标 IP 为 `101.254.114.237` 后可手动继续。本次没有申请新证书，也没有修改 DNS、域名或系统全局信任库。

## 6. 回滚

回滚只恢复发布前 Caddyfile，随后先校验再 reload：

```bash
sudo install -o root -g root -m 0644 \
  /var/backups/gaiop/caddy-public-20260804T090407Z/Caddyfile \
  /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl reload caddy
sudo systemctl is-active caddy
```

回滚后应重新复测 80/443、Caddy Basic Auth、原有路由以及公网内部端口不可达状态。上述命令不会输出备份中的认证哈希。
