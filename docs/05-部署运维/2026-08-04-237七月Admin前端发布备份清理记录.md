# 237 七月 Admin 前端发布备份清理记录

> 执行日期：2026-08-04（UTC+8）
> 执行性质：用户明确授权的手工容量清理

## 背景

237 根文件系统达到约 95% 使用率，影响后续发布与运行安全余量。容量治理规划仍处于待团队决策状态；本次不启用自动留存策略，仅处理用户指定的七月旧 Admin 前端发布切换备份。

## 清理范围与保护条件

- 仅匹配 `/var/backups/gaiop` 一级目录中的 `admin-prestage-202607*`；该目录是 Admin 受控发布在切换前创建的程序与受控回滚快照。
- 清理前确认目标共 43 个，时间范围为 2026-07-20 至 2026-07-31。
- 清理前确认八月 `admin-prestage-202608*` 回滚点有 22 个，满足容量治理建议中“至少保留最近 5 个可用版本”的最低保护要求。
- 未匹配或删除系统升级备份、八月 Admin 备份、`wizard.db`、报告、会话、审计、日志、运行配置、发布包或其它目录。
- 本次未重启、重载或修改任何服务与运行配置。

## 结果

| 项目 | 清理前 | 清理后 |
|---|---:|---:|
| 七月 Admin 前端切换备份 | 43 个 | 0 个 |
| 八月 Admin 回滚点 | 22 个 | 22 个 |
| 根文件系统使用率 | 95% | 51% |
| 根文件系统可用空间 | 约 5.3 GiB | 约 46 GiB |

本次约释放 40 GiB 空间。清理后的文件系统为 97 GiB，总已用约 47 GiB、可用约 46 GiB。

## 验证与恢复边界

- 237 只读复核确认 `admin-prestage-202607*` 剩余数为 0，`admin-prestage-202608*` 保留数为 22。
- 删除的七月备份不再可用，不能作为回滚来源；当前八月回滚点未受影响。
- 后续发布继续按受控流程创建新的切换前备份。若需再次清理，先核对实时容量、可用回滚点数量和精确路径，不将本次手工范围视为自动清理策略。
