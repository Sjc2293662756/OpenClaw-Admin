# 237 审计信息筛选与展示优化发布记录

> 发布日期：2026-08-03（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260803T080837Z`
> 运行代码提交：`552b16c feat(audit): refine filters and top queries`

## 1. 范围与边界

本批次在既有审计底座和第二阶段页面基础上，只优化 GAIOP-Admin 审计查询与展示。未执行 SQLite 迁移，未修改审计写入、权限拒绝、401/410 限频、遗留接口410、四角色权限或其他模块。

- `GET /api/audit-logs` 增加服务器执行的 `maxResults`：缺失或非法时为 TOP 200，最大3000且不小于 `pageSize`。真实 `pagination.total` 和五项汇总不被截断，`browseTotal = min(total, maxResults)` 限制可翻页范围。
- 页面默认查询今日；支持时间、关键词、系统用户单选、角色、分类、结果、来源、错误码筛选，及 TOP 50/100/200/500/1000/自定义和每页10/20/50/100。重置恢复今日和业务条件，并保留展示设置。
- 用户单选复用既有安全的 `/api/users` 只读清单，并提供 `system（系统事件）` 虚拟选项；未新增用户清单接口。
- 表格对空说明、对象、来源和错误码紧凑显示“—”，详情抽屉仍明确显示“历史未记录”。汇总卡改为“历史未结构化”，并提示早期空结果字段不代表操作失败。
- 未修改 GAIOP 后端、OpenClaw、Gateway、Skills、频道插件、升级模块、Caddy、防火墙、DNS、证书或公网配置。

## 2. 测试与构建

所有自动化验证使用隔离内存 SQLite、隔离角色和模拟 API，不使用237真实用户执行写操作。

| 验证 | 结果 |
|---|---|
| 服务端 TOP 边界、分页上限、真实总数与完整五项汇总 | 通过 |
| 默认今日实际 `from/to`、重置、用户单选/system、每页条数、TOP 提示和历史空字段 | 通过 |
| 查询代次、详情抽屉与请求编号复制 | 通过 |
| Node 测试 | 76/76 通过 |
| Vitest | 16 个文件、60/60 通过 |
| `vue-tsc -b`、`npm run build`、`git diff --check` | 通过 |

## 3. 237 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-audit-filter-display-20260803T080837Z.tgz` |
| 条目数 | 303 |
| 大小 | 3,580,147 B |
| SHA-256 | `604f47cdd95ab1f0c648f3f1701b27503c9c8820450115f90bb325e257e88369` |
| 内容 | `dist/`、`server/`、`package.json`、`package-lock.json` |

受控暂存器在切换前创建 Admin 程序快照，并对生产 `wizard.db` 执行完整性检查和受控备份；本记录未读取或输出数据库内容。最终批次启动校验成功，`gaiop-admin.service` 为 `active`、重启次数为0，仅监听 `127.0.0.1:3000`，健康接口为 HTTP 200，Gateway 状态为 `connected`。实际 Node 进程工作目录和服务单元工作目录均为 `/opt/gaiop/admin`。

HTTP 实际响应与本地构建 SHA-256 一致：

| 资源 | SHA-256 |
|---|---|
| `index.html` | `7e9c0d9824a0c009fc0c2eff1cbd516f025c3d40a5652fdb1775c46f1f86a6ff` |
| `assets/index-CkUolNpP.js` | `7b25eb87ff3e674a9f4a9f6c3e5eb7a5523c45fbd380f05654b2937bfa240782` |
| `assets/AuditLogsPage-BUHnhfUL.js` | `1a7b50a06df41e2f589c7c82991d4934b36b912e6cb66c5da4376c3e65b82b14` |
| `assets/AuditLogsPage-B8Bwp82r.css` | `7470698d89521142095895591bbe9d91ee52bf9c57410dcea8c5b9d2c836330f` |

HTTP `/` 返回200，实际入口引用当前主包；主包实际引用本批次 `AuditLogsPage` JavaScript 与 CSS 资源，三个资源也均返回200。未变更 Caddy 或公网路由。

本次曾在两个早期暂存批次的串行核验中回滚；最终批次通过后未触发回滚。若后续需要恢复本批次前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260803T080837Z'
```

该回滚点恢复本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份，不操作 GAIOP 后端、OpenClaw、频道插件或其他服务器。

## 4. 后续边界

审计留存和自动清理、导出、WORM、防篡改签名、SIEM、动态审计规则和策略页面仍未实施。
