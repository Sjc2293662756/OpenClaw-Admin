# 2026-08-06 个人微信频道：备份、回滚与运维记录

> 规划见《2026-08-06-个人微信频道接入-规划与方案.md》，实施见《2026-08-06-个人微信频道接入-实施与部署记录.md》，报告适配见《2026-08-06-个人微信报告接入GAIOP报告管理器-适配记录.md》。

## 1. 备份清单

| 类型 | 位置 | 时间 | 说明 |
|---|---|---|---|
| **实施前完整回滚基线** | `/var/backups/gaiop/deployments/personal-wechat-before-20260806T044945Z/filesystem.tar.gz` | 2026-08-06 UTC 04:49 | **恢复“开始改造之前”状态用这个**；SHA256 已验证 |
| 上线后稳定备份 | `/var/backups/gaiop/deployments/personal-wechat-after-20260806T070914Z/filesystem.tar.gz` | 2026-08-06 UTC 07:09 | 本次上线后完整状态（含适配器、归因工作器、报告目录） |
| 历史中间产物 | `personal-wechat-before-20260806T023649Z` / `T023948Z` | 当天早些时候 | 前期核查尝试，不作为回滚点 |
| 已清理的失败尝试 | `...T043601Z`（INCOMPLETE，已删） | — | 脚本 bug 导致标记 INCOMPLETE，归档本身校验通过但已清理 |

## 2. 回滚方法（实施前基线）

1. 停止 `gaiop-admin.service` 与 netinside 用户服务 `openclaw-gateway.service`；
2. 校验：`cd /var/backups/gaiop/deployments/personal-wechat-before-20260806T044945Z && sha256sum -c SHA256SUMS`；
3. 从 `/` 解压 `filesystem.tar.gz`（tar --numeric-owner --acls --xattrs -xzf ... -C /）；
4. `systemctl daemon-reload`，启动 `openclaw-gateway.service` 与 `gaiop-admin.service`；
5. 验证 Gateway deep health、企微/飞书/钉钉、Admin `/api/health`、公网 HTTPS。

如需彻底清除微信痕迹（可选）：停用 `gaiop-personal-wechat.service` 并删除 `/home/netinside/gaiop-personal-wechat`、`~/.openclaw/extensions/openclaw-weixin` 软链、`/var/lib/gaiop/reports/_sidecar/c10c5d97d9b4ba8f59a4/` 与报告归因索引中 `openclaw-weixin` 条目。

## 3. Git 提交

- `77abfc8`：个人微信频道接入、适配器、报告归因（feat）；
- `f24de0d`：文档提交记录补充（docs）；
- 后续状态修复与文档拆分提交将追加（commit 号以最终 git log 为准）；
- 远端：`origin/dev-yangshuo`，推送后核对远端 HEAD。

## 4. 已知记录（不处理/留待后续）

- 微信平台对高频 bot 消息的投递侧限制迹象（服务端全部发送成功、客户端只显示部分）；插件单段流式回复有 1 秒/条节流；平台确切上限未证实；
- 模型在工具调用间隙的过程叙述会分成多条消息发出，无频道级配置可单独关闭；
- NAPM 报告生成偶发失败属后端问题，由他人修复；
- 报告管理器可见微信来源报告；历史企微 `channel_wecom_*` 目录为旧管线产物，新报告统一走 `_sidecar` 归因。

## 5. 运维操作

- 适配器部署：`deploy/237/Invoke-237PersonalWeChatAdapterDeploy.ps1`（会显式重启适配器服务）；
- 归因工作器部署：`scripts/gateway237-report-attribution-weixin-deploy.cjs`；
- 备份：`deploy/237/Invoke-237PersonalWeChatBaselineBackup.ps1 -BackupId personal-wechat-(before|after)-<UTC时间戳>Z`；
- 只读核查：`scripts/gateway237-personal-wechat-livecheck.cjs`；
- 性能复测：`scripts/gateway237-personal-wechat-perf.cjs`（删除/启停耗时）；
- 状态判断口径：适配器提供存在/配置，Gateway `channels.status` 提供运行状态；Gateway 状态不可用时显示“状态未知”，前端每 20 秒自动刷新。
