# 2026-09-03 237 WebChat 报告归档契约与插件入口漂移修复记录

## 结论

本次复核确认，报告卡片缺失不能只按“报告文件管理器中有文件”判断。237 同时存在两个独立风险：

1. Gateway 实际加载的 `index.mjs` 优先加载同目录遗留的编译版 `index.js`，与 workspace 中的 `remote.js` 版本可能不同。新契约代码因此可能未被执行。
2. Gateway workspace 的报告生成/存储服务曾处于 `legacy-only`：未稳定使用 `GAIOP_REPORTS_DIR=/var/lib/gaiop/reports` 和配对审计 JSON。页面出现“已生成”不等于正式归档完成，后续 Admin 登记和 WebChat 卡片关联会失去可靠来源。

这两个问题与此前 WebChat 前端“嵌套消息 ID 未取到”的问题不同；前端修复保留，后端本次只补入口和正式归档契约，未改变角色权限、下载接口或存储体系。

## 生产证据

- 目标会话：`agent:main:main:dm:webchat-e883c381016743bfb5996af9ed053394`。
- 237 报告目录检查：正式目录存在，报告文件 550、审计 JSON 279、配对报告 270；旧目录仍存在，运行时报告/审计契约检查为 `legacy-only`。
- 生产入口：`/home/netinside/.openclaw/extensions/napm-openclaw-plugin/index.mjs`；该入口原先先加载 `./index.js`，而实际 `index.js`、extension `remote.js` 与 workspace `remote.js` 哈希不一致。
- 最新目标报告的正式文件和审计均存在，来源会话与属主可读；报告登记和权限过滤不是本次根因。
- 受控集成首次在旧验收逻辑处回滚，证明发布器自身也只验证旧的顶层工具消息/第一条候选；修正后改为入口同步、候选逐一验证，并保留合成报告 E2E。

## 最小修复

- `napm-openclaw-plugin.index.mjs` 优先加载同目录的可部署 `napm-openclaw-plugin.remote.js`，仅在该文件不存在时回退到 `index.js`。
- 受控 provenance 发布器备份、替换并校验 `index.mjs`。
- 发布包同时更新 `ReportInputContractService.js`、`ReportGenerationService.js`、`ReportStorageService.js`，确保正式目录和配对审计契约不会被旧 workspace 副本覆盖。
- 发布验收设置 `HOME=/home/netinside`，使用有效模板 ID 的合成报告执行来源解析、正式文件/审计生成和字段核验；无历史工具调用时不以空样本误判发布失败。

## 237 发布与回滚

- 发布批次：`20260903T131500Z`。
- 备份目录：`/var/backups/gaiop/report-provenance-20260903T131500Z`。
- 发布前两次验收误判均自动回滚，未保留半发布状态；最终批次完成。
- 部署包未覆盖整个 OpenClaw workspace，只替换上述白名单文件；未修改历史报告正文、权限矩阵或下载路由。

## 验收

- 合成报告 E2E：`details.ok=true`，正式目录产生 1 个 docx 与 1 个审计 JSON。
- 审计来源字段：`sourceChannel=web`、`sourceUserId=deployment-probe`、`sourceSessionId` 为本次探针会话、`dataSourceId=deployment-probe`。
- Admin、Gateway 均重启并通过健康检查；发布脚本执行 SQLite/服务回滚保护。
- 生产后续仍需用真实登录态完成一次 WebChat 浏览器点击验收；本记录不把无登录态的自动化结果冒充视觉验收。

## 后续注意

- 每次 Gateway 发布必须同时核对 workspace 插件、extension 入口和实际加载文件哈希；不能只看 Git 提交或 `plugins info`。
- 每次报告验收必须同时确认正式 docx、配对审计、`report_files` 登记、来源会话/消息/属主和下载鉴权。
- 不得用文件名、时间或最近消息猜测来源；不得把卡片永久放到会话末尾兜底。
