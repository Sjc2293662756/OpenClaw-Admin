# 2026-09-02 237 WebChat 思考过程详情默认折叠恢复发布记录

## 结论

上一批“当前会话实时思考过程”修复错误地在实时状态首次出现时自动打开“展开详情”。这不是用户要求的行为。此次仅删除该自动展开动作，恢复为紧凑实时状态默认显示、详情由用户手动展开。

## 范围

- WebChat 和 Office 对话面板均保留“显示思考过程”对当前会话实时状态的限制。
- 保留用户手动“展开详情 / 收起详情”交互；不再由设置开启、发送、工具调用或实时状态切换自动修改详情开合状态。
- 不改账户偏好存储、接口、默认值、流式输出、角色权限、报告卡片、历史消息、Gateway、OpenClaw、Skill、GAIOP 后端、频道插件、Caddy、留存或日志配置。

早期会话中多条“数据已采集”等气泡是 Gateway 已持久化的普通助手消息，不是本详情组件。现行历史投影自 2026-08-25 起刻意排除纯 thinking/tool/toolResult 回合；本批不改变该既有产品边界。

## 本地验证

- 详情可见性专项：4/4 通过，断言 WebChat 与 Office 都不存在实时状态自动展开监听。
- Vitest 全量：36 个文件、192 项通过。
- 模块权限回归：12/12 通过。
- Admin Node 回归：231/231 通过，与修复前基线失败集合均为空。
- Node `v22.14.0` 前端生产构建成功，转换 4683 个模块。
- 本批没有修改 JavaScript 文件；`git diff --check` 通过。

## 237 受控发布

- 代码提交：`a2db1f9 fix(webchat): keep live process details collapsed`，已推送 `dev-yangshuo`。
- 发布 ID：`20260902T055546Z`；白名单发布包 433 项，SHA-256 为 `DE0C9B72E893886AE8F042B1E5D17D9CAC6C810CE83BC830F53A4FE9D56E4B9D`。
- stage 已创建 Admin 代码/配置回滚快照，并通过 SQLite 在线备份 API 完成 `wizard.db` 备份与 `integrity_check=ok`；发布过程不迁移业务数据。
- 启动和延迟稳定性探针均通过：Admin active、健康 200、Gateway connected、监听仅 `127.0.0.1:3000`。Caddy active；Upgrade 健康 200；OpenClaw Gateway service active 且健康 200。
- 报告正式目录发布后仍为报告文件 540、审计 JSON 274、配对报告 265；报告路由和数据库架构完整，未发现报告列表错误。未读取报告正文。
- 留存、低风险清理、水位监测、SQLite 备份和会话留存配置均未在本批修改。受控发布不会触及报告文件、会话历史或业务登记。

## 回滚

仅使用本发布 ID 对应的 Admin 代码/配置快照及 SQLite 在线备份回退 Admin 发布物。不得以回滚覆盖 Gateway 历史、报告文件、报告登记或其他业务数据。
