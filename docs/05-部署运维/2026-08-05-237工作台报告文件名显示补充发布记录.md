# 237 工作台报告文件名显示补充发布记录

> 发布日期：2026-08-05（UTC+8）
> 发布批次：`20260805T093351Z`
> 基线：`origin/dev-yangshuo@e5800a9`

## 范围与边界

本批次补正 `20260805T091851Z` 的工作台显示行为：遇到 `MEDIA:` 形式的内部
Word (`.docx`) 路径时，保留安全的裸文件名，而不是隐藏整段内容。

例如，工作台将把：

```text
MEDIA:/home/netinside/.openclaw/workspace/skills/openclaw-napm-report/output/巡检报告_20260805_134713.docx
```

显示为：

```text
巡检报告_20260805_134713.docx
```

- `MEDIA:` 标记、服务器目录、文件查询参数和片段均不显示，也不会进入复制或朗读文本。
- 普通 `.docx` 文件名与图片 `MEDIA:` 内容不受影响。
- 不增加下载按钮，不改 GAIOP 后端、报告 Skill、OpenClaw 核心、频道插件、Admin BFF、数据库、权限、报告归档或公网配置。

## 本地验证

| 验证 | 结果 |
|---|---|
| 文件名清理单元测试 | 3/3 通过：保留裸文件名，移除目录、`MEDIA:` 与查询参数 |
| Node 服务端回归 | 94/94 通过 |
| Vitest | 17 个文件、69/69 通过 |
| 生产构建 | `vue-tsc -b && vite build` 通过 |
| 空白与冲突检查 | `git diff --check` 通过 |

## 发布包与预检

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-report-filename-20260805T093351Z.tgz` |
| 条目数 | 310 |
| 大小 | 3,613,216 B |
| SHA-256 | `4e7a78c36376979c807af941530ef61e85217f44db17c162ae1c254488d6a3b4` |

归档只包含 `dist`、`server`、`package.json` 与 `package-lock.json`。发布前受控核查确认 `gaiop-admin.service` 为 active，仅监听 `127.0.0.1:3000`，源绑定保护存在。

## 237 受控发布与验证

受控暂存已创建应用快照和受控数据库备份；对应回滚点为
`/var/backups/gaiop/admin-prestage-20260805T093351Z`。

- 暂存结果：`staged-awaiting-secret-injection`，归档、目录和依赖检查通过；
  随后的受控启动步骤恢复服务。
- 启动结果：`admin-started-loopback-verified`；`gaiop-admin.service` 为 active，
  仅监听 IPv4 回环，未改公网路由。
- 生产 HTTPS 工作台分包 `ChatWorkspace-BoHjwCaE.js` 下载成功，SHA-256 为
  `a6e75de58205315c113ae31dd43e9309eb215a1daa25293c8eabd6614d26dc17`，与本地构建
  产物一致，并包含 `MEDIA:` Word 文件名清理逻辑。
- 未改 Caddy、443、Gateway、OpenClaw 核心、频道插件、BFF 路由、报告实体、
  数据库业务数据或权限；未触发回滚。

本批次未读取或使用生产登录凭据，因此没有重新触发真实报告生成；单元测试、
完整回归、构建、受控启动和生产静态资源哈希共同覆盖该显示层补正。

## 回滚

如需恢复到文件名显示补充前的 Admin 程序与受控数据库备份，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260805T093351Z'
```

该回滚点不应覆盖发布后新增的业务数据。
