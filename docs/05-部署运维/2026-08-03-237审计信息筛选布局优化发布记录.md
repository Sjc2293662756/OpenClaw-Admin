# 237 审计信息筛选布局优化发布记录

> 发布日期：2026-08-03（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260803T082303Z`
> 运行代码提交：`e65460f style(audit): widen and organize query filters`

## 1. 范围

本批次仅调整 GAIOP-Admin 审计信息页面筛选区布局，未改动审计查询参数、TOP 规则、审计写入、SQLite、权限、其他 BFF 接口或其他模块。

- 宽屏采用两排网格：第一排为扩大后的关键词搜索框与每页/TOP展示设置；第二排为用户、角色、分类、结果、来源、错误码和查询/重置操作。
- 关键词框最大宽度为520px，最小宽度为360px，完整展示“关键词：操作、对象、说明或历史用户”提示；窄屏自动转换为纵向区域并保持自然换行。
- 原有用户单选、系统虚拟用户、服务端筛选、TOP、分页、默认今日、重置、请求竞态保护和详情抽屉保持不变。

## 2. 验证

所有自动化测试使用隔离数据和模拟 API，不操作237真实用户。

| 验证 | 结果 |
|---|---|
| 审计页面组件布局与既有查询交互 | 5/5 通过 |
| Node 测试 | 76/76 通过 |
| Vitest | 16 个文件、60/60 通过 |
| `vue-tsc -b`、生产构建、`git diff --check` | 通过 |

## 3. 237 受控发布

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-audit-layout-20260803T082303Z.tgz` |
| 条目数 | 303 |
| 大小 | 3,581,137 B |
| SHA-256 | `f79d17a4cca66d8da01f2d31220e59a75b8ab78608b66f26e8d7c01442e5307d` |

受控暂存器在切换前创建 Admin 快照并对生产 `wizard.db` 执行完整性检查和受控备份；本记录未读取或输出数据库内容。启动后服务为 `active`、重启次数为0，进程与服务单元工作目录均为 `/opt/gaiop/admin`，只监听 `127.0.0.1:3000`，健康接口为 HTTP 200，Gateway 为 `connected`。

HTTP `/` 实际返回 `index.html` 的 SHA-256 与本地构建一致：`c338f9f483c952502c48a0c926a0e1d15bf3aebbd88af7adb022aba198dc651d`。实际入口引用本批次主包，主包再引用新版 `AuditLogsPage-vPgQ3T6X.js` 与 `AuditLogsPage-Cw9yAyRG.css`，两项资源均返回200且与本地构建哈希一致。

未触发回滚，未修改 Caddy 或公网路由。如后续需要恢复本批次前版本，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260803T082303Z'
```

该回滚点仅恢复本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份。
