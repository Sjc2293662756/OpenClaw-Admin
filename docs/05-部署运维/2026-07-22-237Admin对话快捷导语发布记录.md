# 237 Admin 对话快捷导语发布记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-07-22 |
| 发布批次 | `20260722T053643Z` |
| 变更 | 对话工作台三条快捷导语替换为最近三小时告警、今日系统综述与今日业务报错/慢访问分析。 |

## 1. 发布范围

仅修改 Admin 前端的 `src/views/chat/ChatPage.vue` 文案并重新构建静态产物；未修改 BFF 业务接口、Gateway、报告 Skill、数据源、环境变量、Caddy/443、防火墙或公网路由。

## 2. 备份与回滚

受控发布器在切换前创建本批次的 Admin 应用、systemd 单元和环境目录快照：

`/var/backups/gaiop/admin-prestage-20260722T053643Z`

若需回滚，必须先取得新的服务器写操作授权，再执行：

```powershell
Invoke-237AdminRollback.ps1 -ReleaseId 20260722T053643Z
```

回滚器会恢复切换前应用目录和配置快照、启动旧 BFF；随后需重新验证回环监听、健康接口与 Gateway 连接。

## 3. 验证结果

- 本地 `npm run build` 通过；
- 发布包不含 `.env`、SQLite、报告、私钥或其他敏感配置；
- `gaiop-admin.service` 启动验证为 `admin-started-loopback-verified`；
- BFF 仅监听 `127.0.0.1:3000`；
- Gateway 状态为 connected；
- 报告目录访问、报告接口 JSON 响应和 BFF 归档同步探针保持正常；
- 未出现服务重启失败或回滚。

## 4. 使用验证

通过既有 SSH 隧道访问页面后刷新对话工作台，即可看到新导语。若浏览器缓存旧静态资源，使用 `Ctrl+F5` 强制刷新。

## 5. 横排布局修正

| 项目 | 记录 |
|---|---|
| 发布批次 | `20260722T055015Z` |
| 触发现象 | 较长的新导语在桌面端换行，未保持原有三项横向排列。 |
| 修复 | 快捷导语容器加宽为最多 900px、桌面端禁止换行、压缩文字间距与按钮内边距；仅在窄屏（760px 以下）允许自动换行。 |
| 备份 | `/var/backups/gaiop/admin-prestage-20260722T055015Z` |
| 验证 | 本地生产构建通过；BFF 启动验证为 `admin-started-loopback-verified`，仅监听 `127.0.0.1:3000`，Gateway connected。 |

该批次同样未改动 Gateway、报告、数据源、环境变量、Caddy/443、防火墙或公网路由。若需撤销本次布局修正，先取得新的服务器写操作授权，再执行：

```powershell
Invoke-237AdminRollback.ps1 -ReleaseId 20260722T055015Z
```

## 6. 快捷导语响应式布局重做

| 项目 | 记录 |
|---|---|
| 正式发布批次 | `20260722T061118Z` |
| 触发现象 | 为强制三项横排而缩小字号和内边距，导致宽屏下按钮过小、视觉层级退步。 |
| 修复 | 恢复正常字号与按钮留白；容器最大可用宽度调整为 980px，并按宽屏、中等窗口和小屏三档自然伸缩、换行。宽屏空间充足时保持三项横排。 |
| 影响范围 | 仅 Admin 前端 `src/views/chat/ChatPage.vue` 及重新构建的静态资源；未修改提示词内容、BFF 接口、Gateway、Skill、数据源或网络入口。 |
| 备份 | `/var/backups/gaiop/admin-prestage-20260722T061118Z` |

发布过程曾使用批次 `20260722T060745Z` 发起第一次受控切换，本机等待 124 秒后超时。随后核对确认 237 仍在提供旧静态资源，Admin 服务已恢复且健康，不存在半发布状态。之后使用新的独立批次 `20260722T061118Z` 重新发布并完成验证。

验证结果：

- 本地 `npm run build` 和 `git diff --check` 通过；
- 237 入口 JavaScript 资源指纹与本地构建一致；
- `gaiop-admin.service` 为 `active`，进程退出状态为 0，未发生自动重启；
- `/api/health` 返回 HTTP 200，Gateway 状态为 `connected`；
- Admin 仍仅监听 `127.0.0.1:3000`；
- 未修改 Caddy、公网路由、防火墙或 SSH 配置。

若需撤销本次响应式布局发布，先取得新的服务器写操作授权，再执行：

```powershell
Invoke-237AdminRollback.ps1 -ReleaseId 20260722T061118Z
```
