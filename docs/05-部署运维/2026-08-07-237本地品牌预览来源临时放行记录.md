# 2026-08-07 237 本地品牌预览来源临时放行记录

## 1. 变更范围

- 操作批次：`20260807T044551Z`
- 临时来源：`http://127.0.0.1:5178`
- 用途：本地 Vite 品牌配置页通过 `127.0.0.1:3005 -> 237:3000` SSH 隧道使用 237 Admin 登录和管理数据。
- 仅修改 237 Admin 的 `GAIOP_ALLOWED_ORIGINS` 精确白名单，未修改 Caddy、HTTP 80 跳转、HTTPS 443、证书、DNS、防火墙、公网端口、OpenClaw、升级服务或数据库。
- 本地品牌页代码未发布到 237，本记录不代表新页面已生产部署。

## 2. 备份与实施

- 变更前环境文件备份：`/var/backups/gaiop/admin-origin-20260807T044551Z/admin.env`
- 备份目录权限保持为受控状态；未读取、输出或复制环境文件内容。
- 受控脚本只替换 `GAIOP_ALLOWED_ORIGINS` 行，保留正式来源 `https://101.254.114.237`，并追加 `http://127.0.0.1:5178`。
- 如重启或校验失败，脚本会自动恢复备份并重启 `gaiop-admin.service`；本次未触发回滚。

## 3. 实施后验证

| 验证项 | 结果 |
| --- | --- |
| `gaiop-admin.service` | `active` |
| Admin 监听 | 保持 `127.0.0.1:3000` |
| `Origin: http://127.0.0.1:5178` | HTTP `200` |
| `Origin: https://101.254.114.237` | HTTP `200` |
| `Origin: https://attacker.invalid` | HTTP `403` |

## 4. 项目结束时必须关闭

本放行项是本地联调临时状态。项目结束、本地联调停止或 5178 预览不再使用时，执行：

```powershell
$releaseId = (Get-Date).ToUniversalTime().ToString('yyyyMMddTHHmmssZ')
& .\scripts\Invoke-237TemporaryAdminOrigin.ps1 -Mode Disable -ReleaseId $releaseId
```

关闭验收标准：

- `http://127.0.0.1:5178` 来源恢复 HTTP `403`；
- `https://101.254.114.237` 继续 HTTP `200`；
- 恶意来源继续 HTTP `403`；
- Admin 服务保持 `active` 且只监听 `127.0.0.1:3000`。

受控脚本关闭时只移除 `http://127.0.0.1:5178` 这一项，不覆盖其他后续可能新增的合法来源。
