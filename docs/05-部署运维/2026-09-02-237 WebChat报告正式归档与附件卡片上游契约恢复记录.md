# 2026-09-02 237 WebChat 报告正式归档与附件卡片上游契约恢复记录

## 结论

“报告生成成功但附件卡片不出现”的生产根因位于附件卡片上游。237 当前加载的报告运行时代码被旧工作区内容部分覆盖，导致正式报告目录、来源审计和 WebChat 原生工具回合契约同时退化；前端卡片排序、去重和渲染代码不是本次修改对象。

本次以当前生产文件为基线做语义补丁，恢复 `GAIOP_REPORTS_DIR=/var/lib/gaiop/reports`、正式文件/审计相对路径、可信来源字段，以及 `user -> toolCall -> toolResult -> assistant` 原生回合。自动 `reply_dispatch` 继续仅由企业微信使用，WebChat 不被接管。未覆盖整个 OpenClaw 工作区，未修改角色、模块权限、报告下载接口、Caddy、Admin 数据或留存策略。

## 代码与验证

- Admin 基线为当时最新 `origin/dev-yangshuo@131a7f9`；实施提交依次为 `ca81dcf`、`7b0b33f`、`0897289`、`5108c69`、`8439420`，均已推送 `dev-yangshuo`。
- 修复工具使用严格条目清单、补丁 dry-run、只读 Inspect、临时 Stage、唯一 hunk 门禁、Node 22 语法检查、原子安装和失败自动回滚。
- Node 22 基线 Admin Node `234/234`，合并最新远端后 Admin Node `253/253`；修改前后失败用例名称集合均为空。模块权限 `12/12`、Vitest 37 个文件 `193/193`、Node 22 类型检查和生产构建通过，构建 4683 个模块。
- 报告卡片专项覆盖完成信号重查、空结果不覆盖、实时转持久历史、稳定排序、后续消息、新会话/跨会话隔离和同报告去重；本次没有修改这些前端实现。

## 237 受控发布

- 第一次正式恢复：`20260902T073928Z`，恢复插件、报告输入和正式目录契约。
- 独立复查发现报告生成服务仍缺正式相对审计字段，因此追加最小恢复：`20260902T075544Z`，只补充报告存储正式归属和生成审计契约，已恢复的插件/输入代码保持哈希不变。
- 两次成功发布都建立 Admin 代码/配置快照及在线 `wizard.db` 备份，备份 `integrity_check=ok`。最终回滚点为 `/var/backups/gaiop/report-runtime-contract-20260902T075544Z`。
- 最终运行时 SHA-256：插件 `10341c944d034a227b6bc86efca3f223e80ecb78b02321e60537298d2aaf1a07`；报告存储 `2d8c9167f36dc789f4747d8b5d5ec5b28533408dbad2e2af94766db2b97b3a63`；报告输入 `ccda9f8cebfa36d18a60701c17d8318119de08c0d18d0435c62f45a488092270`；报告生成 `12471cdb91c5e6ec001a98b6a9e465b66d426541bab65c9d066d60f7e83bcdc5`。

## 生产核验

- Gateway、Admin、Upgrade、Caddy 均为 active；Admin 健康 200，仅监听 `127.0.0.1:3000`；Upgrade 未认证 401，HTTPS 200。
- 正式目录 540 个文件，其中审计 JSON 274、配对报告 265；Gateway 和 Admin 服务账户均保持正式目录读写权限。
- `runtimeReportContract=contract-aware`、`runtimeAuditContract=contract-aware`、Gateway 正式目录覆盖正确、报告运行时依赖可用。
- SQLite `integrity_check=ok`；发布前后用户 6、工作区会话 129、报告登记 265、交付 9，业务表未被发布脚本修改。审计计数随本次受控操作正常增加。
- 报告、Admin、Upgrade 留存定时器、水位监测和两套 SQLite 备份定时器保持 active/enabled；会话留存继续 inactive，未启用。

## 验收边界

用户提供的故障会话 key 仍为占位符，因此没有按时间或“最近会话”猜测对象。该会话的 `workspace_sessions`、`chat.history`、`report_files` 来源关系、本人/非本人下载和浏览器刷新/历史回拉验收尚未执行，也未主动生成生产业务报告。

现有 `gateway237-report-storage-inspect.cjs` 的 `adminReportSyncProbe` 仍返回 failed，但其 mock 只允许 `INSERT` prepare，而当前 `syncGeneratedReports()` 在扫描前固定还会 prepare `SELECT` 和 `UPDATE`；生产报告列表无错误、schema 完整且 265 个配对报告已登记。该字段记录为检查脚本假阴性，不作为会话级成功证明。
