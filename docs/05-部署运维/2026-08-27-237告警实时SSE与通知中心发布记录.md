# 2026-08-27 237 告警实时 SSE 与通知中心发布记录

## 范围

- Receiver：持久化告警事件的回放 SSE。
- Admin：Receiver SSE 到 Admin BFF、浏览器单条 Bearer SSE，以及全局告警通知和消息中心前端。
- 未变更 NAPM watcher、Gateway、Skill 运行时、频道插件、生产角色范围或公网路由。
- 未人为注入生产告警，未触发外部通知。

## 发布版本与回滚点

| 部件 | 提交 | Release ID | 备份 / 回滚点 |
| --- | --- | --- | --- |
| Receiver | `1cf669d1d768b028ae33089fe4105234da7edeb7` | `20260827T143641Z` | `syslog-receiver-sse-20260827T143641Z` |
| Admin | `154632b2e879b0f89395794f761021519553de55` | `20260827T143737Z` | `admin-prestage-20260827T143737Z` |

Receiver 先于 Admin 发布。Receiver 发布脚本仅替换 Receiver Skill 目录，只停止/启动 `gaiop-syslog-receiver.service`；在原子切换前建立备份，并在验证失败时自动恢复旧目录和服务。Admin 通过既有 Stage/StartVerify 流程发布，保留预发布 Admin 与数据库备份；未修改 Caddy 或网络配置。

## 发布前门禁

- Admin：Vitest 30 文件、159 用例通过。
- Admin：Node 213 用例通过。
- Admin：`vue-tsc -b`、Vite 生产构建、`git diff --check` 通过。
- Receiver：Jest 101 套件、796 用例通过；运行时契约核验通过；`git diff --check` 通过。
- Receiver 受控发布脚本：语法检查与 2 项边界/脱敏测试通过。

## 线上验证

- Receiver 服务 active，监听仅限 loopback。
- Receiver `/health`、`/alerts` 和标准 SSE 连接首帧验证通过。
- 历史 `alerts.jsonl` 与序列未被清空；发布脚本确认历史行数与最大序列未回退。
- Admin 服务启动、loopback `:3000`、`/api/health` 和 Gateway 连通验证通过。
- Admin 静态入口存在；`/api/events` 对无 Bearer 请求保持 401。
- Admin 持久化的 Receiver 上游流状态为 `connected`，无 gap。

## 发布过程说明

发布期间的早期 Receiver 尝试均在受控验证阶段自动回滚；随后针对 systemd 路径编码与 Receiver 冷启动完成脚本硬化，最终 Release ID `20260827T143641Z` 验证成功。所有临时发布包、预览日志和连接材料均未纳入版本库或发布包。

## 后续

1. 在下一次自然 NAPM Syslog 告警到达时，观察 Receiver 持久化 → Admin BFF → 浏览器消息中心/通知的真实链路；不得为此注入生产告警。
2. 例行确认 Receiver 与 Admin 备份保留策略后，再按运维窗口清理过期发布备份。
