# 2026-09-02 237 WebChat 思考过程下拉样式发布记录

## 范围

将“系统设置 → 界面偏好”中的账户级 `显示思考过程`改为与“界面语言”“主题模式”一致的表单下拉项。可选值为：

- `显示过程与结果`：显示思考、工具执行与回复状态。
- `仅显示对话结果`：隐藏上述中间过程，仅显示对话结果。

选择仍保存为当前账户的既有 `showThinkingProcess` 布尔值；默认值仍为开启。流式输出仍固定开启。

## 不变边界

- 未修改 `GET/PUT /api/chat/preferences`、`chat_display_preferences`、数据迁移或默认值。
- 未修改 basic、standard、admin、auditor 的任何模块权限、路由权限或本人设置范围。
- 未修改 Gateway、OpenClaw、Skill、报告登记/归属/下载、Caddy、留存、备份或日志配置。

## 本地验证

- 思考过程下拉的双选项、失败回退和重试：`6/6` 组件/集成专项测试通过。
- Vitest 前端测试：`188/188` 通过。仓库配置同时扫描 Node 原生 `.test.js`；直接以 Vitest 运行时会将这些文件报告为“无 Vitest suite”，因此以项目正式 Node 原生命令作为完整 Admin Node 验收。
- Node `v22.14.0`：模块权限 `12/12`、Admin Node 回归 `231/231` 均通过。
- Node `v22.14.0` 生产构建通过，转换 `4682` 个模块；本地生产包可正常渲染欢迎页。认证后的设置控件由组件、store 和路由位置测试覆盖。
- 修改关联 JS 的语法检查与 `git diff --check` 通过。

## 237 受控发布

- 发布 ID：`20260902T032844Z`。
- 发布包：433 项，SHA-256 `0530A10AFC9F0BF76F712E2CBE39D11F8E551B80369AA7781EC7D7385C47C311`。
- 发布脚本先完成 Admin 代码/配置回滚快照与 SQLite 在线备份；数据库完整性检查通过。
- 注入既有受控运行配置后，Admin 已稳定启动且仅监听 `127.0.0.1:3000`；内部健康检查与 Gateway 连接均为成功。
- Gateway 与 Upgrade 服务保持 active；Upgrade 健康检查为 HTTP 200，未认证检查为 HTTP 401；Caddy 保持 active，未变更公网路由。
- 发布后报告存储计数保持：报告文件 540、审计 JSON 274、配对报告 265。用户聚合仍为 6 个 active 账户（admin 3、standard 1、basic 1、auditor 1）。
- 会话性能探针：Gateway 会话 100、Admin 增强投影 99、外部来源会话错配 0；历史回拉 5 条成功。

## 回滚

若需要回退，可使用本次受控发布前的 Admin 代码/配置快照与 SQLite 在线备份恢复 Admin 发布物；不得用数据库回滚替代或覆盖报告文件、报告登记与会话数据。该发布未触及上游组件或业务数据。
