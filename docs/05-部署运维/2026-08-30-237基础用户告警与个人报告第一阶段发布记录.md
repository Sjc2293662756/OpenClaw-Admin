# 2026-08-30 237 基础用户告警与个人报告第一阶段发布记录

## 发布范围

- Admin 从 `45bee04a10d02f2dfcd056453150e4a1caae3b5d` 更新至 `902d04f111ba716dfedb9cca9ca5e8b7935e2e96`。
- 基础用户现在可只读查看系统级告警记录、接收既有 `alert` 与 `alertStreamState` SSE 事件、在通知抽屉中管理**本人**实时提醒/声音/三档弹窗与通知偏好。
- 基础用户现在只能列出、预览和下载 `source_user_id` 为当前可信登录用户的报告；其他用户、未知归属和历史未归属报告一律按 `404` 隐藏。
- 不包含第二阶段“管理员逐用户勾选模块权限”。没有修改 Receiver、NAPM、OpenClaw/Gateway 核心、Skill、频道插件、Caddy、防火墙或外部通知。

## 权限与数据暴露边界

| 能力 | basic | standard / auditor / admin |
| --- | --- | --- |
| 告警记录、筛选、分页、详情、SSE 补偿 | 允许只读 | 保持既有能力 |
| 告警通知、未读、浏览器内移除/清空、本人偏好 | 允许，仅当前浏览器与当前账号 | 保持既有能力 |
| 告警导出、告警规则/系统配置、其他管理写入 | `403` | 保持既有矩阵 |
| 报告列出、预览、下载 | 仅本人；他人和未归属报告为 `404` | 保持既有归属与管理边界 |
| 报告上传、删除、编辑、管理 | `403` | 保持既有矩阵 |
| 非告警敏感 SSE / Gateway RPC | 继续拒绝 | 保持既有矩阵 |

通知保持既有断线重连、游标补偿和最近通知上限语义。补偿或 recovered 事件不新增弹窗或声音，且不会因重连重复播放。个人通知偏好不接受目标用户 ID，服务端只对当前会话的用户 ID 读写并记录审计。

## 版本、发布包与回滚

| 项目 | 记录 |
| --- | --- |
| 远端分支 | `origin/dev-yangshuo` |
| 发布提交 | `902d04f111ba716dfedb9cca9ca5e8b7935e2e96` |
| Release ID | `20260830T014753Z` |
| 发布包 | 仅 `dist`、`server`、`package.json`、`package-lock.json`，423 个条目 |
| 发布包 SHA-256 | `dbd10c2990ede7149cf380a893184aadb6ec752ff69465b3fbf67e50ee1ce2f0` |
| 回滚 | 使用 Release ID `20260830T014753Z` 的既有 Admin 回滚入口；不得覆盖生产 SQLite、告警历史、报告或运行时凭据 |

发布包经白名单检查，不含 `.env`、SQLite、告警历史、报告文件、`node_modules` 或凭据。受控 Stage 在切换前保留既有 Admin、systemd、运行配置与在线 SQLite 安全备份，失败按既有边界自动恢复。

## 发布前验证

- `node --test server/lib/permissions.test.js server/lib/sse-access.test.js server/routes/alerts.test.js server/routes/reports.test.js`：29 项通过。
- `npx vitest run src/permissions/access-control.test.ts src/stores/alert-realtime.test.ts src/realtime/global-sse-lifecycle.test.ts`：27 项通过。
- 完整 `npm test` 通过，其中 Node 测试 218 项通过、0 失败。
- `npm run build` 与 `git diff --check` 通过。

覆盖项包括：basic 的告警列表/时间/补偿/偏好与 SSE；basic 本人报告列出/预览/下载；他人报告与未归属报告的 `404`；basic 的报告上传/删除、告警导出及其它受保护写入/敏感事件拒绝；standard、auditor、admin 既有权限不回退。

## 237 受控发布与验证

- 已执行既有 `Invoke-237AdminStage.ps1`，随后执行 `Invoke-237AdminStartVerify.ps1`；启动结果为 `admin-started-loopback-verified`，`gaiop-admin.service` 仅绑定 `127.0.0.1:3000`，未改公网路由。
- 已在生产端只读核验本发布的告警补偿、报告预览和 basic SSE 角色标识代码存在。
- 既有告警流只读验证通过：`/api/health` 正常、未认证浏览器 SSE 继续被拒绝、Receiver 上游为 `connected`、`gapState=none`。
- 未注入生产告警、未触发外部频道，也未使用或读取任何用户登录凭据。

## 登录后验收边界

请使用既有基础用户账号进行一次交互式验收：打开告警记录和通知抽屉，保存本人偏好，检查本人报告的预览/下载，并确认猜测他人或未归属报告 ID 返回 `404`。本发布未以生产账号模拟此操作；该交互不影响上述服务端权限与自动化测试结论。
