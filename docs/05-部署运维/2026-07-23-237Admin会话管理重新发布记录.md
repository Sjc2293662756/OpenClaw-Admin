# 237 Admin 会话管理重新发布记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-07-23 |
| 目标 | 将 `dev-yangshuo` 中已完成的会话归属、历史 WebChat 标题、会话列表、报告来源及相关前端体验改动重新发布到 237 |
| 最终发布批次 | `20260723T021608Z` |
| 最终结果 | 发布成功，新版 Admin 前端/BFF 已生效 |

## 1. 发布前校验与发布边界

发布前完成以下本地校验：

- `npm test`：Node 测试 40 项、Vitest 测试 11 项，合计 51 项全部通过；
- `npm run build`：生产构建成功；
- 发布器脚本通过 `node --check` 与 `git diff --check`；
- 发布包共 272 个条目，仅包含 `dist`、`server`、`package.json` 和 `package-lock.json`；
- 发布包未包含 `.env`、SQLite、报告、私钥、Token、频道 Secret 或客户配置。

本次仅更新 Admin 前端/BFF 及 Admin systemd 单元的停止超时保护。未修改 Gateway、频道插件、数据源、告警接收器、Caddy/443、防火墙或公网路由。

相关 Git 提交：

- `f1ca1bf`：会话归属、会话标题、报告来源与页面体验改动；
- `82f9660`：Admin 服务停止改为非阻塞轮询，并增加有界强制结束保护；
- `a42b309`：将 Admin 启动监听和回滚验证等待窗放宽到适合 237 实际启动耗时的范围。

## 2. 第一次重发与受控恢复

第一次重发批次为 `20260723T021006Z`，切换前备份为：

`/var/backups/gaiop/admin-prestage-20260723T021006Z`

阶段发布成功，返回结果确认已创建备份，未修改 Caddy 和网络。启动验证在 `LOOPBACK_LISTENER` 阶段超过原 45 秒等待窗，受控启动器停止了未通过验证的新服务；当时未出现公网监听。

随后执行：

```powershell
Invoke-237AdminRollback.ps1 -ReleaseId 20260723T021006Z
```

回滚器也因原 45 秒监听等待窗返回超时，但目录恢复动作已经完成。紧随其后的只读核验确认：

- `gaiop-admin.service` 为 `active`；
- systemd 结果为 `success`，主进程退出状态为 `0`；
- 监听仅为 `127.0.0.1:3000`；
- 完整健康与 Gateway 连接复验通过；
- 公网路由没有变化。

因此，本次现象的直接原因是发布验证等待窗短于 237 上 Admin 实际从启动到形成监听所需的时间，不是业务代码构建失败，也不是端口对公网开放。

## 3. 发布器调整

针对两次超时，发布器作了以下有界调整：

1. `systemctl stop` 改为非阻塞调用；
2. 停止后轮询 30 秒，仍未退出时仅对 `gaiop-admin.service` 执行强制结束，并再核验 10 秒；
3. Admin systemd 单元增加 `TimeoutStopSec=20s`、`KillSignal=SIGTERM` 和 `SendSIGKILL=yes`；
4. 服务 active 与回环监听等待窗放宽至最多 120 秒；
5. 回滚后的回环监听等待窗同步放宽至最多 120 秒；
6. 本地验证客户端总等待上限放宽，但远端失败时停止服务和禁止公网监听的保护保持不变。

## 4. 第二次发布与验证

最终发布批次为 `20260723T021608Z`，切换前备份为：

`/var/backups/gaiop/admin-prestage-20260723T021608Z`

执行顺序：

```powershell
Invoke-237AdminStage.ps1 -ReleaseId 20260723T021608Z -ArchivePath <本地受控发布包>
Invoke-237AdminStartVerify.ps1
```

非敏感验证结果：

- 阶段发布完成并创建切换前备份；
- `gaiop-admin.service` 启动成功；
- 监听范围为 `loopback-ipv4`，即仅 `127.0.0.1:3000`；
- `/api/health` 验证通过；
- Gateway 状态为已连接；
- Caddy、网络和公网路由均未改变；
- 通过现有本机 SSH 隧道读取到的前端入口资源为 `assets/index-CPNUDwo9.js`，与本地本次生产构建指纹一致，确认新版静态资源已生效。

## 5. 回滚方法

如最终批次出现需要整体恢复的问题，使用本次切换前快照：

```powershell
Invoke-237AdminRollback.ps1 -ReleaseId 20260723T021608Z
```

回滚后必须重新核验：服务为 `active`、监听仅为 `127.0.0.1:3000`、健康接口成功、Gateway 已连接，并通过 SSH 隧道检查前端页面。不得通过开放 3000 端口绕过隧道验证。

## 6. 未完成项

- 本次只解决发布生效与发布器等待窗问题；会话管理首次打开和“继续对话”加载较慢的问题仍按既有记录留待后续专项优化；
- 最近活动排序问题未在本次处理；
- 外部频道 `/new` 后历史上下文的产品展示方式、飞书渠道用户名解析和报告来源链路仍需后续设计与联调。
