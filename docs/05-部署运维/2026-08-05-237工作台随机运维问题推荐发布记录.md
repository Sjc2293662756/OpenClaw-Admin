# 237 工作台随机运维问题推荐发布记录

> 发布日期：2026-08-05（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260805T080132Z`
> 运行代码提交：`d8209d2 feat(workspace): randomize operations prompts`

## 范围与边界

本批次复用对话工作台欢迎区现有的“点击问题后填入输入框”行为，将原本固定的三条问题替换为前端静态题库。

- 题库包含网络、业务、应用、告警、报告五个方向的正常运维分析问题；不包含数据包任务或客户端 IP 排行问题；
- 每次进入未开始的新对话时，先随机选择三个不同方向，再从各方向随机抽取一条；切换中英文显示时重新生成对应语言的三条问题；
- 不增加后端接口、数据库、持久化配置、权限规则、发送行为或查询/报告 Skill 的执行逻辑；点击后仍仅填入草稿，不会自动发送；
- 报告问题覆盖今日、昨日、近 7 天、近 30 天和本月的全局、网络、业务、应用、业务组、告警综述及健康巡检，并保留“某业务”与“某应用”两条可由用户补充名称的提示。

## 本地验证

| 验证 | 结果 |
|---|---|
| 随机选择单元测试 | Vitest 1 个文件、2/2 通过；验证三条来自不同方向和中英文文本可用 |
| 相关前端单元测试 | 随机选择与既有品牌显示共 2 个文件、4/4 通过 |
| 生产构建 | `vue-tsc -b && vite build` 通过 |
| 空白与冲突检查 | `git diff --check` 通过 |

## 237 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-workspace-prompts-20260805T080132Z.tgz` |
| 条目数 | 310 |
| 大小 | 3,612,811 B |
| SHA-256 | `0755cd988075868c9592124d1afda040cbf682d19befd52f8f150c84c0840d1a` |

受控暂存完成归档哈希校验、应用快照和受控数据库备份。启动和发布后验证结果：

- `gaiop-admin.service` 为 `active`，仅监听 `127.0.0.1:3000`，源绑定保护存在；
- 通过本地隧道访问 `/api/health` 返回 HTTP 200，Gateway 为 `connected`；
- 当前工作台分包 `ChatWorkspace-D_wzq2XL.js` 返回 HTTP 200，包含新题库中的“当前流量分析系统健康巡检报告”；
- 未改 Caddy、公网路由、DNS、防火墙、证书、Gateway、OpenClaw 核心、频道插件、数据库业务数据或权限；未触发回滚。

如需恢复到本批次前的 Admin 程序和受控数据库备份，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260805T080132Z'
```

该回滚点仅覆盖本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份。
