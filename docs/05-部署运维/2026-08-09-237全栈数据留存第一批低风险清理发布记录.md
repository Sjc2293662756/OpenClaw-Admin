# 237 全栈数据留存第一批低风险清理发布记录

| 属性 | 内容 |
|---|---|
| 发布日期 | 2026-08-09 |
| 成功批次 | `20260809T041948Z` |
| Admin 提交 | `16458b65b0d257b9a7765f8a2ece227cf481ec3a` |
| Upgrade 提交 | `eaef3501a11a34f91f81f0d95152ad94922522b9`、`efc05136c588e7215a9d4956c0e309c1b1bc61bd` |
| 自动删除 | Admin、Upgrade 均为 `false` |

## 1. 发布范围

- Admin：报告来源信封 48 小时物理删除保护、升级上传中转 24 小时补偿清理、独立 one-shot、单实例锁、受限审计和每日 timer。
- Upgrade：成功包残留补删、失败/回滚包 7 天保留、staging 24 小时清理、回滚备份 90 天且超出最近 5 个可用组的双条件保护、共享目录和物理优先删除、独立 one-shot 与每日 timer。
- 正式报告及其审计/来源/交付、数据库内容、`.bak`、会话、告警、日志、原始数据包、外部频道发送副本和磁盘水位治理均未纳入。

## 2. 237 实时路径

- Admin 代码：`/opt/gaiop/admin`；数据库：`/var/lib/gaiop/admin/wizard.db`；来源信封：`/var/lib/gaiop/runtime/report-provenance`；升级中转：`/opt/gaiop/admin/data/upgrade-upload-staging`。
- Upgrade 当前 systemd 工作目录：`/opt/gaiop-upgrade-e2e-20260805`；数据库：`/var/lib/gaiop-upgrade/napm-upgrade.db`；数据库同级包目录：`/var/lib/gaiop-upgrade/packages`；staging：`/var/lib/gaiop-upgrade/staging`；回滚备份根：`/var/backups/gaiop/upgrade`。
- 发布脚本按 `gaiop-upgrade.service` 的实时 `WorkingDirectory` 和非敏感路径配置锁定目标，没有沿用历史文档中的固定应用目录。

## 3. 发布与回滚

- 首次批次 `20260809T041317Z` 在 Upgrade 重启阶段发现定向归档未包含当前 `src/index.js` 新依赖的 `runtime-safety.js`，受控脚本自动恢复 Admin/Upgrade 源码、环境文件和 unit，两个服务恢复 active；自动删除未开启，one-shot 未执行。
- 成功批次改为发布经全量测试的完整 Upgrade `src` 树；发布前再次备份，回滚点为 `/var/backups/gaiop/deployments/retention-20260809T041948Z`。
- 新启动日志仅记录 Upgrade 服务启动；历史进程内 `BackupCleaner.start()` 已从代码移除，成功批次启动后没有再次出现“备份定时清理已启动”消息。

## 4. 关闭态验证与计数

两个 one-shot 只在总开关关闭状态下运行一次，用于验证 unit、锁和受限审计；记录结果均为 `auto_delete_disabled`，没有执行文件删除。发布前后计数一致：

| 项目 | 发布前 | 发布后 |
|---|---:|---:|
| Admin 数据库各业务表合计行数 | 1788 | 1788 |
| Upgrade 数据库各业务表合计行数 | 15 | 15 |
| 报告来源信封目录项 | 60 | 60 |
| Admin 升级中转目录项 | 0 | 0 |
| Upgrade packages 目录项 | 0 | 0 |
| Upgrade staging 目录项 | 0 | 0 |
| Upgrade 回滚备份根目录项 | 3 | 3 |
| 正式报告根目录项 | 11 | 11 |

以上计数只证明发布未改变生产文件和数据库；它们是发布时快照，不得作为以后清理候选依据。

## 5. 最终状态

- `gaiop-admin.service`、`gaiop-upgrade.service`、Gateway、Caddy 均为 active；Admin 与 Upgrade 内部健康检查为 HTTP 200，Upgrade 未认证接口为 401。
- 公网 Caddy HTTPS 入口返回 HTTP 200；Admin 3000 仍只监听 loopback。
- `gaiop-admin-retention-cleanup.timer` 与 `gaiop-upgrade-retention-cleanup.timer` 均为 enabled/active；前者每日 UTC 02:15，后者每日 UTC 02:30。
- `GAIOP_ADMIN_RETENTION_AUTO_DELETE=false`、`GAIOP_UPGRADE_RETENTION_AUTO_DELETE=false`。首次真实开启前必须重新只读列出实际目录、候选类别/数量/时间/空间和保护项，并取得单独确认。
