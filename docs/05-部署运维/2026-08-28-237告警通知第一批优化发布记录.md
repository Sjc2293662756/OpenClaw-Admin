# 2026-08-28 237 告警通知第一批优化发布记录

## 发布范围

- Admin 从上一轮发布记录提交 `b9c1c41f3532eb7c0f6642274a620796b69fa6bc` 更新至 `e96b591b9f5d322fa6237548531b3e3edc2c1d2f`。
- 包含告警记录精确定位与短暂高亮、详情布局与横向表格滚动优化、列表/通知语义区分、告警通知中英文文案、按严重级别的内存批量已读/清空、权威告警编号分析入口、阅读详情时视觉弹窗静音，以及最终“叮咚”提示音。
- 未修改 Receiver、NAPM watcher、Gateway/OpenClaw 核心、Skill、频道插件、Caddy、防火墙、DNS、证书或角色权限。
- 未人为注入生产告警，未触发外部通知。

## 版本、发布包与回滚点

| 项目 | 记录 |
| --- | --- |
| 代码提交 | `e96b591b9f5d322fa6237548531b3e3edc2c1d2f` |
| 远端分支 | `origin/dev-yangshuo` |
| Release ID | `20260828T132817Z` |
| 发布包 | 仅 `dist`、`server`、`package.json`、`package-lock.json`，417 个条目 |
| 发布包 SHA-256 | `287ef3e6695b963ea164bc134bdd3eb7b4e99d62a4b12fbbd1b88beeed41de52` |
| 回滚点 | `/var/backups/gaiop/admin-prestage-20260828T132817Z` |

受控 Stage 在切换前保存 Admin、systemd unit、运行配置快照以及在线 SQLite 备份；失败时执行既有自动回滚。发布包不包含 `.env`、SQLite、告警历史、模拟数据、预览日志、`node_modules` 或凭据。

## 发布前门禁

- 提交与远端分支均核对为 `e96b591`，工作树 clean。
- 告警与提示音定向 Vitest：6 文件、22 用例通过。
- 已有完整回归记录：全量 Vitest、Node 契约测试、`vue-tsc`、Vite 生产构建与 `git diff --check` 通过。
- 本次发布前再次执行 `npm run build` 与 `git diff --check`，通过。

## 237 受控发布与验证

- 使用根目录既有 `Invoke-237AdminStage.ps1` 完成 Admin 受控切换；随后 `Invoke-237AdminStartVerify.ps1` 返回 `admin-started-loopback-verified`。
- `gaiop-admin.service` 已启动，监听范围为 `loopback-ipv4`（`127.0.0.1:3000`）；未修改公网路由。
- `/api/health` 返回成功，Gateway 为 connected。
- 静态入口存在，且入口引用的全部哈希资源与本次本地 `dist` 匹配；浏览器 `/api/events` 无 Bearer 请求继续返回 401。
- 告警 SSE 只读验证两次均显示 `gapState=none`，但 Receiver 上游状态为 `unavailable`，未能确认 `connected`。本次未通过制造生产告警或修改 Receiver 来掩盖该依赖状态。

## 声音验证边界

“叮咚”提示音为浏览器 Web Audio 代码；已通过本地定向测试和生产构建。受浏览器自动播放策略与本次不注入生产告警的限制，未在 237 上实际播放或声称已远程听到声音。

## 发布后 Receiver 依赖复核

- 使用既有受控 Receiver SSE 只读检查器确认 `gaiop-syslog-receiver.service` 为 active、监听 loopback，`/health` 与 `/alerts` 成功，历史 `alerts.jsonl` 存在，且没有遗留 stage/incoming/previous 发布目录；受控 Receiver 备份标记存在。
- 该服务当前技能目录缺少 `ReceiverHttpServer.js`。补充的、同一受控包装器下的 `/events` 首帧探针未取得 SSE `: connected` 首帧，因此不能将 Receiver SSE 入口视为健康。
- Admin 在有界等待重连后仍报告 Receiver upstream `unavailable`，`gapState=none`。未重启健康的 Receiver，未重新发布 Receiver 代码，未重启 NAPM watcher、Gateway 或其他服务，也未再次部署 Admin。
- 此项阻断需要在独立的 Receiver 发布/恢复任务中，以与 2026-08-27 Receiver 发布相同的受控范围核对当前 Receiver 代码与 SSE 运行时；本次 Admin 发布不越权处理。

## Receiver SSE 精确恢复结果

- 在独立、clean 的 Receiver 工作树中核对已审批版本 `1cf669d1d768b028ae33089fe4105234da7edeb7` 与 `origin/main` 一致；未开发或修改 Receiver 代码。
- 使用既有受控 Receiver 发布器恢复该精确版本：Receiver Release ID 为 `20260828T134803Z`，回滚/备份点为 `/var/backups/gaiop/syslog-receiver-sse-20260828T134803Z`，归档仅包含 9 个已跟踪的 `skills/openclaw-napm-syslog-receiver` 文件，SHA-256 为 `a6fa8aec62e7265f185b203a27ac38cfb72224b0f3aa17ed6c1b20cef89bb446`。
- 发布器已验证精确 user unit `gaiop-syslog-receiver.service`、`/health`、`/alerts`、`/events` 的 `: connected` SSE 首帧以及历史行数和最大 `streamSequence` 均未回退；未停止或重启 NAPM watcher、Gateway、频道插件或其他服务，未发生回滚。
- 发布后只读复核显示 Receiver active、loopback、`ReceiverHttpServer.js` 存在、历史存在且无遗留切换目录。先前缺少该文件的事实由本次精确恢复消除。
- Admin 在有界重连后报告 Receiver upstream `connected`、`gapState=none`；`/api/health`、Gateway connected、loopback `:3000`、静态资源和未认证 `/api/events` 401 均继续正常。

## 回滚

如需回滚 Admin，使用 Release ID `20260828T132817Z` 的既有 Admin 回滚入口。该操作应恢复切换前 Admin 目录和服务，不覆盖生产 SQLite、告警历史、报告或运行时凭据；回滚后重新核验服务 active、回环监听、健康接口、Gateway 与告警 SSE 状态。

## 后续

在不注入告警的前提下，待 Receiver 恢复后复核其上游状态为 `connected`；随后只在自然到达的 NAPM Syslog 告警时观察真实 Receiver → Admin BFF → 浏览器通知链路。
