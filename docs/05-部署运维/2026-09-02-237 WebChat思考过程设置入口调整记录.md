# 2026-09-02 237 WebChat 思考过程设置入口调整记录

## 1. 调整结论

- “显示思考过程”继续是当前登录账户自己的设置，不改为系统级配置。
- 唯一正式入口为“系统设置 → 界面偏好”。
- 移除 WebChat 头像菜单中的重复“我的设置”入口及弹窗，避免用户误以为存在两套设置入口。
- 保留 `GET/PUT /api/chat/preferences`、`chat_display_preferences` 表、默认开启值以及既有 `basic`、`standard`、`admin`、`auditor` 权限边界。
- 不调整“系统设置”模块本身的角色权限；没有系统设置访问权的角色仍遵循原有页面权限规则。

## 2. 影响范围

- 仅修改 `GAIOP-Admin/src/views/workspace/ChatWorkspace.vue` 的导航入口和弹窗。
- 思考过程显示逻辑、流式输出、报告完成消息、报告附件卡片、下载接口和报告权限均不变。
- 未修改 OpenClaw、Gateway、Skill、频道插件、GAIOP 后端、数据库业务数据或生产配置。

## 3. 验证要求

- 检查头像菜单不再渲染“我的设置”或设置弹窗。
- 检查“系统设置 → 界面偏好”仍加载并保存当前账户的“显示思考过程”。
- 执行 Admin Node 回归、Vitest 全量、前端生产构建、修改 JS 的语法检查和 `git diff --check`。

## 4. 本地验证结果

- Node `v22.14.0` 下 Admin Node 回归 `231/231`、模块权限 `12/12` 通过；Vitest 为 35 个文件、`189/189` 通过。
- 前端生产构建通过，4682 个模块转换完成；`node --check` 和 `git diff --check` 通过。
- 新增契约测试确认系统设置页面只保留一个思考过程面板，WebChat 工作台不再包含设置弹窗或“我的设置”文本。

## 5. 237 受控发布与复核

- 提交：`ec97824`（`fix(webchat): keep thinking preference in system settings`），已推送 `origin/dev-yangshuo`。
- 发布批次：`20260902T024020Z`；发布包仅含 `dist`、`server`、`package.json`、`package-lock.json`，433 项，禁止项 0，SHA-256：`b91e0229b09ea498047d53eda27094ba0fa6deda90c1e2c28233faa8115f1c0d`。
- 暂存完成并创建 Admin 回滚快照、SQLite 在线备份；启动验证 `admin-started-loopback-verified`，稳定性复核通过，公网路由未修改。
- 237 Admin `active/enabled`，监听仅为 `127.0.0.1:3000`；Gateway connected，Upgrade/Caddy 状态由只读预检保持正常，安全接口探针全部通过。
- SQLite `integrity_check=ok`；用户聚合仍为 6 个 active（admin 3、standard 1、basic 1、auditor 1）；会话性能探针 100 条、增强投影 99 条、历史回拉成功、外部会话归属错配 0。
- 发布未修改报告、用户、会话、交付、审计业务数据及留存/备份任务；未修改权限矩阵、OpenClaw、Gateway、Skill、频道插件、GAIOP 后端、Caddy 或系统日志配置。

## 6. 回滚

使用本批次 `/var/backups/gaiop/admin-prestage-20260902T024020Z` 中的 Admin 快照和 SQLite 在线备份，按既有受控回滚流程执行；回滚后重新验证 Admin/Gateway/Upgrade/Caddy、回环监听、SQLite 完整性和前端资源。
