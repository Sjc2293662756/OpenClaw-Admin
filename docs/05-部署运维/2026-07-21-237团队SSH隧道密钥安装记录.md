# 237 团队 SSH 隧道密钥安装记录

| 项目 | 记录 |
|---|---|
| 日期 | 2026-07-21 |
| 范围 | 为共同开发人员访问回环 Admin BFF 安装团队公钥 |
| 安装批次 | `20260721T023704Z` |

## 1. 授权与边界

用户明确授权安装团队 SSH 公钥，用于建立本机到 `237:127.0.0.1:3000` 的回环 Admin BFF 隧道。未开放公网端口，未修改 Caddy、443、防火墙、Gateway、Admin BFF、频道、接收器或业务配置。

## 2. 密钥处理

本机生成独立团队 ED25519 密钥对；私钥由用户自行设置口令。全过程未读取、打印、复制、上传或提交私钥、口令、服务器密码或其他敏感值。

## 3. 服务器变更与备份

修改 `netinside` 的 `authorized_keys` 前已创建版本化备份，再追加团队公钥并进行精确存在性检查。备份文件为：

`/home/netinside/.ssh/authorized_keys.gaiop-backup-20260721T023704Z`

## 4. 验证与回滚

服务端公钥安装校验完成。团队私钥受用户口令保护，实际本地隧道验证须由持钥人在本机输入私钥口令后执行；口令不交给自动化脚本。

如需撤销，必须在另行授权后确认当前 `authorized_keys` 与上述备份目标，再恢复备份并检查所有者、权限及既有登录方式。不能用广泛覆盖或删除方式影响其他已有登录公钥。
