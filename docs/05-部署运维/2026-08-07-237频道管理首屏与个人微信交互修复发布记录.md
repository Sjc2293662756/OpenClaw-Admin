# 2026-08-07 237 频道管理首屏与个人微信交互修复发布记录

## 1. 发布结论

- 发布批次：`20260807T092035Z`
- 发布范围：GAIOP Admin 前端；BFF 代码随完整 Admin 包保持当前基线，未修改接口契约。
- 回滚快照：`/var/backups/gaiop/admin-prestage-20260807T092035Z`
- 未变更：生产 SQLite 数据、个人微信适配器、OpenClaw Gateway、个人微信插件、报告归因工作器、Caddy、公网入口、证书、防火墙、DNS、升级服务和 238。

## 2. 修复内容

1. “添加个人微信”弹窗限制为最大 `520px`，同时保留窄屏 `32px` 页面边距和视口内滚动，不再占满整页。
2. “刷新”和“添加个人微信”使用 `12px` 操作间距。
3. 飞书、钉钉、企业微信、个人微信四个频道进入页面时默认全部收起；刷新状态不再改写用户当前折叠状态，只有创建某频道草稿时展开该频道。
4. 初次频道和个人微信状态请求继续并行执行，但不再用整页加载遮罩阻塞折叠列表；标题标签先显示“读取中”，请求完成后原位更新。
5. 个人微信账号启停采用前端乐观状态：停用后立即显示“已停用”，启用后先显示“未知”；接口失败恢复原状态。20 秒状态轮询不会覆盖正在进行的账号操作。

## 3. 本地验证

| 验证项 | 结果 |
| --- | --- |
| Node 测试 | 118 项通过 |
| Vitest | 21 个文件、87 项通过 |
| 合计 | 205 项通过，0 失败 |
| 新增回归用例 | 停用状态立即更新；服务端拒绝后恢复原账号状态 |
| 生产构建 | `npm run build` 通过 |
| 差异检查 | `git diff --check` 通过 |
| 浏览器视觉检查 | 最新构建的频道页活动折叠项数量为 0，四个频道首屏紧凑展示；本地预览无 BFF，不将其状态请求错误作为生产结论 |

## 4. 发布包与生产验证

- 发布包：`gaiop-admin-channel-ui-20260807T092035Z.tgz`
- 大小：3,669,114 bytes
- SHA-256：`b945cbf4e2ab617a66bd3ea8ca0be8e7779999dfa667e28292b7b904dbc5c770`
- `dist/index.html` SHA-256：`a4a1c67273d75925c538ce45b9f16855203ca630e566fe495818ba65b5e76ee1`
- `ChannelsPage` JS SHA-256：`8960ff327e9ee60ad5748f079d5300c56c679c20881c59047bbf6b3f9bc46a4d`
- `ChannelsPage` CSS SHA-256：`59e1e0f2f6594b4cee75f3c7ed73459b4a8c0c2299438ba7660a0b9bd45e8247`
- 237 上述三个静态文件哈希均与本地构建一致。
- `gaiop-admin.service`、`caddy.service`、`openclaw-gateway.service`、`gaiop-personal-wechat.service`、`gaiop-report-attribution.service` 均为 `active`。
- Admin 监听 `127.0.0.1:3000`，个人微信适配器监听 `127.0.0.1:19091`；未出现公网监听。
- Admin 健康接口 200，公网 HTTPS 200；未认证 Admin 个人微信接口和适配器状态接口均为 401。

## 5. “报告已生成但微信未收到”只读诊断

本批次发布前对用户刚生成的最近一份个人微信报告做了不读取正文和身份信息的事件级诊断：

- 报告归因索引存在对应 `openclaw-weixin` 条目，报告实体和审计文件均存在；证据为官方报告工具结果。
- 对应 Gateway 会话中，报告汇总工具和报告导出工具均成功；其后执行了带媒体文件的 `message` 发送工具，工具结果未标记错误。
- 会话最后存在 `stop` 状态的 assistant 文本，并包含 `MEDIA:` 媒体标记，说明最终回复没有被 Admin BFF 或报告归因工作器截断。
- 报告完成前后 15 分钟的个人微信日志中存在发送活动，但未发现发送失败、超时、鉴权、限流或网络错误；个人微信插件代码包含媒体和文档发送实现。
- 当前只能证明 Gateway/插件已接受发送，不能证明微信客户端已确认送达。该边界不经过 Admin BFF，本次前端发布未修改 OpenClaw 核心或个人微信插件，因此不将其判定为本次 BFF 回归，也不在未获单独授权时升级或修改插件。

后续复测若再次出现，应记录触发时间，由运维按同一会话时间窗核对个人微信插件的出站确认能力；如需改 OpenClaw 核心或频道插件，必须按频道插件升级保护流程单独审批、全频道验证和整体回滚。

## 6. 回滚

- 发布切换点：`/var/backups/gaiop/admin-prestage-20260807T092035Z`
- 本批次没有数据库迁移或 BFF 契约变化。发生前端回归时优先恢复快照中的 Admin 代码和静态资源并保留当前生产数据库。
- 受控 `Invoke-237AdminRollback.ps1 -ReleaseId 20260807T092035Z` 会同时恢复该批次的 SQLite 备份；只有确认允许覆盖发布后新增写入时才可直接使用。
- 回滚后重新验证 Admin 回环监听、健康接口、Gateway 连接、个人微信适配器状态和公网 HTTPS；无需修改 Caddy、证书、DNS、防火墙或插件。
