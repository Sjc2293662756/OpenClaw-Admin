# 237 部署前只读预检记录

| 项目 | 内容 |
|---|---|
| 时间 | 2026-07-20 15:43:27 +08:00 |
| 范围 | 237 部署前只读预检 |
| 授权 | 用户明确授权建立受控 SSH 连接并执行固定只读命令集 |
| 变更 | 无服务器写入、无配置修改、无安装、无重启、无部署 |

## 1. 目的

确认 237 当前运行基线是否适合承接 GAIOP-Admin 前端和 BFF 部署，并识别部署前必须补齐的进程、网络、持久化和回滚信息。

## 2. 执行方式与敏感信息边界

1. 先通过根目录受控连接脚本建立 Gateway 与告警接收器的本地回环隧道，确认本机 3003、3004 均只监听回环地址；预检后关闭本次创建的隧道进程。
2. 使用本地受控只读调用器经 SSH 执行固定命令：操作系统/Node/Gateway 版本、Gateway 深度状态、三频道插件状态、频道探针、PM2、Nginx、TCP 监听、磁盘和 GAIOP 数据根目录元数据。
3. 调用器不接受任意远端命令，不打印主机、账号、密码、Token、Secret、完整配置、原始日志或原始命令输出；文档仅记录脱敏摘要。

## 3. 非敏感结果

| 检查项 | 结果 | 判断 |
|---|---|---|
| 受控隧道 | Gateway 与告警接收器均可经本机回环端口访问 | 当前开发链路可达 |
| Gateway 基础端点 | 经隧道 TCP 可达且返回 HTTP 响应 | 基础服务存在；未进行 Token/RPC 写操作 |
| Syslog 接收器 | 健康、已启用、已有接收记录、当前无错误 | 正式告警接收链路的接收器部分正常 |
| 操作系统 | Ubuntu 22.04.5 LTS，Linux 5.15 内核 | 记录为当前部署输入 |
| Node | v22.14.0 | ISO/发布清单后续需冻结精确运行时版本 |
| PM2 | 状态命令可执行 | 当前 Gateway 仍可能由 PM2 管理，部署前需确认实际应用与启动方式 |
| Gateway CLI、插件检查、频道探针 | 非交互 SSH 会话中未得到可用结果 | 不能据此认定 Gateway/频道故障；需先只读确认实际 OpenClaw 可执行路径与 PM2 运行环境 |
| Nginx | 未确认处于 active 状态 | 443 入口尚不能视为已就绪；可能未安装、未启用或非 systemd 管理，需继续核验 |
| TCP 监听摘要 | 发现 2 个回环监听、5 个通配监听、1 个其他监听 | 未输出地址/端口；部署前必须映射其所属服务，并确认 Gateway/BFF/接收器不被非预期网络面暴露 |
| 磁盘 | 最大已用空间 48% | 当前无明显容量阻塞；部署包、备份和报告目录仍需按实际容量复核 |
| ISO 数据根目录 | 未发现或当前 SSH 用户不可读取预期根目录 | 当前尚未形成 ISO 持久化布局；部署前需创建并授权，但本次未写入 |

### 3.1 第二轮补充预检（2026-07-20 15:48:34 +08:00）

| 检查项 | 结果 | 判断 |
|---|---|---|
| 默认 OpenClaw CLI 路径 | 当前非交互 SSH 会话未发现 | 需要只读确认实际运行环境后，才能对 Gateway/三频道做 CLI 级版本核验 |
| Node 运行时 | 发现 4 个 Node 进程，其中 2 个工作目录具有 GAIOP/OpenClaw 特征 | Gateway/接收器链路仍在运行，但当前不应覆盖未知 Node 进程 |
| PM2 | PM2 可执行，但未发现可识别的受管应用状态 | 当前服务可能由直接 Node 或其他启动方式管理；需确认启动所有者与恢复方式 |
| 443 | 已被现有服务占用，且不是可确认的 Nginx 进程 | 不得覆盖、停止或改写该监听；需由服务所有者确认其归属与反向代理策略 |
| Nginx | 未安装或当前用户无法以 systemd 方式确认 | 不能假定可直接使用 Nginx 模板部署 443 |
| 权限 | 当前 SSH 用户无非交互 sudo，且不能写入 `/opt` | 无权安装系统组件、创建 ISO 建议目录、接管 443 或执行系统级部署 |
| 包管理器 | 可用 | 仅代表目标系统具有后续安装能力；本次未安装任何软件包 |
| 防火墙状态 | 当前用户无法安全确认 | 后续由具备相应权限的部署账户按变更计划核验 |

## 4. 诊断命令

固定只读命令为：`uname -srm`、`cat /etc/os-release`、`node --version`、Gateway 状态与版本查询、三频道插件运行时检查、频道探针、`pm2 status --no-color`、`systemctl is-active nginx`、`ss -ltn`、`df -P` 和受控数据根目录 `stat` 元数据查询。

未执行配置读取、日志内容读取、文件内容读取、安装、升级、重启、服务停止、部署、删除或防火墙变更。

## 5. 结论与未完成项

当前 237 的 Gateway 与告警接收器基础链路仍在运行，但不应直接覆盖部署前端/BFF。下一次需经用户单独授权进行第二轮只读预检，至少确认：

1. PM2 中实际运行的 Gateway 应用、工作目录和启动方式（仅摘要）；
2. OpenClaw 实际可执行路径及三频道运行状态；
3. 每个非回环监听的服务归属与是否符合公网/私网部署目标；
4. Nginx 或其他 443 反向代理的实际管理方式；
5. Admin BFF 的目标安装目录、服务账户、持久化目录、备份位置和可用磁盘。

以上确认完成后，才可提交前端/BFF 发布包、执行服务器备份并进行可回退部署。

第二轮结论：当前账户与监听状态不允许直接部署前端/BFF。后续应由具备系统部署权限的账户，或由当前服务所有者，先确认 443 服务归属、反向代理策略、Admin 安装目录和备份位置；不得用普通 SSH 账户覆盖现有监听或绕过 sudo 权限。

### 3.2 补充核验：sudo 与 443 入口（2026-07-20）

| 检查项 | 结果 | 判断 |
|---|---|---|
| `netinside` sudo | 以当前受控连接凭据执行 `sudo -v` 成功；随后执行 `sudo -k` 清除认证缓存 | 可在每次受控部署操作中临时取得系统权限；仍不具备免密 sudo，不应依赖遗留认证状态 |
| 443 监听归属 | Caddy 处于 active 状态并持有 443；存在 systemd 单元及显式配置参数 | 不安装或接管 Nginx；GAIOP 必须按 Caddy 现有配置的路由方式接入，且先备份活动配置 |
| Caddy 默认配置文件 | 可读，但本次未读取配置内容 | 后续部署仅可在完整备份后进行语法校验和最小路由变更；不得输出域名、证书或既有上游配置 |

本轮只执行了固定的 sudo 能力核验与监听/服务元数据查询；没有读取秘密、配置正文或日志，没有创建文件、安装软件、修改 Caddy、重启服务或改变网络。

### 3.3 Admin BFF 内网预配置（2026-07-20）

| 项目 | 结果 |
|---|---|
| 授权 | 用户明确授权安装固定编译工具链并继续 Admin BFF 内网预配置；明确要求暂不开放公网，仅保留隧道访问路线 |
| 初始两次预配置 | `npm ci --omit=dev --no-audit --no-fund` 在原生依赖编译阶段失败；受控发布器已自动删除阶段目录、恢复已有 Admin 项目/服务单元（如存在）并清除临时上传包。未启动服务、未修改 Caddy、443、防火墙、Gateway 或接收器 |
| 原因分类 | 仅保留为“原生编译工具链缺失”；未保存或输出 npm 原始日志、配置或注册表凭据 |
| 系统变更 | 通过受控 sudo 安装 `build-essential` 与 `python3`，并核验 `make`、`g++`、`python3` 可用；安装前 `build-essential` 未存在 |
| 最终预配置 | 发布包校验后完成生产依赖安装、Node 语法检查、受控目录创建、空白环境文件生成、systemd 单元生成与 `systemd-analyze verify` 校验 |
| 服务状态 | `gaiop-admin.service` 已生成但未启动、未启用；Admin BFF 尚未监听任何端口 |
| 网络状态 | Caddy、443、Firewall、Gateway 和 Syslog 接收器均未改动；未创建公网入口 |
| 敏感项 | 发布包明确排除 `.env`、SQLite、报告数据和本机依赖目录；`/etc/gaiop/admin.env` 仅为服务端填写的空白模板，权限为 root:gaiop 0640，本文不记录其内容 |
| 备份 | 每次预配置先创建受控的版本化服务器备份；最终成功预配置保留部署前快照，供后续启动验证失败时恢复 |

在最终预配置前发现并修正两项本地 Admin BFF 缺陷：其一，`server.listen` 未显式指定绑定地址，Node 默认可能监听全部网卡；其二，服务只读取项目 `.env`，不会读取 systemd 已注入的环境变量。现已改为默认 `GAIOP_BIND_HOST=127.0.0.1`，显式回环监听，并以“进程环境变量优先、项目 `.env` 兼容回退”加载配置。该修正通过生产构建与 44 项自动测试后，已重新打包并替换服务器上仍未启动的发布目录。此变更仅属于 GAIOP-Admin/BFF，不改动 GAIOP 后端。

随后又将 Admin SQLite 与应用备份目录参数化为 `GAIOP_ADMIN_DATA_DIR` 与 `GAIOP_ADMIN_BACKUP_DIR`，目标均在 `/var/lib/gaiop/admin` 持久化根内。最终发布器仅补充不存在的三个非敏感固定环境项（回环绑定与两个持久化目录）；如任一项存在但不等于预期安全值则会中止并回滚，绝不覆盖环境文件中的管理员密码、令牌或加密密钥。该版再次完成生产依赖校验、Node 语法校验和 systemd 单元校验，仍未启动或启用服务。

最终只读收尾核验：`gaiop-admin` 非运行、非启用；3000 无监听；无 npm 编译进程；无残留阶段目录；Caddy/443 与既有 Gateway、接收器均保持原状。磁盘最大使用率在预配置后为 58%，仍需在正式交付前纳入容量与备份策略验收。

经用户授权，另执行一次环境文件“字段存在性”只读检查：调用器仅返回指定字段是否为空，不读取、不输出或保存值。`AUTH_PASSWORD`、`DATA_SOURCE_ENCRYPTION_KEY`、`OPENCLAW_AUTH_TOKEN` 以及回环/持久化目录项均已存在；`SENSITIVE_CONFIG_ENCRYPTION_KEY` 尚为空。`OPENCLAW_AUTH_PASSWORD`、报告来源签名密钥和告警接收器 Token 均为空，符合当前“Token 鉴权、报告来源签名关闭、接收器内部 Token 未启用”的配置。敏感配置密钥当前会安全回退使用已存在的数据源加密密钥，但正式交付前仍建议为密钥分离单独填写该项。

### 3.4 Admin BFF 回环启动验证（2026-07-20）

用户随后填写了 `SENSITIVE_CONFIG_ENCRYPTION_KEY`。受控存在性检查确认四个当前必需项（初始管理员密码、数据源加密密钥、敏感配置加密密钥、Gateway Token）均存在；三个明确可选项继续为空。

启动动作固定为 `systemctl start gaiop-admin.service`，并且**未执行 enable**。首轮验证在服务延迟完成监听前未在 10 秒窗口内取得端口状态，验证器按预案请求停止；另一次监听归类将 `ss` 的简写格式误判为通配，也按预案停止。两次均未变更 Caddy、443、Gateway、接收器或防火墙。为避免运行时覆盖绑定地址，Admin BFF 新增硬性防线：仅接受 `127.0.0.1` 与 `::1`，其它地址在调用 `listen` 前直接拒绝启动。该修正经本地生产构建和 44 项自动测试后重新发布。

最终只读验证结果：`gaiop-admin` 处于 active，但未 enable；3000 仅监听 IPv4 回环；本机 `GET /api/health` 返回 HTTP 200；无 npm 编译进程与残留阶段目录。发布目录已确认包含回环绑定、运行时环境变量加载、回环地址硬性拒绝和持久化目录修正。Caddy/443 仍不代理 GAIOP，故当前不存在 GAIOP 公网入口。最大磁盘使用率为 60%。

本阶段服务回滚命令为 `systemctl stop gaiop-admin.service`；若需恢复发布文件，停止服务后恢复本阶段版本化备份的发布目录、环境文件和服务单元，再 `systemctl daemon-reload`。服务未 enable，因此服务器重启后不会自动恢复该 BFF，后续需单独确认开机自启策略。

### 3.5 本机 Admin BFF SSH 隧道（2026-07-20）

用户指定使用本机 3005（3002 已被既有本机 Node 进程占用）。通过受控本地启动器建立 `127.0.0.1:3005 → 237:127.0.0.1:3000` 的 SSH 转发，仅本机回环监听；该动作没有写入服务器、没有修改 Caddy/443 或防火墙。随后从本机经隧道请求 Admin BFF 健康接口，得到 HTTP 200。关闭该本地隧道进程即可撤销本机访问路径。

执行的非敏感命令类别：`sudo apt-get install -y build-essential python3`、受控发布包校验/解包、`npm ci --omit=dev --no-audit --no-fund`、`node --check server/index.js`、`systemctl daemon-reload`、`systemd-analyze verify`。没有执行 Caddy 配置读取或写入、端口开放、服务启动/停止、Gateway 配置变更、告警接收器变更或防火墙变更。

下一步由部署所有者直接在服务器填写 `admin.env` 中的初始管理员密码和加密密钥；该文件不能通过聊天、Git、发布包或隧道外传。填写完成后，需获得下一次明确授权，才可启动 `gaiop-admin.service` 并仅通过回环 SSH 隧道完成健康检查。

## 6. 回滚

本次为纯只读预检，无服务器回滚项。已关闭本次创建的本地 SSH 隧道；服务器服务和配置保持不变。

补充：对本次 Admin BFF 预配置，如后续启动验证失败，先停止并禁用新增的 `gaiop-admin.service`，再恢复本次版本化备份中的 Admin 目录、环境文件和服务单元，执行 `systemctl daemon-reload`，最后删除本次新增持久化目录中的空白初始化状态。若确认不再需要编译环境，可由系统管理员执行 `apt-get remove build-essential`；不建议为回滚而卸载系统 Python。

### 3.6 Gateway 会话恢复与最终部署验证（2026-07-20）

| 项目 | 记录 |
|---|---|
| 触发现象 | 用户登录后页面持续重连，Usage 接口提示“GAIOP 智能体服务暂未连接”。BFF 健康接口可用，但 Gateway 状态为 `disconnected`。 |
| 脱敏诊断 | BFF 服务账户到本机 `18789` 的 TCP 可达；未读取 Gateway 配置、环境文件或原始日志。由此排除 BFF 到 Gateway 的基础本机网络不通。 |
| 原因与修正 | 按 OpenClaw `v2026.5.4` Gateway 协议，带共享 Token/密码的本机受信任后端可使用 `client.id=gateway-client`、`client.mode=backend` 建连而不携带设备身份。Admin BFF 对仅限 `127.0.0.1`、`::1` 或 `localhost` 且已配置共享认证的连接采用该兼容身份；非本机连接仍保留既有 CLI/设备签名流程。协议版本仍固定为 3，未擅自升级 Gateway。 |
| 本地验证 | 已执行语法检查、生产构建和自动化测试：共 46 项通过。改动仅在 GAIOP-Admin 的 `server/gateway.js` 及其测试；不修改 GAIOP 后端、Gateway 配置、频道插件或频道消息链路。 |
| 部署批次 | 最终成功批次为 `20260720T095930Z`。部署器在切换前保留版本化备份，未覆盖 `.env`、SQLite、报告数据或任何密钥。 |
| 启动保护 | 首两次受控验证因启动窗口过短及 `ss` 简写格式被保守地中止，服务均被停止并恢复旧版；未产生公网监听。最终验证改为独立采样：仅接受 `127.0.0.1:3000`/`[::1]:3000`，并要求健康接口返回 Gateway `connected`。 |
| 最终结果 | `gaiop-admin.service` 为 active、未 enable；远程仅监听 `127.0.0.1:3000`；现有本机隧道 `127.0.0.1:3005 -> 237:127.0.0.1:3000` 已恢复，隧道健康检查返回 Gateway `connected`。 |
| 未触及范围 | 未读取或记录任何密钥；未修改 Caddy/443、防火墙、Gateway 配置、Syslog 接收器、频道插件或公网路由。 |

本次服务器侧非敏感操作类别为：停止/启动 `gaiop-admin.service`、版本化备份与发布目录切换、受控健康检查、回环监听检查和本机 SSH 隧道重建。服务回滚方法：先执行 `systemctl stop gaiop-admin.service`，再从 `/var/backups/gaiop/admin-prestage-20260720T095930Z/preexisting-admin` 恢复发布前目录，执行 `systemctl daemon-reload` 后启动服务；本次失败尝试的目录仍保留在同一版本化备份内，未删除。若仅需撤销本机访问，关闭本机 3005 隧道进程即可，不涉及服务器配置变更。
