# 2026-08-24 237历史报告登记盘点与无损修复记录

## 盘点结论

本次在 237 上对正式报告目录执行了只读元数据盘点，并在 `wizard.db` 在线副本上运行与 Admin 相同的报告扫描逻辑。

- 正式报告文件：243
- 审计 JSON：242
- 有效报告/审计配对：242
- `report_files` 已登记：242
- 可安全补登记：0
- 异常配对：0
- 审计 JSON 解析异常：0
- 未配对报告文件：1

唯一未配对文件为：

`_unattributed/report/napm_全局综述报告_20260731_134036.docx`

该文件没有对应审计 JSON，无法可靠确认报告 ID、生成时间、来源、大小登记关系和审计归属，因此按“不确定不写入”原则保护，不人工伪造 `report_files` 记录。

## 修复执行

- 受控入口：`scripts/Invoke-237ReportRegistryReconcile.ps1`
- `inspect`：在线备份到临时副本，扫描副本，无生产写入
- `repair`：候选数为 0，正确进入 no-op；未停止 Admin、未写入数据库、未删除报告
- 正式数据库：`PRAGMA integrity_check=ok`
- `report_files`：保持 242

早期一次 repair 预演曾因 no-op 条件的 shell 参数解析问题创建了数据库备份并短暂重启 Admin，但没有写入任何数据库记录或删除任何报告；随后已修正并重新执行，最终 no-op 验证通过。

## 生产状态

- Admin、Upgrade、Gateway、Caddy：active
- Admin：`127.0.0.1:3000`
- Admin、Upgrade、Gateway、内部 HTTPS、公网 HTTPS：健康检查正常
- 会话留存：inactive/disabled
- 报告后台登记同步：继续运行

本次只读取目录结构、审计 JSON 元数据、路径、数量、时间、大小和登记关系，没有读取报告正文。
