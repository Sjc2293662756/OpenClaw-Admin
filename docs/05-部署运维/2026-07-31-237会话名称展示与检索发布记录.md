# 237 会话名称展示与检索发布记录

> 发布日期：2026-07-31（UTC+8）
> 代码提交：`dev-yangshuo@2457f63`
> 发布批次：`20260731T032400Z`

## 1. 变更

- 会话管理列表首列标题由“会话”调整为“会话名称”。
- 每行优先突出显示对用户有意义的会话名称，Agent、渠道和 24h 活跃状态作为辅助标签显示在名称下方。
- 关键词检索增加实际显示的会话名称，WebChat 首条请求生成的固定标题可以直接检索。
- 搜索提示调整为“搜索会话名称 / agent / 渠道 / 对话方 / 模型”，不再向用户展示内部 key 概念。
- 内部会话 key 继续作为诊断检索字段保留，输入完整 key 或其中片段仍可定位会话，但页面不将它作为用户可见名称。

本次未修改真实会话 key、会话归属、删除权限、OpenClaw、Gateway 协议或 GAIOP 后端。

## 2. 测试

- 会话展示测试覆盖 WebChat 固定名称、名称片段检索、内部 key 片段检索和无匹配场景。
- Node：56 项通过。
- Vitest：13 个文件、46 项通过。
- TypeScript 和 Vite 生产构建通过。

## 3. 发布与验证

发布包：

- 条目数：287
- 大小：3558004 B
- SHA-256：`73d306fd711a368e3162196791d3c0f8bf0a130cbd6623f0b787d2562a21eaad`
- 未包含 `.env`、`data/`、`.bak`、本地数据库、图片删除记录或临时目录。

237 验证：

- 发布前已建立 Admin 目录、配置、服务单元和数据库迁移前备份。
- 服务状态：active。
- 健康检查：HTTP 200。
- Gateway：connected。
- 监听：`127.0.0.1:3000`。
- 会话分包：`assets/SessionsPage-CW2sgt60.js`，HTTP 200。
- 公共文案资产包含“搜索会话名称”和“会话名称”，不再包含“搜索 key”。
- 会话展示资产同时包含会话名称与内部 key 的检索字段。

## 4. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T032400Z'
```
