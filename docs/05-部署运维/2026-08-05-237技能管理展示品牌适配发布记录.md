# 237 技能管理展示品牌适配发布记录

> 发布日期：2026-08-05（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260805T063750Z`
> 运行代码提交：`82f8fa4 fix(skills): brand upstream display text`

## 范围与边界

技能管理页面的插件名称和描述由 Gateway `skills.status`（兼容回退 `skills.list`）返回，前端 Store 保留原始值。本批次只在 `SkillsPage` 的渲染边界增加展示转换：

- 不区分大小写的 `openclaw-napm` 显示为 `GAIOP-NAPM`；
- `OpenClaw NAPM` 显示为 `GAIOP NAPM`；
- 其他任意大小写的 `openclaw` 显示为 `GAIOP`。

未修改 Gateway RPC、插件元数据、技能实际标识、后端、数据库、权限、数据归属、真实业务数据、OpenClaw 核心或频道插件。

## 本地验证

| 验证 | 结果 |
|---|---|
| 展示转换单元测试 | Vitest 1 个文件、2/2 通过 |
| 生产构建 | `vue-tsc -b && vite build` 通过 |
| 空白与冲突检查 | `git diff --check` 通过 |

## 237 受控发布与验证

| 项目 | 值 |
|---|---|
| 本地归档 | `gaiop-admin-skills-branding-20260805T063750Z.tgz` |
| 条目数 | 310 |
| 大小 | 3,610,421 B |
| SHA-256 | `874214781dc9a89ee9c9fdf60dd2e748372856f4c73d49d6b45dba5f6b646f8e` |

受控暂存完成归档哈希校验、应用快照和受控数据库备份。启动验证结果：

- `gaiop-admin.service` 为 `active`，仅监听 `127.0.0.1:3000`，源绑定保护存在；
- 通过本地隧道访问 `/api/health` 返回 HTTP 200，Gateway 为 `connected`；
- 当前 Skills 分包 `SkillsPage-clQ-bjVQ.js` 返回 HTTP 200，包含 `GAIOP-NAPM` 和通用 `GAIOP` 展示转换文本；
- 未修改 Caddy、公网路由、DNS、防火墙、证书、Gateway、OpenClaw 核心或频道插件；未触发回滚。

如需恢复到本批次前的 Admin 程序和受控数据库备份，执行：

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260805T063750Z'
```

该回滚点仅覆盖本批次前的 Admin 程序、静态资源、服务单元、环境快照和受控数据库备份。
