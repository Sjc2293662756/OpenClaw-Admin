# 2026-08-18 237 Admin第一批留存首次真实清理与定时启用记录

## 1. 范围与当前状态

本次只启用 Admin 第一批低风险清理：

- 报告来源信封：超过 48 小时且通过现有统一候选校验后删除。
- Admin 升级上传中转：超过 24 小时且满足现有统一候选校验后删除。

本次没有启用或修改 Upgrade 清理、正式报告留存、会话留存、SQLite 备份、后端留存、系统日志留存或水位治理。水位监测继续保持原有 `active/enabled` 状态。

生产代码来自第十三批 Admin `dev-yangshuo` 的 `5221e65cfff178c70c81e5f8431cec93a8f475f7`，其中清理实现提交为 `889f8fedc6db8aa70b167405baa50aa6a9b9ff7d`。发布前核对的生产文件哈希与该版本一致。

## 2. 首次人工 one-shot

首次真实开启态验证使用正式 `gaiop-admin-retention-cleanup.service`，批量上限临时设为 1；上传中转使用独立空目录，避免本次验证处理实际中转目录。原始 `/etc/gaiop/admin.env` 未修改。

执行时间：2026-08-18 08:02:41 UTC。

| 类别 | 执行前候选 | 成功删除 | 失败 | 释放空间 |
| --- | ---: | ---: | ---: | ---: |
| `report_provenance_envelope` | 63 | 1 | 0 | 462 B |
| `admin_upgrade_upload_staging` | 0 | 0 | 0 | 0 B |

执行后核对：来源信封 63 → 62，来源信封字节 34,042 → 33,580；Admin staging 仍为 0；`wizard.db` `integrity_check=ok`。用户、会话、报告、交付和 `audit_logs` 业务计数均未变化，Admin、Upgrade、Gateway、Caddy 健康状态保持正常。

清理审计已写入预留和完成记录，完成记录为来源信封 `success=1`、`failed=0`，中转 `success=0`、`failed=0`。

## 3. 定时清理启用

启用时间约为 2026-08-18 08:10 UTC。为不改动原始环境文件，使用独立的 systemd 覆盖：

- `/etc/systemd/system/gaiop-admin-retention-cleanup.service.d/retention-enabled.conf`
- `/etc/gaiop/admin-retention-enabled.env`

覆盖文件只提供 `GAIOP_ADMIN_RETENTION_AUTO_DELETE=true`；原始 `/etc/gaiop/admin.env` 保持关闭态模板值，批量上限继续使用生产配置的 100。

启用时建立回滚点：

`/var/backups/gaiop/admin-retention-enable-20260818T085500Z`

定时器启动后立即执行了一次持久化补偿运行，按相同候选逻辑处理剩余数据：

| 类别 | 执行前候选 | 成功删除 | 失败 | 释放空间 |
| --- | ---: | ---: | ---: | ---: |
| `report_provenance_envelope` | 62 | 62 | 0 | 33,580 B |
| `admin_upgrade_upload_staging` | 0 | 0 | 0 | 0 B |

最终状态：来源信封 0 个、0 B；Admin staging 0 个、0 B；没有待清理候选。

## 4. 最终实时核对

- `gaiop-admin-retention-cleanup.timer`: `active/enabled`
- `gaiop-admin-retention-cleanup.service`: `inactive`, 最近结果 `success`
- 持久化开关覆盖：存在且为 `GAIOP_ADMIN_RETENTION_AUTO_DELETE=true`
- 清理锁：不存在
- 清理审计：最近完成记录来源信封 `success=62`、`failed=0`；累计预留/完成记录数量一致
- `wizard.db`: `integrity_check=ok`
- 业务计数：用户 6、会话 63、报告 228、交付 9、`audit_logs` 1595
- Admin 健康：200；Gateway、Upgrade、Caddy：`active`；公网 HTTPS：200
- 水位：低于 75% 正常；没有调用其他清理器

## 5. 故障排查说明

首次执行器的两次失败均为本地临时执行器问题，没有删除生产数据：一次是临时结果文件放在清理服务的 `RuntimeDirectory` 内，被 systemd 启动时清理；另一次是 `systemctl show` 不能直接证明 `EnvironmentFile` 的最终覆盖值。修正为独立临时目录、按文件顺序覆盖环境，并以正式 journal 完成记录作为成功依据后，真实 one-shot 成功。

`systemd-analyze verify` 返回成功；输出中的 `snapd.service` `RestartMode` 警告来自系统已有 unit，不属于本次 GAIOP unit，也未阻止启用。

## 6. 回滚边界

如需停止本批自动清理，只需先 `disable --now gaiop-admin-retention-cleanup.timer`，再按回滚点移除本次覆盖文件并执行 `systemctl daemon-reload`。不得通过回滚恢复已按规则过期删除的来源信封，不得删除清理审计或业务数据库。
