# 237 四角色页面访问与操作权限第一阶段收口发布记录

> 发布日期：2026-07-31（UTC+8）
> 分支：`dev-yangshuo`
> 发布批次：`20260731T050750Z`
> 发布提交：`dev-yangshuo@b55989a`

## 1. 收口范围

- 正式侧栏菜单保持可见；无权限点击和直接 URL 统一进入“无访问权限”页，明确模块、当前角色、返回和仪表盘入口。
- 基础用户只读本人对话历史，可查看、搜索和删除本人会话，只能列出和下载本人报告。
- 审计用户只读全部对话、会话和报告；可只读任务、频道、Skills、系统监控和审计信息，不提供写操作入口。
- 标准用户正常使用本人对话和会话，只能列出和下载本人报告；可查看频道、Skills 和系统监控安全状态，不再拥有任务、Skills 安装更新、模型切换和 Office/Wizard 管理写权限。
- 管理员保持全部页面和管理能力；报告删除、系统策略保存、配置、用户、升级等继续只允许管理员。
- 仪表盘使用 BFF 聚合摘要，不再为显示数量向浏览器返回任务、模型、Skills 或配置明细。
- `config.get`、频道、插件和 Skills 的非管理员读取改用字段白名单安全投影，不返回地址、路径、凭据或原始配置。
- `sessions.usage` 对基础/标准按会话归属投影并移除全局聚合旁路；全局费用统计只允许审计和管理员。

本次只修改 GAIOP-Admin 前端、Admin BFF、测试和文档；未修改 GAIOP 后端、OpenClaw 核心、频道插件、生产用户或生产业务数据。

## 2. 自动化与构建

- Node：66 项通过。
- Vitest：14 个文件、49 项通过。
- `vue-tsc -b && vite build`：通过。
- 覆盖四角色页面矩阵、直接 RPC 写入拒绝、会话/报告数据范围、频道安全配置、敏感字段投影、仪表盘摘要和账号安全既有回归。
- 测试使用内存或临时 SQLite；未读取或修改生产用户。

## 3. 发布包与 237 验证

- 条目数：289
- 大小：3565008 B
- SHA-256：`814069ac944816a35a27918b0eb2d527fda2366031dac5ead98990cfdb66afe2`
- 发布包排除 `.env`、`data/`、`.bak`、本地数据库、图片删除记录和临时目录。
- 发布前备份：由受控发布脚本建立 Admin 目录、配置、服务单元和数据库迁移前备份。
- 服务状态：active。
- 健康检查：HTTP 200。
- Gateway：connected。
- 监听：`127.0.0.1:3000`，仅回环 IPv4。
- 入口资产：`assets/index-C1nX9eHT.js`。
- 发布后再次执行237只读预检：暂存目录为0，Admin运行目录、固定回环绑定与持久化存储补丁均存在，Caddy和443状态未变化。
- 237 未创建或修改真实用户；四角色行为由隔离自动化验证，生产侧验证服务、鉴权、静态资产和部署哈希。

## 4. 回滚

```powershell
& '..\ops\237\Invoke-237AdminRollback.ps1' -ReleaseId '20260731T050750Z'
```

回滚只恢复本批次发布前的 Admin 代码、静态资源、配置、服务单元和受控数据库备份；不操作 GAIOP 后端或 OpenClaw 核心。
