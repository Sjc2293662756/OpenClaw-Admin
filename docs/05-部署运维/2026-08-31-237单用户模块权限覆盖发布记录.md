# 2026-08-31 237 单用户模块权限覆盖发布记录

## 发布范围

- Admin 从 `e6c0bc91ef688b8d8ca9b5efee31fdbe1f1cbb71` 更新至 `5f3efe6d2f18d9c44f8dc6f72dfab2b1993eb800`。
- 服务端新增 21 项固定模块目录、单用户 `allow/deny` 覆盖、恢复角色默认、乐观版本、事务内差异审计和统一有效权限 resolver。
- 登录及认证检查返回服务端计算的 `effectiveModules` 与 `permissionVersion`；导航、直接路由、REST、告警 SSE 和正式 Gateway RPC 使用该投影。
- 单用户编辑页新增分组权限表格、搜索、只看个人调整、草稿统一保存、差异预览和高风险二次确认。
- 四角色默认矩阵、对话工作台固定边界、会话归属、报告本人 404、系统级脱敏告警、审计和现有高风险动作边界保持不变。
- 未修改 GAIOP 后端、Receiver、OpenClaw/Gateway 核心、Skill、频道插件、防火墙、Caddy 或公网路由。

### 复核补丁

正式复核补丁 `752ccaaa0a8730d08b601911016680d78a1126aa` 修复三处权限一致性问题：审计列表和导出改由有效 `audit` 模块作为唯一查看门禁，basic/standard 个人 allow 不再被旧角色复判拒绝；Gateway `status/health` 正式登记到 `system`，deny 在转发前生效；`GET /api/users` 对所有获准角色统一移除 `mustChangePassword`，而登录和认证检查仍返回当前用户自己的首次改密状态。复核确认，会话留存全量概览、报告根路径及账户/配置/升级写操作的内层限制属于既有数据范围或高风险动作边界，不应随模块 allow 一并移除。

## 版本、发布包与回滚

| 项目 | 记录 |
|---|---|
| 远端分支 | `origin/dev-yangshuo` |
| 初次发布提交 | `5f3efe6d2f18d9c44f8dc6f72dfab2b1993eb800` |
| 复核补丁提交 | `752ccaaa0a8730d08b601911016680d78a1126aa` |
| 初次 Release ID | `20260830T171716Z` |
| 当前 Release ID | `20260831T015635Z` |
| 发布包 | 仅 `dist`、`server`、`package.json`、`package-lock.json`，428 个条目 |
| 当前发布包 SHA-256 | `89a7c6dca256a8e92dedd601faa7ab76365235ec7a435251a4132aa6ea589c17` |
| 当前回滚点 | `/var/backups/gaiop/admin-prestage-20260831T015635Z` |
| 回滚 | 使用 Release ID `20260831T015635Z` 的既有 Admin 回滚入口；不得覆盖生产 SQLite、告警历史、报告或运行时凭据 |

发布包路径白名单核验通过，不含 `.env`、SQLite、报告、告警历史、`node_modules`、备份或凭据。Stage 返回 `backupCreated=true`、`databaseBackupCreated=true`、`dependencyFailure=null`，且 `caddyChanged=false`、`networkChanged=false`。

## 数据迁移

- `users` 已幂等增加 `permission_version INTEGER NOT NULL DEFAULT 0`。
- `user_module_permission_overrides` 已按 `user_id/module_key/effect/updated_by/created_at/updated_at` 创建，`user_id + module_key` 为复合主键，effect 仅允许 `allow/deny`。
- 生产只读探针只读取 SQLite schema 元数据，不读取或输出用户、覆盖、审计、告警、报告或凭据内容；结果为 `migrationReady=true`。
- Stage 在切换前创建在线 SQLite 安全备份；未恢复、清理或复制生产业务记录。

## 测试证据

- `npm test` 完整通过：模块权限 8 项、Node 225 项、Vitest 32 个文件 171 项，共 404 项。
- `npm run build` 完成 `vue-tsc -b` 与 Vite 生产构建；仅保留既有大分包提示。
- `git diff --check` 通过。
- 覆盖四角色无覆盖逐项矩阵、allow/deny/恢复、角色变化、同角色隔离、未知 key、非法 effect、重复项、锁定、依赖冲突、409 版本冲突和非初始管理员拒绝。
- 覆盖认证投影、导航/路由、REST、SSE、RPC、安全只读与管理员动作分层、报告本人归属、会话归属及第一阶段 basic 告警/导出/偏好回归。
- 新增真实 HTTP 回归：basic/standard `audit` allow、auditor/admin 默认查看、auditor deny 同时覆盖列表和 Excel 导出；basic/standard `users` allow 与 auditor/admin 默认列表均不返回 `mustChangePassword`，登录/认证检查仍保留本人字段。
- 新增 RPC 回归：basic 默认 `system` 拒绝、basic allow、standard 默认和 standard deny 同时覆盖 `status/health`；`chat.send` 固定工作台能力不受影响。

## 237 受控发布与验证

1. `Invoke-237AdminStage.ps1` 成功完成备份、发布包校验、依赖复用、Node 语法检查、在线 SQLite 备份和目录切换，服务按流程保持未启动。
2. 第一次 `Invoke-237AdminStartVerify.ps1` 在 `HEALTH` 阶段未完成，已自动将服务停回安全状态；监听范围当时已是 `loopback-ipv4`，诊断归类为 `application-runtime-error`。
3. 既有独立启动探针随后确认 `127.0.0.1:3000` 与 Gateway `connected`；再次执行完整 start-verify 成功，返回 `admin-started-loopback-verified`、`rolledBackToStopped=false`、`publicRoutingChanged=false`。
4. 告警只读探针确认健康正常、未认证 `/api/events` 为 401、Receiver 上游 `connected`、`gapState=none`。
5. 本阶段只读探针确认健康正常；未认证模块目录、目标用户权限读取、权限写入、告警和报告接口全部为 401；新表和版本列迁移结构完整。

初次发布未注入生产告警、未触发外部频道，也未使用既有生产用户登录或读取、打印、复制凭据。生产报告本人 404、在线页面跳转和同角色隔离继续由自动化及真实浏览器事项覆盖；随后复核补丁只使用下述随机临时账户做最小权限验收，没有冒用既有用户。

### 复核补丁发布与真实权限验收

1. Release ID `20260831T015635Z` 的 stage 成功：`backupCreated=true`、`databaseBackupCreated=true`、`serviceStarted=false`、`dependencyFailure=null`，Caddy 和网络均未修改。
2. 第一次 start-verify 在 `HEALTH` 阶段失败，监听已是 `loopback-ipv4`；脚本立即将服务停回安全状态，诊断归类为 `application-runtime-error`，未进入持续重启。只做一次重试后返回 `admin-started-loopback-verified`，12 秒延迟稳定性复核通过。
3. 最终只读状态为 service active、唯一绑定 `127.0.0.1:3000`、Gateway connected。健康、未认证目录/目标投影/权限写/告警/报告拒绝和迁移结构均通过；浏览器 SSE 未认证为 401，Receiver 为 connected，gap 为 none。
4. `gateway237-admin-module-boundary-live-verify.cjs` 使用生产 BFF 的真实 HTTP/RPC 路径和随机临时账户完成最小验收，不读取现有用户密码或 Token，也不输出用户、Token、审计正文或凭据。结果为：审计 basic/standard allow、auditor/admin 默认、deny 全通过；`status/health` 的 basic 默认拒绝、standard 默认允许、standard deny 全通过；工作台固定 chat 边界通过；四类获准账户列表安全投影及本人认证字段分离通过。
5. 临时密码仅在远端验收进程内随机生成；所有临时会话先注销，临时覆盖、偏好和用户行在 `finally` 中删除，并以 `cleanupCompleted=true` 复核。登录、导出和退出形成的正常安全审计保留，不删除或篡改生产审计。

## 真实浏览器验收事项

仍需由持有现有测试账户的验收人员在真实浏览器完成：

1. 初始管理员打开一个普通用户编辑页，确认 21 项目录、三个分组、搜索、只看个人调整、风险/范围和锁定说明。
2. 对目标用户分别保存一个 allow 和 deny，核对差异预览、高风险二次确认、状态与数量；再执行全部恢复默认。
3. 保持目标用户在线，确认导航即时变化、已失权页面退出、SSE 重建；同角色另一个用户不受影响。
4. 分别核对告警 records/notifications/export 三键；通知独立允许但 records 拒绝时不得出现详情跳转，export 缺少 records 时不得保存。
5. 以目标用户验证直接 URL、REST 和正式 RPC 不能绕过 deny；模块 allow 只开放安全读取，高风险写动作仍拒绝。
6. 验证本人报告仍可访问、他人报告仍为 404，会话与 SSE 事件仍按本人归属。

## 回滚后复核

如需回滚，使用 Release ID `20260831T015635Z` 的既有 Admin 回滚入口恢复切换前程序目录和 unit；不得覆盖生产 SQLite、告警历史、报告、运行时配置或凭据。回滚后重新核验服务 active、回环监听、健康、Gateway、未认证拒绝、告警 SSE 和报告边界。新迁移表和列可保留为空闲兼容结构，不应执行破坏性降级迁移。
