# 2026-08-30 237 账户告警通知配置发布记录

## 发布范围

- Admin 从 `e63652c32000dceebc06ce392644d4e31764b89c` 更新至 `b61fa02101fabce6e840aef4c5b1aab4bb4b7905`。
- 已发布当前账户的服务端告警通知设置：实时提醒、声音提醒，以及轻微/重大/紧急的页面弹窗与告警通知矩阵。
- 页面弹窗是同级告警通知的展示方式：关闭通知会同步关闭并禁用该行弹窗，但保留原来的弹窗选择；重新打开通知后恢复。声音只在实际创建浮层时播放。
- 未实现或发布服务端账户收件箱、跨浏览器已读/清空、离线未读汇总、服务端分页，或浏览器 150 条内存上限调整。没有修改 Receiver、NAPM、OpenClaw/Gateway 核心、频道插件、Caddy、防火墙或外部通知。

## 版本、发布包与回滚

| 项目 | 记录 |
| --- | --- |
| 远端分支 | `origin/dev-yangshuo` |
| 发布提交 | `b61fa02101fabce6e840aef4c5b1aab4bb4b7905` |
| Release ID | `20260830T003354Z` |
| 发布包 | 仅 `dist`、`server`、`package.json`、`package-lock.json`，420 个条目 |
| 发布包 SHA-256 | `223b224983f00dcc70ebb289dd213293075d5ac257f931c33cbb7e2d0521cdca` |
| 回滚 | 使用 Release ID `20260830T003354Z` 的既有 Admin 回滚入口；不得覆盖生产 SQLite、告警历史、报告或运行时凭据 |

发布包不含 `.env`、SQLite、告警历史、模拟 Receiver、预览日志、`node_modules` 或凭据。受控 Stage 在切换前保留既有 Admin、systemd、运行配置与在线 SQLite 安全备份，并沿用失败自动回滚边界。

## 发布前验证

- 告警实时、通知队列、声音和生命周期定向 Vitest：5 文件、27 用例通过。
- `npm run test:vitest` 通过。
- `npm run test:node`：218 项通过。
- `vue-tsc -b`、`vite build` 和 `git diff --check` 通过。

## 237 受控发布与验证

- 已执行既有 `Invoke-237AdminStage.ps1`，再执行 `Invoke-237AdminStartVerify.ps1`，结果为 `admin-started-loopback-verified`，未回滚到停止状态，未改动公网路由。
- 发布后只读监听检查确认 `gaiop-admin.service` 为 `active`，仅绑定 `127.0.0.1:3000`；运行环境文件仍启用，源码绑定保护存在。
- 未注入生产告警，未触发外部频道。浏览器声音受自动播放策略和不制造生产告警的边界限制，未声称已在 237 实听。

## 登录后验收边界

部署过程未读取或使用用户登录凭据。请使用现有账户进入“系统设置 → 告警通知设置”，确认中文/英文、明暗主题、窄屏与保存交互；下一次自然到达的 NAPM 告警可继续验证 Receiver → Admin → 当前账户通知的真实链路。关闭账户提醒期间，实时游标仍会推进且不会在重新开启后补放。
